---
id: P-5NAAPaWR
type: project
title: design-correction-project-flag-is-outside-chat-not-model-in-chat-command
created_at: 2026-06-24T18:51:55Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 6483a5766cd15ff5c4182ce6714f2139f2225ca5cae6ccdd6be7455c9a29b4f7
related: [TRULY-FINAL-kiro-windows-allowlist-bug-5376-7431-blocks-correct-commands-unfixable, CORRECTED-cd-prefix-breaks-allowlist-kiro-4579-project-flag-is-the-fix]
---

DESIGN CORRECTION (the user, 2026-06-24): `--project` is for OUTSIDE-the-chat invocation (hooks/scripts running from a non-project cwd) — it is WRONG to put it in the MODEL's in-chat command. Asking the kiro-cli model to run `cmk remember ... --project "C:\..."` in chat is the wrong design: (a) it relies on the model running commands, which kiro's Windows allowlist bug (#5376/#7431) blocks, and (b) it's fragile to make the model construct absolute paths. The RIGHT design: explicit capture in kiro-cli should go through a HOOK (outside the chat), the SAME way automatic capture does — the model should NEVER run cmk remember/search. The hooks already work (kiro runs them, no allowlist gate; gate4 proved auto-extract writes facts). So: (1) REVERT the kiro-cli agent PROMPT instruction telling the model to run `cmk remember/search --project` — instead the prompt says memory is automatic via hooks, the model does nothing. (2) KEEP the --project flag on cmk remember/search/mcp serve (it's CORRECT for outside-the-chat use: the install bakes --project into the mcp.json args so the SERVER/HOOK knows the project; and Claude Code/scripts can use it). (3) keep includeMcpJson:false (popup fix). (4) For 'explicit remember this' UX in kiro-cli: the proper path is a userPromptSubmit HOOK that detects intent + runs cmk remember ITSELF (outside the chat) — but that's a follow-up; for v0.4.0, automatic-capture-every-turn already captures everything (the user never NEEDS to say 'remember this', like AgentCore). NET v0.4.0: kiro-cli memory = fully automatic via hooks (works); the model is not asked to run any memory command (correct design); explicit-via-hook is a v0.4.x enhancement. This is CLEANER than the in-chat-command approach and sidesteps kiro's allowlist bug entirely. The trusted-commands cmk remember/search entries can stay (harmless, useful if a hook or the user runs them) OR be removed from the prompt's reliance.

**Why:** The user clarified: --project is for outside-the-chat invocation (hooks/scripts), and putting it in the model's in-chat command is wrong design — it relies on the model running commands (blocked by kiro's allowlist bug) and is fragile. The right design: memory is automatic via hooks (which work); the model never runs memory commands. This sidesteps kiro's bug entirely and matches how real kiro projects (AgentCore) work.

**How to apply:** REVISE the kiro-cli agent prompt: REMOVE the instruction to run `cmk remember/search --project` in chat. Replace with: 'Your memory for this project is captured automatically each turn and injected at session start by the kit hooks — you do not need to run any memory command. Do NOT call MCP memory tools (they do not work here).' KEEP: --project flag on cmk remember/search/mcp serve (correct for outside-the-chat: install bakes it into mcp.json args for the server; Claude Code/scripts use it), includeMcpJson:false (popup), normalizeProjectPath, resolveMcpProjectRoot. The trusted-commands cmk remember/search can stay (harmless). FOLLOW-UP (v0.4.x, tasks.md): a userPromptSubmit hook that detects 'remember this' intent and runs cmk remember OUTSIDE the chat (the proper explicit-capture path that bypasses the allowlist bug). Update tests (prompt no longer asserts the model runs commands). Then consolidate + PR + ship. Relates the allowlist bug #5376/#7431, the --project flag, the popup fix.
