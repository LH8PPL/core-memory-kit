---
id: P-E2WMaTAE
type: project
title: TRULY-FINAL-kiro-windows-allowlist-bug-5376-7431-blocks-correct-commands-unfixable
created_at: 2026-06-24T18:50:00Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 170420d0c11d422b325cfbbf3a0bd2263da86a5b96ad1a29ad5c69016b3fea66
related: [CORRECTED-cd-prefix-breaks-allowlist-kiro-4579-project-flag-is-the-fix, CONFIRMED-kiro-bugs-5873-5662-custom-agents-dont-get-mcp-too]
---

TRULY FINAL + EVIDENCE-COMPLETE (2026-06-24): explicit memory in kiro-cli is BLOCKED BY KIRO'S OWN WINDOWS ALLOWLIST BUG (#5376/#7431), unfixable from the kit. FULL PROOF CHAIN: the agent ran the PERFECT command — `cmk remember "..." --why "..." --type user --project "C:\Temp\kiro-cli-gate6"` (NO cd, correct Windows --project path, exactly as the prompt instructed). The allowlist HAS `^cmk remember .*` which this command MATCHES. When I run that EXACT command myself it LANDS (P-YYJF6Q99, rich fact, gate6 audit 0→1). YET the agent's identical call produced ZERO audit + NO <tool_result> in the transcript (cut off after </tool_call>) → kiro BLOCKED/DROPPED it. So: command correct ✅, --project correct ✅, no-cd ✅, allowlist match ✅ — and kiro STILL blocks it. This is confirmed Kiro bug #5376 ('execute_bash rejects ALL commands despite allowedCommands' — bad regex) + #7431 ('shell tool fails on Windows native kiro-cli 2.0'). Kiro's allowlist matching is broken on Windows for custom agents — it rejects commands that genuinely match. EVERY kit lever is exhausted + correct: MCP route (#5873 custom agents no MCP tools), CLI route, --project flag, normalizeProjectPath, no-cd prompt, allowlist regex — ALL correct, kiro blocks anyway. NOT FIXABLE FROM THE KIT. WHAT WORKS regardless: the HOOKS (agentSpawn/stop) — kiro runs THOSE itself (not the model, no allowlist gate), proven gate4 (2 rich facts auto-extracted). So AUTOMATIC memory works (capture+extract+inject); ALL model-initiated explicit memory is blocked by kiro's Windows allowlist/tool-wiring bugs. FINAL v0.4.0: kiro-cli = automatic memory via hooks (works, like real projects AgentCore which ALSO don't use model-tool-calls); explicit remember/search is a documented kiro-cli-on-Windows limitation (#5376/#7431/#5873/#4579). Popup FIXED. KEEP the code (--project + resolveMcpProjectRoot + normalizeProjectPath — all correct + help Claude Code + work when kiro fixes its Windows allowlist). SHIP: Claude Code + Kiro IDE fully work; kiro-cli automatic works + documented explicit gap. STOP — every lever exhausted, proven kiro's bug.

**Why:** Evidence-complete: the agent ran the perfect command (--project, no cd, matches the allowlist) and kiro STILL blocked it (no audit, no tool_result), while the identical command lands when I run it. This is confirmed Kiro Windows allowlist bugs #5376/#7431 — kiro rejects commands that genuinely match allowedCommands. Every kit lever (MCP/CLI/--project/no-cd/allowlist) is correct; kiro blocks anyway. Not fixable from the kit. Hooks work (kiro runs them). Ship with automatic memory + documented explicit gap.

**How to apply:** SHIP v0.4.0 NOW: (1) KEEP all the code (--project flag on cmk remember/search/mcp serve, resolveMcpProjectRoot, normalizeProjectPath, includeMcpJson:false popup fix, @ name fix, D-198 location) — all correct, help Claude Code, and work the moment kiro fixes its Windows allowlist. (2) kiro-cli prompt is fine as-is (instructs the right command; kiro just blocks it — not the kit's fault). Maybe soften to lean on hooks first. (3) DOCUMENT in CHANGELOG + cut-gate-kiro-cli + README: 'kiro-cli (Windows): AUTOMATIC memory via hooks works (capture+inject, like AgentCore); explicit model-run remember/search is currently blocked by Kiro Windows bugs #5376/#7431 (allowlist rejects matching commands) + #5873 (no MCP tools for custom agents) — the kit emits the correct commands; track upstream.' (4) tasks.md follow-up. (5) consolidate branch fix-kiro-cli-mcp-project-resolution → ONE PR → two-pass review → merge → v0.4.0 tag. Claude Code + Kiro IDE = full. kiro-cli = automatic + documented gap. STOP iterating — proven unfixable; the win is automatic-memory-works + popup-fixed + all-code-correct.
