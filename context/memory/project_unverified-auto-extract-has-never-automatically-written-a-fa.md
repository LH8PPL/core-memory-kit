---
id: P-TXWT72PM
type: project
shape: State
title: 'UNVERIFIED: auto-extract has never automatically WRITTEN a fact on Kiro (0 auto-extract-sourced facts)'
created_at: 2026-07-09T06:18:08Z
write_source: user-explicit
trust: high
recurrence_count: 1
source_file: user-explicit
source_line: 1
source_sha1: c6f7bc18b5482b64433ff16be98af1f9e75e839767e44f6e3af1e49428461e83
related: [auto-extract-works-on-kiro-confirmed-by-direct-probe-the-not, kiro-gate-v0-5-0-complete-all-3-sessions-pass-kit-works-end]
---

CORRECTION (the user caught it, 2026-07-09): Kiro's automatic FACT-WRITE path is UNPROVEN, not verified. Across all Kiro gate folders: 0 facts with write_source:auto-extract, all 5 facts are user-explicit (from mk_remember), and every automatic extract.log run returned nothing_durable. I proved the extractor auto-TRIGGERS (extract.log written by the Stop hook, no manual command) AND that the engine produces parseable output given a good turn (direct probe) — but NOT that a real Kiro turn flows hook→extractor→written-fact automatically end-to-end.

**Why:** The user's precise challenge: "you say it works, but did it automatically run?" — exposing that my two probes (P-FSaL7MCB) proved the ENGINE works in isolation and the hook TRIGGERS, but NOT the automatic end-to-end fact-write. Evidence: C:\Temp\kiro-gate-v050 has 5 user-explicit facts (all from mk_remember the model called explicitly) and 0 auto-extract facts; C:\Temp\kiro-coldopen-v050 has 0 facts; every extract.log entry across both = nothing_durable/obs:0. So the automatic path (Kiro Stop hook → detached auto-extract child → runAutoExtract → written fact with write_source:auto-extract) has produced ZERO facts on Kiro. Two candidate causes, indistinguishable from current data: (a) auto-extract legitimately SKIPPED because in S1 the model ALSO called mk_remember on the same preferences, so the content was already explicitly saved and auto-extract dedup/skip-demoted it (assistant-origin candidates demote a trust level; if the fact already exists it's a dup) — this would mean auto-extract WORKS but had nothing new; (b) a genuine Kiro automatic-path defect. The direct probe (P-FSaL7MCB) proved the extraction LLM through kiro-cli returns TRUST_HIGH + PERSONA CANDIDATE lines for a clean preference turn — so the engine is fine — which makes (a) more likely, but it is NOT PROVEN. This is the D-169 automatic-path discipline: the load-bearing claim is 'captured automatically with no command,' and that specific claim is untested on Kiro. My earlier P-TBGK35LC 'Kiro COMPLETE' overstated this — Kiro's capture/inject/recall/wedge/privacy/explicit-write are proven; its automatic FACT EXTRACTION is not.

**How to apply:** Prove it with a CLEAN targeted test: a Kiro turn where the user states a NEW preference the model does NOT explicitly mk_remember (e.g. state a preference casually mid-work, or a preference for something the model just does rather than saving) → then check for a context/memory/*.md with write_source:auto-extract that appeared with NO mk_remember call. The turn-file the detached child reads must contain a real USER_TURN with the preference. Critical: run it in a folder ONLY Kiro touches (no assistant CLI commands while open — the P-4VAY63ST contamination). If a write_source:auto-extract fact appears → Kiro automatic path PROVEN, Kiro fully green. If it does NOT (and the turn genuinely had a new extractable preference the model didn't save explicitly) → real Kiro automatic-extract bug to fix (check: did the Stop hook spawn the child with the right CMK_PROJECT_DIR + turn-file? did the child's runAutoExtract get a populated USER_TURN or an empty one? is the Kiro capture writing the user turn into the turn-file at all — recall Kiro S1 now.md had 0 user-turn headings, which HINTS the Kiro capture may only pass the assistant turn to the extractor, which WOULD break bi-turn extraction). That last hint (0 user-turn headings in Kiro now.md) is the most likely real-bug candidate and should be checked FIRST: does capture-turn on Kiro write the USER_TURN into the extract turn-file, or only the assistant turn? Relates P-FSaL7MCB (engine works), P-TBGK35LC (the overstated 'complete'), P-4VAY63ST (contamination), the bi-turn extraction design (auto-extract.mjs needs USER_TURN to catch user-stated preferences), D-169.
