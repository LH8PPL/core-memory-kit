---
id: P-EC97GY7E
type: project
title: d198-fix-built-kiro-dir-location-open-file-uri-resolution-question
created_at: 2026-06-24T04:23:45Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 30984d39a97473099304b8d4d0d8b6397433c92d94f9f6430ca7d78ec5c47136
related: [TRUE-root-cause-kiro-cli-agent-config-wrong-location-no-hooks-fire, kg-guard-still-fails-with-star-matcher-hook-not-firing-or-env-payload]
---

D-198 FIX BUILT + UNIT-GREEN + SCHEMA-VALIDATED, but ONE runtime path question open (2026-06-24, branch fix-kiro-cli-agent-location). THE FIX: rewrote kiro-cli-agent.mjs to write the agent to ~/.kiro/agents/cmk.json (NOT ~/.aws/amazonq/cli-agents/) + register {"chat.defaultAgent":"cmk"} in ~/.kiro/settings/cli.json (the load-bearing step our own research had but the impl dropped). Dropped the invalid top-level `managedBy` (kiro-cli `agent validate` REJECTS unknown fields) — ownership marker now in the `description` field ('[claude-memory-kit]'). Agent name 'cmk' (q_cli_default doesn't exist in kiro; built-in default is kiro_default). Param: kiroDir (awsDir kept as back-compat alias so callers unchanged). Full suite 2258/0, references green. LIVE-VALIDATED via real `kiro-cli agent validate` — caught TWO bugs unit tests missed: (1) `includeMcpJson` + `useLegacyMcpJson` are the SAME setting → supplying both = 'duplicate field' error → dropped includeMcpJson, kept useLegacyMcpJson:false; (2) after that, validate PASSES (clean) when resource files are present. ★ OPEN QUESTION (do NOT guess — primary source): the validator resolves `file://AGENTS.md` (prompt) + `file://.kiro/steering/cmk.md` (resources) RELATIVE TO THE AGENT FILE'S DIR (~/.kiro/agents/), not the project root. If kiro-cli does the same AT RUNTIME, our file:// paths point at ~/.kiro/agents/AGENTS.md = WRONG (AGENTS.md is in the project root). MUST verify how kiro-cli resolves agent prompt/resources file:// paths (agent-dir-relative vs project-cwd-relative) before claiming the inject/steering legs work. The OLD impl had the same file:// paths but in ~/.aws so it never ran at all — this is newly-reachable. STILL TODO: resolve the file:// path question, then rebuild+install (EBUSY: close Claude Code first), cmk install --ide kiro in gate, re-run the INSTRUMENTED PROBE (C:\tmp\kg-probe*.mjs) to confirm hooks FIRE this time, then KG-guard. Update D-197→D-198 (location was THE bug; matcher was secondary). The gate's q_cli_default.json.kg-probe-backup + the probe-wired config are still in ~/.aws (stale location, harmless now).

**Why:** The real root-cause fix (D-198: agent config to ~/.kiro/agents/ + cli.json default registration) is built, unit-green, and SCHEMA-validated against real kiro-cli (which caught the includeMcpJson dup + the managedBy rejection). But a runtime path-resolution question remains: kiro-cli resolves file:// resources relative to the agent-file dir, which would break our project-root AGENTS.md/steering refs — must verify before claiming inject works.

**How to apply:** Next session: (1) PRIMARY SOURCE — how does kiro-cli resolve agent `prompt`/`resources` file:// paths? Check kiro.dev/docs/cli/custom-agents + test live (put AGENTS.md in project root, run kiro-cli from there, see if the agent loads it). If agent-dir-relative, switch to absolute paths or a different ref mechanism; if cwd-relative, we're fine. (2) rebuild tgz (packages/cli; close Claude Code for EBUSY), npm i -g, cmk install --ide kiro in C:\Temp\kiro-gate2. (3) verify ~/.kiro/agents/cmk.json + ~/.kiro/settings/cli.json land + `kiro-cli agent list` shows '* cmk'. (4) re-run the instrumented probe (point preToolUse+agentSpawn at C:\tmp\kg-probe.mjs/kg-probe-spawn.mjs, run ls in kiro-cli, confirm BOTH logs appear → hooks FIRE). (5) restore real guard command, re-run KG-guard → expect BLOCKED. (6) two-pass review, update D-197→D-198 in DECISION-LOG/CHANGELOG/cut-gate/tasks.md, PR. Branch: fix-kiro-cli-agent-location.
