---
id: P-234QKaCL
type: project
title: CONFIRMED-kiro-bugs-5873-5662-custom-agents-dont-get-mcp-tools
created_at: 2026-06-24T15:52:25Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: de9836cb4e96a16ffce9922cf11f74e83510eecda87280594dcfc05a3398325a
related: [kiro-cli-mcp-list-reveals-agent-server-link-broken-includemcpjson-shows-legacy, DEFINITIVE-kiro-cli-does-not-pass-mcp-json-env-to-stdio-server]
---

★★★ ROOT CAUSE = CONFIRMED OPEN KIRO-CLI BUGS (2026-06-24, found in kirodotdev/Kiro issues — the user's call to check upstream). The mk_remember silent-data-loss in kiro-cli is NOT a kit bug and NOT fixable by kit config. TWO confirmed open Kiro issues: (1) #5873 "MCP tools not injected into the LLM tool list for workspace-defined agents (.kiro/agents/*.json)" — "Any agent defined via .kiro/agents/*.json does NOT receive MCP tools in its tool list. ONLY the built-in kiro_default agent receives MCP tools. The bug is in how kiro-cli bridges MCP server tools to the LLM for custom agents." THIS IS EXACTLY our mcp-list output (server on kiro_default; our cmk agent shows [legacy]). (2) #5662 "Workspace agents cannot access MCP server tools despite successful server initialization" — "both modern inline mcpServers config AND legacy useLegacyMcpJson:true — NEITHER works for workspace agents." This KILLS the project-local-agent fix I was about to build (inline mcpServers doesn't work for custom agents either). CONCLUSION: the kit MUST use a custom agent (cmk) to be the default + carry hooks, but kiro-cli has a CONFIRMED BUG where custom agents don't get MCP tools wired to the LLM — so mk_remember silently fails (the LLM 'calls' it but kiro never routes it; the agent hallucinates 'saved'). NO kit-side config fixes this (inline + includeMcpJson + useLegacyMcpJson all confirmed broken for custom agents). The --project arg fix + @claude-memory-kit fix were both correct improvements but can't overcome kiro not wiring tools to custom agents at all. Other relevant: #9657 (stdio server saved but never registered in fresh sessions), #9178 (MCP servers fail to load on new window, no UI feedback/logs), #9713 (stdio servers spawn visible CMD windows on Windows — OUR POPUP, confirmed kiro bug), #6883 (kiro-cli should log spawned MCP command/args), #3909 (MCP config issues between CLI and IDE). DECISION for v0.4.0: kiro-cli MCP-driven mk_remember is BLOCKED BY KIRO (not us) — document as a known kiro-cli limitation w/ the upstream issue links; the kit's CLI AGENT HOOKS (agentSpawn inject + stop capture) DO work (they don't need MCP-tool-wiring — they run cmk hook directly), so AUTOMATIC memory via hooks works in kiro-cli; only the EXPLICIT mk_remember MCP tool is affected. The IDE + Claude Code are unaffected. The PR (fix-kiro-cli-mcp-project-resolution) is still correct + merge-worthy (right code, helps Claude Code, works the moment kiro fixes #5873).

**Why:** The user's call to check upstream found the definitive root cause: kiro-cli issues #5873 + #5662 confirm custom agents (.kiro/agents/*.json) do NOT receive MCP tools — only kiro_default does — and NEITHER inline mcpServers NOR includeMcpJson works for them. So mk_remember silently fails in kiro-cli by a CONFIRMED KIRO BUG, not a kit bug, and no kit config fixes it. This kills the project-local-agent fix and settles the v0.4.0 stance.

**How to apply:** DO NOT build the project-local-agent fix (#5662 confirms inline mcpServers doesn't work for custom agents). The kit's code fixes (--project arg, @claude-memory-kit name) are correct + merge the PR (they help Claude Code + are right-the-moment-kiro-fixes-#5873). For v0.4.0: document kiro-cli MCP-driven mk_remember as a KNOWN KIRO LIMITATION (link #5873 + #5662) in CHANGELOG + the cut-gate-kiro-cli doc + README Kiro section + a tasks.md follow-up. CRUCIAL scope: the kit's kiro-cli AGENT HOOKS (agentSpawn→inject, stop→capture) work WITHOUT MCP tools (they run `cmk hook` directly) — so AUTOMATIC memory (capture every turn + inject at start) works in kiro-cli; only the EXPLICIT mk_remember tool is blocked. VERIFY that hook-capture actually persists (it resolves project from stdin cwd, which kiro provides to hooks per the agentSpawn probe). Subscribe/watch #5873. Also file/confirm #9713 for the popup. The IDE works fully (it also can't do file-based preToolUse but capture/inject work). Don't chase a kit fix for a confirmed upstream bug.
