---
id: P-GELYMC3Q
type: project
title: kiro-cli-mcp-popup-is-6th-cross-agent-instance-mcp-command-never-node-direct
created_at: 2026-06-24T10:30:08Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: b11308761d401c614843afef16f84f846e983bb58422b95ce8cc6e5854e7e955
related: [kiro-cli-popup-fix-is-task81-node-direct-windowshide-pattern-already-in-kit, d198-shipped-pr225-kiro-cli-capture-inject-proven-live]
---

THE DOCS TRAIL settles the kiro-cli popup completely (2026-06-24). The Windows console-popup is a KNOWN, TWICE-FIXED class: Task 81 (v0.2.0 cut-blocker) fixed the lazy-compress SPAWN with node-direct (process.execPath + .mjs, NOT the .cmd/.ps1 shim) + windowsHide:true; D-190 (2026-06-22, "the 5th live-found cross-agent cut-blocker") found Task 81's fix was Claude-Code-ONLY and the KIRO HOOK path still flashed → fixed by self-resolving the bin path inside injectContext so EVERY caller gets node-direct. BUT THE GAP the trail reveals: BOTH fixes were for the detached-CHILD spawns (lazy-compress). The MCP SERVER registration was NEVER converted — install-agent.mjs:32 (Claude) + install-kiro.mjs (Kiro) BOTH still use {command:'cmk', args:['mcp','serve']} (the shim). Claude Code spawns the MCP child HEADLESS so command:'cmk' never flashed there; KIRO-CLI wraps it in `cmd.exe /C cmk mcp serve` (verified PID 16468→node cmk.mjs) → the PERSISTENT visible window. THIS IS THE 6TH INSTANCE of the documented D-185/186/187/188/190 cross-agent class ("a Claude-Code-TOLERATED mechanism that BREAKS on Kiro"): command:'cmk' is tolerated by Claude Code's headless spawn but breaks under kiro-cli's cmd.exe-wrapping spawn. THE FIX (the established Task-81/D-190 pattern): emit the MCP entry command as node-direct (process.execPath for node + resolved bin/cmk.mjs path) instead of the 'cmk' shim — for the kiro-cli MCP at minimum (Claude can stay 'cmk' since it's headless, OR convert both for consistency). The kit ALREADY has the path-resolution machinery (resolveCompressLazyPath's import.meta.url→../bin/<x>.mjs pattern). Manually tested node-direct on the gate mcp.json: the server starts fine (smoke test passed); awaiting the user's on-screen confirm that the persistent window is gone.

**Why:** The docs trail (Task 81 + D-190) shows the popup class was fixed twice — but only for the lazy-compress detached spawn, NEVER for the MCP server registration (still command:'cmk' in install-agent.mjs + install-kiro.mjs). Claude Code tolerates command:'cmk' (headless spawn); kiro-cli wraps it in cmd.exe → the persistent window. This is the 6th instance of the documented Claude-tolerated/Kiro-breaks cross-agent class, with the SAME established node-direct fix.

**How to apply:** FIX (follow Task 81/D-190's node-direct pattern): change the MCP entry command from 'cmk' to node-direct. Resolve the node binary (process.execPath at runtime, or a stored path) + the cmk.mjs entry (the resolveCompressLazyPath import.meta.url→../bin/cmk.mjs pattern). Emit {command:<node>, args:[<cmk.mjs>,'mcp','serve']}. Scope: install-kiro.mjs MCP_ENTRY (the one that flashes) at minimum; consider install-agent.mjs MCP_ENTRY too for consistency (Claude is headless so cosmetic-neutral there, but node-direct is more robust). CAVEAT: a hardcoded absolute path is machine-specific — but mcp.json is per-project + gitignored-ish (re-generated by cmk install), and process.execPath/import.meta.url resolve at install time on THAT machine, so it's correct per-install (like the lazy-compress path already is). Test-first: a spawn-descriptor/MCP-entry unit test asserting node-direct (no bare 'cmk' shim). Live-verify the persistent cmd.exe window is GONE in kiro-cli. The kiro-cli HOOKS may also flash (fast) — same fix applies to kiroHookCommand if so, but the IDE side needs cmd.exe/c for WSL (verify). v0.4.x UX task. Restore the gate config via `cmk install --ide kiro` after manual testing.
