---
id: P-RJ2QDBCQ
type: project
title: mk-remember-kiro-cli-fix-cmk-project-dir-env-project-local-agent
created_at: 2026-06-24T14:04:17Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 6bba0c782edb443e0d71bd4206bf8e231f28675c6af0b88e953e6b36c21a25d8
related: [CUT-BLOCKER-mcp-mk-remember-silent-dataloss-kiro-cli-claude-project-dir-only, d198-shipped-pr225-kiro-cli-capture-inject-proven-live]
---

CUT-BLOCKER mk_remember-kiro-cli ROOT CAUSE CONFIRMED + fix direction (2026-06-24): PROVEN via decisive test — `cmk mcp serve` launched FROM the gate cwd (no CLAUDE_PROJECT_DIR) writes correctly (written_to: C:\Temp\kiro-cli-gate\context\MEMORY.md, accepted:true, audit 3→4). So process.cwd() is NOT broken. THE BUG: kiro-cli launches the MCP server with cwd != the project (its own dir), so subcommands.mjs:1907 `projectRoot = CLAUDE_PROJECT_DIR ?? process.cwd()` resolves wrong (CLAUDE_PROJECT_DIR is Claude-only/unset in kiro; process.cwd()=kiro's dir). The Session-1 facts vanished (no stray context/ in HOME → writes errored silently or misfiled). PRIMARY SOURCE (kiro.dev/docs/cli/custom-agents/configuration-reference): the agent mcpServers entry supports `command/args/env/timeout/oauth` — `env` IS supported, but there is NO `cwd` field, and kiro sets no documented workspace/project env var. FIX OPTIONS: (1) ENV INJECTION — set the MCP entry's `env: {CMK_PROJECT_DIR: <absolute project path>}` AND make subcommands.mjs:1907 ALSO honor CMK_PROJECT_DIR (not just CLAUDE_PROJECT_DIR). BUT the GLOBAL agent (~/.kiro/agents/cmk.json) can't bake a per-project path. SOLUTION: write a PROJECT-LOCAL agent (<project>/.kiro/agents/cmk.json) — which kiro-cli resolves with workspace precedence over global — that bakes env:{CMK_PROJECT_DIR: <thisProjectAbsPath>}. cmk install --ide kiro runs per-project so it KNOWS the path. Trade-off: project-local agent = committed to the repo (good — travels) but the absolute path is machine-specific (bad — won't match a teammate's clone). BETTER FIX (2): make the MCP server WALK UP from process.cwd() to find the nearest context/ dir (agent-agnostic, no env, no baked path) — robust to ANY launcher cwd. But if kiro launches MCP from a cwd with no context/ ancestor, walk-up fails too. (3) HYBRID: env CMK_PROJECT_DIR in a project-local agent (exact, travels via re-install) + walk-up fallback. RECOMMEND: add CMK_PROJECT_DIR support to mcp serve (subcommands.mjs:1907) + emit a PROJECT-LOCAL kiro agent with env:{CMK_PROJECT_DIR} (re-resolved per machine by cmk install). Test-first: mcp serve honors CMK_PROJECT_DIR over cwd; install writes project-local agent with the env. v0.4.0 CUT-BLOCKER.

**Why:** Confirmed the mk_remember silent-data-loss root cause (kiro launches MCP from non-project cwd; CLAUDE_PROJECT_DIR is Claude-only) and the fix direction: the agent mcpServers entry supports `env` (not cwd), and a project-local kiro agent can bake env:{CMK_PROJECT_DIR:<absPath>} which mcp serve must honor. A v0.4.0 cut-blocker — capture must persist in kiro-cli.

**How to apply:** IMPLEMENT (test-first): (1) subcommands.mjs:1907 — projectRoot = CLAUDE_PROJECT_DIR ?? CMK_PROJECT_DIR ?? process.cwd() (add the kit's own env, agent-neutral). (2) kiro-cli-agent.mjs — add env:{CMK_PROJECT_DIR: projectRoot} to the MCP entry, AND write the agent PROJECT-LOCAL (<project>/.kiro/agents/cmk.json) so the absolute path matches the install; OR keep global agent + a separate per-project mcp env. Verify kiro-cli workspace-agent precedence (research said <project>/.kiro/agents/ resolves over ~/.kiro/agents/). (3) consider a walk-up-to-context/ fallback in mcp serve for robustness. (4) the absolute path is machine-specific → re-resolved by `cmk install --ide kiro` per machine (like the lazy-compress path already is); document that a teammate re-runs install. Live-verify: state a pref in kiro-cli → the fact LANDS in <project>/context/. Then re-run the gate Session 1. Relates D-198 (agent-location sibling), the D-185..190 cross-agent class (CLAUDE_PROJECT_DIR is the Claude-only mechanism here).
