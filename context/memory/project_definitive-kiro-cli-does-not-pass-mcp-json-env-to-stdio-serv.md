---
id: P-Y3LNUP3A
type: project
title: DEFINITIVE-kiro-cli-does-not-pass-mcp-json-env-to-stdio-server
created_at: 2026-06-24T15:10:16Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 40e3550ac230cc4f8a3ec45cb961b15440a0008fdf2939817d8e859c59aea6d9
related: [mk-remember-fix-self-review-agent-config-inline-mcp-composition-risk, CUT-BLOCKER-mcp-mk-remember-silent-dataloss-kiro-cli-claude-project-dir-only]
---

DEFINITIVE (2026-06-24, clean gate2 test): kiro-cli does NOT deliver the mcp.json `env` field to the spawned stdio MCP server. PROVEN exhaustively: (1) fresh project gate2, correct install (global has resolveMcpProjectRoot + includeMcpJson + CMK_PROJECT_DIR; live agent has includeMcpJson:true + no mcpServers; mcp.json has env.CMK_PROJECT_DIR='C:\Temp\kiro-cli-gate2' in the EXACT format kiro-cli's own `mcp add --env` produces). (2) Killed all stale MCP servers, fresh PID 18044 spawned. (3) Stated 2 prefs in kiro-cli (.venv, uv) — agent called mk_remember, said "saved to project memory". (4) EXHAUSTIVE disk check: NEITHER fact landed — gate2 MEMORY.md has only seed examples + my P-MGVG2NWH diagnostic; gate2 audit.log has ONLY P-MGVG2NWH (my direct test, zero from kiro sessions); NO stray context/ anywhere (Users/Temp/C:/Windows\Temp); the dev-repo now.md hits were OLD June-11 content (red herring). The writes ERRORED SILENTLY — the MCP server resolved a projectRoot with no valid context/, write failed, but the agent reported "saved" anyway. CONCLUSIVELY kiro-cli's fault: my diagnostic P-MGVG2NWH PROVED the code works when CMK_PROJECT_DIR reaches the process (cmd.exe /C cmk mcp serve from a NEUTRAL cwd with the env set → wrote to gate2 correctly, accepted:true). So the code fix is CORRECT but kiro-cli never delivers the env lever. THE BUG IS kiro-cli not passing mcp.json `env` to the stdio process. CONSEQUENCE: mk_remember silently fails in kiro-cli — the agent says "saved" but nothing persists (the worst UX: false success + data loss). NEXT OPTIONS: (a) does kiro-cli launch the MCP from the PROJECT cwd? if it does, the walk-up fallback should work — but gate2 facts vanished with walk-up active, so kiro's MCP cwd has NO context/ ancestor → walk-up can't save it; (b) file upstream w/ Amazon Q (env not honored for stdio MCP); (c) check if kiro-cli V3 (--v3) or the V3 .kiro/hooks redesign delivers env; (d) accept that kiro-cli MCP-driven capture is broken until kiro fixes env-passing, and document it — the IDE works, Claude Code works, only the kiro-cli MCP-tool path is affected (the CLI hooks agentSpawn/stop still fire for inject/capture). This may BLOCK claiming "mk_remember works in kiro-cli" for v0.4.0 but does NOT block the IDE or the hook-based capture.

**Why:** Exhaustively proven: kiro-cli does not deliver the mcp.json `env` field to the spawned MCP server, so CMK_PROJECT_DIR never reaches it, so mk_remember silently fails (agent says "saved", nothing persists). The kit's code fix is correct (proven by a direct env-set test that DID land) but kiro doesn't provide the lever. A kiro-cli platform bug, not a kit bug — but it breaks MCP-driven capture in kiro-cli.

**How to apply:** DECIDE the v0.4.0 stance: the kit's fix (resolveMcpProjectRoot + env + includeMcpJson) is correct + merge-worthy (it makes mcp serve robust + works the moment kiro delivers env, AND helps Claude Code's CLAUDE_PROJECT_DIR path stay clean). Options for the kiro-cli gap: (1) VERIFY where kiro-cli launches the MCP cwd — if it's at/under the project, the walk-up fallback ALREADY handles it (but gate2 evidence says cwd has no context/ ancestor); instrument: make mcp serve log its resolved projectRoot + process.cwd() to a file, run a kiro session, read it — this tells us kiro's actual MCP cwd + whether env arrived. (2) If kiro truly doesn't pass env AND launches from a non-project cwd → file upstream + document kiro-cli MCP-capture as a known gap for v0.4.0 (the IDE + Claude Code + the kiro-cli AGENT HOOKS agentSpawn/stop still work — only the MCP-TOOL mk_remember path is affected). (3) Try kiro-cli --v3. Do NOT claim mk_remember works in kiro-cli until a fact LANDS in a real session. The PR (fix-kiro-cli-mcp-project-resolution) is still worth merging as the correct-and-necessary half; the kiro env-delivery is the blocker beyond our code.
