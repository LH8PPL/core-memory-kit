---
id: P-6YCQPD3K
type: project
title: THE-ACTUAL-BUG-missing-tools-field-agent-had-no-shell-tool-enabled
created_at: 2026-06-24T18:59:10Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: f2bf7674ad4f2f3b2e4fb01d642150fdea347a3ced1a095a2a52dc13d74795e7
related: [TRULY-FINAL-kiro-windows-allowlist-bug-5376-7431-blocks-correct-commands-unfixable, design-correction-project-flag-is-outside-chat-not-model-in]
---

★★★★★ THE ACTUAL BUG (the user's reframe 'cmk remember is just a tool like ls — how do you make it run in chat?' cracked it, 2026-06-24): OUR KIRO-CLI AGENT WAS MISSING THE `tools` FIELD. kiro.dev/docs/cli/custom-agents/configuration-reference + troubleshooting: the `tools` field is the agent's CAPABILITY SET — 'lists all tools the agent can potentially use' (built-in + MCP). To run shell commands the agent MUST include 'shell' in tools (or '*'). Troubleshooting: 'Ensure tools are listed in BOTH tools and allowedTools arrays.' OUR AGENT HAD NO `tools` FIELD AT ALL → the custom agent had ZERO tools → it literally COULD NOT run shell commands → the model 'called' cmk remember but nothing executed (every '✅ Remembered' was the model PRETENDING — it had no shell tool). Kiro's own example agent lists: read, write, shell, aws, report, introspect, knowledge, thinking, todo, delegate, grep, glob, @mcp_server/tool. THIS WAS NEVER A KIRO BUG — we just never enabled the tools. ALL my earlier 'confirmed kiro bugs' conclusions (#5873 no MCP for custom agents, #5376/#7431 allowlist, #4579 cd) were WRONG diagnoses — the real cause was the missing tools field the whole time. THE FIX: add `tools: ['*']` to the kiro-cli agent (buildAgentConfig) — '*' = all built-in tools (incl shell → cmk remember/search RUN) + MCP tools. This LIKELY ALSO FIXES THE MCP ROUTE (#5873 may have been the missing tools field too — the MCP tools weren't in the capability set). RE-TEST EVERYTHING with tools:['*']: (1) does cmk remember run + land? (2) do the MCP tools (mk_remember) work now? if MCP works with tools:['*'], we can REVERT includeMcpJson:false (but that also fixed the popup — weigh it). The user was right at every step: 'this can't be this hard', 'we're thinking backwards', 'cmk remember is just a tool like ls/jq/grep'. LESSON: I went down a rabbit hole of 'confirmed kiro bugs' instead of checking the most basic thing — does the agent even HAVE tools enabled. Read the config-reference 'tools' field FIRST next time. NEXT: live-test tools:['*'] — expect cmk remember to FINALLY land in chat.

**Why:** The user's reframe (cmk remember is just a tool like ls — how do you run tools in chat?) led to the real bug: our kiro-cli agent had NO `tools` field, so the custom agent had zero tools enabled and literally couldn't run shell commands — every '✅ Remembered' was the model pretending. Kiro's docs: tools is the capability set; must include 'shell' or '*'. This was never a kiro bug; we forgot to enable tools. All my 'confirmed kiro bugs' diagnoses were wrong.

**How to apply:** FIX: tools:['*'] added to buildAgentConfig (kiro-cli-agent.mjs). RE-TEST live with tools:['*']: (1) cmk remember in chat → expect it to RUN + land (the real fix). (2) Re-check MCP: with tools:['*'] the MCP tools are now in the capability set — does mk_remember work? If yes, #5873 was also the missing tools field; could revert includeMcpJson:false EXCEPT that also fixed the popup — so weigh: keep includeMcpJson:false (no popup) + rely on CLI commands which now work via tools:['*'], OR re-enable MCP if it works + accept the popup. LIKELY KEEP includeMcpJson:false (popup-free) + the now-working CLI shell commands (cmk remember/search) since the agent can run them with tools:['*']. Update tests: agent has tools:['*']. The --project flag + normalizeProjectPath STAY (correct for the command). The prompt telling the model to run cmk remember is now CORRECT (it can actually run them). Test-first, live-verify cmk remember LANDS in a real kiro-cli chat. Then consolidate + PR + ship. Revisit ALL the 'kiro bug' memory notes — they were misdiagnoses.
