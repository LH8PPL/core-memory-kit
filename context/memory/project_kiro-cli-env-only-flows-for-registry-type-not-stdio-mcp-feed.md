---
id: P-RP9NNKAY
type: project
title: kiro-cli-env-only-flows-for-registry-type-not-stdio-mcp-feed-json-proof
created_at: 2026-06-24T15:13:24Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: d7ba79657d162821b1f87b4f60b38f29b8a5c309611c9dbafc504860cc343e91
related: [DEFINITIVE-kiro-cli-does-not-pass-mcp-json-env-to-stdio-server, mk-remember-fix-self-review-agent-config-inline-mcp-composition-risk]
---

ROOT CAUSE FOUND in kiro-cli's OWN changelog (2026-06-24, feed.json at AppData\Local\kiro-cli\feed.json): kiro-cli only passes mcp.json `env` to the launched server for REGISTRY-TYPE entries, NOT personal/stdio ones. Exact quote: "`mcp.json` env, header, and timeout overrides for REGISTRY-TYPE MCP server entries now flow to the launched server" + a separate entry "PERSONAL MCP servers defined in mcp.json now load alongside registry-managed servers when the registry is active". Our entry is type:"stdio" = a PERSONAL server → kiro does NOT flow its env → CMK_PROJECT_DIR silently dropped → mk_remember writes to the wrong/no project. THIS EXPLAINS EVERYTHING (the silent-data-loss): the env is in mcp.json (correct format, matches kiro's own `mcp add --env` output) but kiro only honors it for registry-type servers. ALSO found in feed.json: "duplicate steering/skills deduped when working directory equals home" — implies kiro CAN launch the MCP/agent with cwd=HOME (not the project), which is why process.cwd() fallback fails too. NEXT (investigate, don't guess): (1) what IS a 'registry-type' MCP entry vs personal/stdio? kiro.dev/docs/cli/mcp/registry/ — does it support env that flows? if so, can the kit register as registry-type? (2) kiro-cli version — feed.json is the changelog; check if env-for-stdio is coming. (3) the walk-up fallback is the kit's only env-independent lever, BUT it fails if kiro launches MCP from HOME (no context/ ancestor) — the feed's 'working directory equals home' note suggests that's exactly what happens. SO: the kit's code fix (resolveMcpProjectRoot + env + includeMcpJson) is correct and merge-worthy but kiro-cli's stdio-env limitation means CMK_PROJECT_DIR won't flow until either (a) we register as registry-type (if that path exists + flows env), or (b) kiro extends env to stdio servers. Document kiro-cli MCP-driven mk_remember as a known gap for v0.4.0 unless the registry path works. The CLI AGENT HOOKS (agentSpawn/stop) still fire — only the MCP-TOOL capture path is blocked.

**Why:** kiro-cli's own changelog (feed.json) proves it: mcp.json `env` only flows to REGISTRY-TYPE MCP servers, not personal/stdio ones like ours. So CMK_PROJECT_DIR is silently dropped → mk_remember silent-data-loss. Primary-source confirmation of the kiro-cli limitation behind the cut-blocker; the kit's code fix is correct but kiro won't deliver the env to a stdio server.

**How to apply:** INVESTIGATE the registry-type path (kiro.dev/docs/cli/mcp/registry/): what makes an entry registry-type vs personal/stdio, and does registry-type env flow to the server? If the kit can register as registry-type WITH env that flows → that's the fix (change the mcp.json entry shape). If not → the kit's env lever is dead in kiro-cli; rely on walk-up (but feed.json's 'working directory equals home' note implies kiro launches from HOME, so walk-up finds no context/ → also fails). Fallbacks: (a) file upstream (env should flow for stdio too); (b) document kiro-cli MCP-driven mk_remember as a known v0.4.0 gap — the IDE + Claude Code + the kiro-cli agent HOOKS (agentSpawn/stop inject/capture) all work; only the MCP-TOOL capture path is affected. The PR (fix-kiro-cli-mcp-project-resolution) is still correct + merge-worthy (it's the right code + helps Claude Code + works the moment kiro flows env). Do NOT claim mk_remember works in kiro-cli until a fact LANDS live.
