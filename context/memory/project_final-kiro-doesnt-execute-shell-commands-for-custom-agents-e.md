---
id: P-C3MJVGP5
type: project
title: FINAL-kiro-doesnt-execute-shell-commands-for-custom-agents-explicit-memory-dead
created_at: 2026-06-24T18:18:45Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 0133f1efc0df37d286e39b84d7bebf371f0afd048f0e477da81e153a0399d1ef
related: [CONFIRMED-kiro-bugs-5873-5662-custom-agents-dont-get-mcp-too, v0.4.0-decision-remove-mcp-from-kiro-cli-agent-includemcpjson-false-document-gap]
---

DEFINITIVE FINAL ROOT CAUSE (2026-06-24, the user pushed me to read the code + I traced it): the kiro-cli explicit-save failure is NOT a cwd/path bug in the kit — it's that KIRO DOESN'T ACTUALLY EXECUTE the shell command for a CUSTOM agent (the same class as MCP #5873). PROOF: (1) Read subcommands.mjs:983 — `cmk remember` resolves projectRoot from process.cwd() only (correct mechanism, no --project/walk-up). (2) I ran the agent's EXACT command `cd /c/Temp/kiro-cli-gate5 && cmk remember "..."` MYSELF in git-bash → it LANDED (P-96a2ESaa, gate5 audit 0→1). So the command + path + cd all WORK. (3) BUT the gate5 SESSION's identical command produced ZERO audit entries AND no now.md — nothing ran. (4) The tool name in the transcript was 'terminal' (this session) vs 'execute_command' (earlier) — the model is GUESSING/HALLUCINATING the tool name + the '✅ Remembered' result. CONCLUSION: kiro does not wire a real shell-execution tool to a custom agent's LLM either (parallel to #5873 for MCP tools) — so the model FAKES the tool_call and FABRICATES the success result; nothing actually executes. This means BOTH explicit paths are dead in kiro-cli for a custom agent: MCP tools (#5873) AND shell commands (the model can't really run them). NO kit fix helps (--project, path normalization, etc. are moot if the command never runs). What DOES work: the HOOKS (agentSpawn/stop) — because kiro itself runs those as runCommand hooks (not via the model's tool-calling), feeding cwd via stdin. So AUTOMATIC memory works; ALL explicit (model-initiated) memory is blocked by kiro's custom-agent tool-wiring bugs. FINAL v0.4.0 STANCE: kiro-cli = automatic memory via hooks ONLY (works); explicit 'remember this' / 'search memory' by the model is BLOCKED BY KIRO (document as known limitation, link #5873 + the broader custom-agent-tool-wiring issue). Popup is FIXED (includeMcpJson:false). REVERT the --project + normalizeProjectPath changes to cmk remember/search (moot — the command never runs from the model). KEEP the mcp serve --project + resolveMcpProjectRoot (correct for Claude Code). Lean the kiro-cli prompt on hooks + note explicit-save is unavailable. STOP chasing — it's confirmed unfixable from the kit.

**Why:** The user's 'did you read the code?' push led to the definitive root cause: reading subcommands.mjs:983 proved cmk remember is cwd-only (correct), and running the agent's EXACT command manually LANDED — but the session's identical command did NOTHING (0 audit, no now.md) and the model used a hallucinated tool name + fabricated '✅ Remembered'. So kiro doesn't execute shell commands for custom agents either (parallel to MCP #5873) — the model fakes it. ALL model-initiated explicit memory is dead in kiro-cli; only hooks (kiro-run, not model-run) work. No kit fix helps.

**How to apply:** FINAL: (1) REVERT the cmk remember/search --project + normalizeProjectPath additions (moot — the command never executes from the model in kiro-cli; keep them ONLY if useful for Claude Code/manual CLI, else drop to keep the diff clean). KEEP mcp serve --project + resolveMcpProjectRoot (correct for Claude Code). (2) kiro-cli-agent.mjs prompt: lean on AUTOMATIC hooks ('memory is captured automatically each turn; recall is injected at session start') — do NOT instruct the model to run cmk remember/search (it can't really run them; it'll hallucinate success). Keep includeMcpJson:false (popup fix). (3) DOCUMENT in CHANGELOG + cut-gate-kiro-cli + README: kiro-cli = automatic memory via hooks (works); explicit model-initiated remember/search is a known kiro-cli limitation (kiro doesn't wire shell/MCP tools to custom agents — #5873 + related). (4) tasks.md follow-up for when kiro fixes custom-agent tool wiring. (5) consolidate the branch + open ONE PR (D-198 location + popup fix + the mcp --project for Claude Code + docs). Two-pass review. Ship v0.4.0: Claude Code + Kiro IDE fully work; kiro-cli has automatic memory + documented explicit-save gap. STOP — confirmed unfixable from the kit.
