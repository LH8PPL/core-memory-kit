---
id: P-TQSG9PCA
type: project
shape: State
title: 'CONFIRMED Kiro bug: user turn never captured → auto-extract can''t catch user-stated preferences (only assistant)'
created_at: 2026-07-09T06:50:25Z
write_source: user-explicit
trust: high
recurrence_count: 1
source_file: user-explicit
source_line: 1
source_sha1: cc7a6832e1323e70ea12dd8e466365f992d476298847b00a4b73444879fc69d3
related: [unverified-auto-extract-has-never-automatically-written-a-fa, kiro-ide-transcript-parser-works-both-turns-extracted-from-r]
---

CONFIRMED KIRO BUG (v0.5.0): auto-extract cannot capture USER-STATED preferences on Kiro IDE because the user turn is never captured — only the assistant turn. On Kiro IDE, the UserPromptSubmit hook is wired to INJECT (recall) only; there is NO capture-prompt hook firing, so the user prompt never reaches now.md / the auto-extract turn-file. Result: auto-extract reads assistant-only turns → nothing_durable every run → 0 write_source:auto-extract facts ever. Explicit mk_remember, recall, inject, wedge all WORK; the automatic 'captures what the user says' path is BROKEN on Kiro.

**Why:** Definitive clean test (2026-07-09, C:\Temp\kiro-autoextract-test, uncontaminated, user drove alone): user stated a strong extractable preference ('I always use httpx never requests, my default across all projects'). Evidence chain: (1) now.md has 0 '— user' headings, 1 '— assistant'; the httpx/preference TEXT appears 0 times in now.md — the user turn never reached what auto-extract reads; (2) live transcript has 0 user headings; (3) extract.log: 3 automatic runs (success:true, ~8s real kiro-cli LLM calls), ALL nothing_durable/obs:0/rich:0; (4) 0 write_source:auto-extract facts (the only fact is user-explicit from the model's own mk_remember call). THE LOGS pin the root cause: recall.log shows source:inject fired (UserPromptSubmit→inject works); redactions.log's ONLY source is transcript-promote — there is NO capture-prompt and NO capture-turn redaction source, meaning capture-prompt (the user-turn write) NEVER FIRED. In kiro-ide-hooks.mjs: the IDE-1.0 hooks map UserPromptSubmit→INJECT (line 125), Stop→capture-turn (line 116, assistant only), and capture-prompt is only on the LEGACY promptSubmit .kiro.hook (line 76) which IDE 1.0 doesn't fire. So on Kiro IDE, the user prompt triggers inject but NOT capture-prompt → the user turn is dropped → capture-turn's readKiroTurn gets assistant-only (or the fallback hits the user-turn-less kiro-cli reader) → now.md user-turn-less → auto-extract's BI-TURN extraction (which needs USER_TURN to identify user-stated preferences) starves → nothing_durable. This BREAKS the kit's headline 'captures automatically, prompt-free' for user preferences on Kiro. NOT contamination (clean session), NOT dedup (the preference text isn't even in now.md to dedup against), NOT the parser (parseKiroIdeV1Messages works on real data, P-MRXZDQa4). This SUPERSEDES the false P-TBGK35LC/P-GETTJWDJ 'Kiro complete' claims re: auto-extract.

**How to apply:** FIX: Kiro IDE needs a capture-prompt hook that writes the USER turn, like Claude's UserPromptSubmit→capture-prompt. Currently UserPromptSubmit→inject only. Options: (a) the UserPromptSubmit hook should do BOTH inject AND capture-prompt (the user prompt is in the hook payload/env at that moment — capture it there, don't rely on reading it back from the transcript later); (b) OR ensure capture-turn's readKiroTurn reliably returns userText on the IDE path (it CAN parse it — parseKiroIdeV1Messages works — so the issue is the fallback chain hitting the user-turn-less kiro-cli reader first, OR a timing issue where the user turn isn't yet in the session file when Stop fires). Path (a) is more robust: capture the user prompt AT UserPromptSubmit time (when it's in-hand) rather than reconstructing it. Verify the fix by re-running THIS exact clean test: state a casual preference, confirm a write_source:auto-extract fact about it appears with no mk_remember. TAG IMPACT (user's call): this is a real gap in the Kiro compatibility claim (the automatic capture headline) — either fix before the v0.5.0 tag, OR document Kiro auto-extract-of-user-preferences as a known limitation (explicit mk_remember + recall + wedge all work), OR lane a fix to v0.5.1. Given D-292 (compatibility claim = truth), the user should decide fix-before-tag vs document-limitation. Relates P-TXWT72PM (the unproven claim now PROVEN broken), P-MRXZDQa4 (parser ruled out), P-FSaL7MCB (engine works in isolation), kiro-ide-hooks.mjs (the hook mapping to fix), the D-169 automatic-path discipline.
