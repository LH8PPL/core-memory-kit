---
id: P-5SDX2A4Y
type: project
title: kiro-cli-mcp-list-reveals-agent-server-link-broken-includemcpjson-shows-legacy
created_at: 2026-06-24T15:47:43Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: a79bd7b745eee5ef814defb8300ed825dffb20e003585a1181c2b0f40a451b99
related: [DEFINITIVE-kiro-cli-does-not-pass-mcp-json-env-to-stdio-server, mk-remember-fix-self-review-agent-config-inline-mcp-composition-risk]
---

TWO kiro-cli issues clarified (2026-06-24, gate4): (A) THE POPUP the user kept asking about = the cmd.exe /C cmk mcp serve window kiro opens for our MCP server EVERY kiro-cli launch — already diagnosed as kiro-cli's own behavior (wraps every stdio MCP in cmd.exe on Windows), cosmetic, NOT fixable from the kit (P-V2CQQLQQ). It is NOT the data-loss bug. (B) THE REAL data-loss bug, revealed by `kiro-cli mcp list`: the active agent is 'cmk' (bottom prompt) but mcp list shows: "default: kiro_default → claude-memory-kit cmk" AND "global: cmk → claude-memory-kit cmk [legacy]". So the cmk AGENT's server shows as [legacy], and the non-legacy attachment is on kiro_default, NOT cmk. This means includeMcpJson:true (my Option B change removing the agent's inline mcpServers + relying on the project .kiro/settings/mcp.json) does NOT properly expose the project mcp.json server to the cmk agent — it shows [legacy], suggesting the server isn't connected through the path mk_remember calls use. So mk_remember tool calls go nowhere (no audit entry) even though: the server process has --project gate4 (correct), my DIRECT JSON-RPC call to that exact server WRITES correctly (P-WDCD2aQU landed in gate4). The break is the AGENT→SERVER link in kiro-cli, possibly caused by removing the agent's inline mcpServers. HYPOTHESIS: Option B was WRONG — the agent NEEDS its own inline mcpServers entry (named 'claude-memory-kit', WITH the --project args + env) for the tool calls to route correctly; includeMcpJson alone marks it [legacy]/doesn't wire it. NEXT: RESTORE the agent's inline mcpServers (named claude-memory-kit, args:['mcp','serve','--project',<proj>]) — but a GLOBAL agent can't bake a per-project path... UNLESS we write a PROJECT-LOCAL agent (<project>/.kiro/agents/cmk.json) which CAN bake the path AND carry inline mcpServers. The project-local agent is likely the real answer (ties back to the D-198 project-local idea). Verify: does kiro-cli prefer a project-local agent + does its inline mcpServers route tool calls correctly (non-legacy)?

**Why:** kiro-cli mcp list shows the cmk agent's server as [legacy] and the real attachment on kiro_default not cmk — so includeMcpJson:true (Option B) doesn't properly wire the project mcp.json server to the cmk agent, and mk_remember calls go nowhere. The server itself is correct (direct calls write); the agent→server link is broken. Likely Option B (dropping the agent's inline mcpServers) was wrong; a project-local agent with inline mcpServers may be the real fix.

**How to apply:** INVESTIGATE the agent→server routing: (1) restore the agent's INLINE mcpServers (named 'claude-memory-kit' to match allowedTools @claude-memory-kit, with args ['mcp','serve','--project',<proj>] + env) — but the GLOBAL agent can't bake a per-project --project path, so (2) write a PROJECT-LOCAL agent <project>/.kiro/agents/cmk.json (kiro-cli resolves workspace agents over global) that bakes the project path into inline mcpServers args. This combines D-198's project-local-agent idea with the --project arg. (3) Verify kiro-cli mcp list then shows the server NON-legacy + attached to the cmk agent. (4) live-test mk_remember lands. The [legacy] tag is the clue — find what makes a server non-legacy (likely inline mcpServers in the active agent, not includeMcpJson). For the POPUP (separate, cosmetic): document as known kiro-cli behavior, don't chase. Relates P-V2CQQLQQ (popup), the Option-B change, D-198 (project-local agent).
