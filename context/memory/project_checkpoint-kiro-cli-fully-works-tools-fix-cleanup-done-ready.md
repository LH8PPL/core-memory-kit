---
id: P-SSULEHW2
type: project
title: CHECKPOINT-kiro-cli-fully-works-tools-fix-cleanup-done-ready-for-pr
created_at: 2026-06-24T19:32:06Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: fce8abb70acd30368d59499bede0daaaa9d8d31825aeddd2ab0a48ec371f8739
related: [CUT-BLOCKER-CLOSED-kiro-cli-explicit-memory-works-tools-star-fix-proven, THE-ACTUAL-BUG-missing-tools-field-agent-had-no-shell-tool-enabled]
---

CHECKPOINT (2026-06-24, context at 2%) — kiro-cli FULLY WORKS + cleanup done, committed a60b11a on branch fix-kiro-cli-mcp-project-resolution (pushed, NOT yet PR'd/merged). THE FIX: tools:['*'] (the agent had NO tools field → couldn't run shell commands → cmk remember faked success; one line fixed it, PROVEN live gate7 P-DTRaa79G). kiro-cli config now: tools:['*'], includeMcpJson:false (no MCP=no popup), prompt tells agent to use `cmk remember/search --project "<abs>"` with NO cd (cd prefix breaks the ^cmk remember allowlist → approval prompt; --project keeps it matching + prompt-free), trusted cmds ^cmk remember/search. CLEANUP (user: 'no useless code', done systematically): CUT the dead MCP project machinery — IDE mcp.json back to plain `cmk mcp serve` (no --project/env: IDE launches from workspace, kiro-cli doesn't use MCP); removed mcp-serve --project + readFlag; resolveMcpProjectRoot simplified to CLAUDE_PROJECT_DIR→walk-up→cwd. KEPT: cmk remember/search --project + normalizeProjectPath (kiro-cli explicit path), tools:['*'], includeMcpJson:false, D-198 location. Suite 2265/0, real kiro-cli validate passes. STATUS — ALL AGENTS WORK: Claude Code (MCP) ✅, Kiro IDE (MCP, Session1 proven) ✅, kiro-cli (hooks auto + cmk remember/search explicit + no popup) ✅. REMAINING TO SHIP v0.4.0: (1) one open question the user raised, untested: 'if you tell the model to run bare cmk remember WITHOUT cd AND without --project, does it land?' — if kiro runs from project cwd it might, letting us drop --project too (try in next session). (2) two-pass review (self + code-review-excellence) on the full branch. (3) DOCS: CHANGELOG + cut-gate-kiro-cli + README (kiro-cli fully works now). (4) CORRECT the wrong 'kiro bug' memory notes (#5873/#5376/#7431/#4579 were misdiagnoses — real cause = missing tools field). (5) CLAUDE.md binding lesson: 'agent calls a tool but nothing runs → check the agent tools capability field FIRST'. (6) consolidate branch → ONE PR → merge → v0.4.0 tag. The branch has ~10 commits (D-198 location already merged in #225; this branch adds tools fix + popup + --project + cleanup + the cut-gate-kiro-cli doc).

**Why:** Context at 2% — durable checkpoint. kiro-cli now fully works (tools:['*'] was the fix, proven live; popup fixed; explicit memory via cmk remember/search works) and the dead MCP machinery is cleaned up (committed a60b11a). All 3 agents work. Remaining: test the bare-no-project question, two-pass review, docs, correct the misdiagnosis notes, consolidate → PR → v0.4.0 tag.

**How to apply:** NEXT SESSION ('continue the kiro-cli ship'): (1) OPTIONAL test the user's question — set the prompt to bare `cmk remember` (no --project) + see if it lands from kiro's cwd; if yes, drop --project for max simplicity. (2) Two-pass review the branch (self + code-review-excellence): focus the tools:['*'] security (is all-tools too broad? real agents use it, but consider a scoped list ['read','write','shell',...]+mcp), the cleanup correctness, five doors. (3) Docs: CHANGELOG [0.4.0] (kiro-cli fully works), cut-gate-kiro-cli.md (update the explicit-save section — it WORKS now via cmk remember, remove the 'limitation' framing), README Kiro section. (4) Correct the wrong memory notes (the confirmed-kiro-bug ones were misdiagnoses; mk_forget or annotate). (5) Add the CLAUDE.md lesson (check agent tools field first). (6) git checkout fix-kiro-cli-mcp-project-resolution; consolidate → gh pr create → two-pass → merge → then E1/uninstall gate items → user pushes v0.4.0 tag. Branch state: a60b11a pushed, suite 2265/0, NOT PR'd yet. Dogfood context/ memory has many uncommitted session facts — commit to main separately (never auto-commit memory).
