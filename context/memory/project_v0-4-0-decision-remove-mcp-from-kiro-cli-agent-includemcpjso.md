---
id: P-DRML7RX3
type: project
title: v0.4.0-decision-remove-mcp-from-kiro-cli-agent-includemcpjson-false-document-gap
created_at: 2026-06-24T16:49:43Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: a09aad2db605d6992f21d51772f952fa4723c748dcf8c87ef97e526640aedcef
related: [THE-FIX-use-cmk-remember-cli-not-mk-remember-mcp-in-kiro-cli, CONFIRMED-kiro-bugs-5873-5662-custom-agents-dont-get-mcp-too]
---

DECISION (2026-06-24, user chose): for v0.4.0, remove the MCP server from the KIRO-CLI agent (kills the popup + the dead MCP route) + document the explicit-save gap, ship. KEY FACTS established: (1) HOOKS WORK in kiro-cli — gate4 now.md = 3641 bytes; hooks get cwd from kiro's STDIN payload (kiro provides it correctly), so AUTOMATIC memory (capture every turn + inject at start) functions. (2) BOTH explicit-save routes fail in kiro-cli: MCP tools (#5873, kiro doesn't wire them to custom agents) AND the CLI cmk remember (the agent ran `cd /c/Temp/gate5 && cmk remember` → saw '✅ Remembered' but the fact landed NOWHERE — gate5 has 0 audit entries; kiro's shell/cwd for execute_command is unreliable, the /c/Temp bash-path cd + cmk-shim cwd resolution failed). So explicit save is genuinely broken in kiro-cli V3/2.9.0 by multiple kiro bugs. (3) THE POPUP = cmd.exe /C cmk mcp serve, kiro's MCP launcher, every session. SCOPING SUBTLETY (critical): the .kiro/settings/mcp.json MCP server is SHARED between Kiro IDE (MCP WORKS — Session 1 proven) and kiro-cli (broken+popup). CANNOT remove it globally (breaks IDE MCP). THE LEVER: set includeMcpJson:FALSE on the kiro-cli AGENT (~/.kiro/agents/cmk.json) — then the kiro-cli agent won't load/spawn the MCP server (no popup, no dead route in cli) while the IDE keeps reading .kiro/settings/mcp.json directly (unaffected). PLAN: (a) kiro-cli-agent.mjs: includeMcpJson:false + remove allowedTools @claude-memory-kit (no MCP tools to approve) — keep the CLI-route prompt (best-effort recall via cmk search which the agent CAN run, even if save is flaky) + the hooks (which WORK). Actually reconsider: if cmk remember cwd is also flaky, the prompt should lean on HOOKS for capture (automatic, working) and note explicit-save is unreliable. (b) Document kiro-cli: automatic memory (hooks) works; explicit 'remember this' is a known kiro-cli V3 limitation (link #5873 + the cwd issues) — CHANGELOG + cut-gate-kiro-cli + README. (c) IDE + Claude Code unaffected/fully working. VERIFY includeMcpJson:false actually stops the kiro-cli MCP spawn (no popup) live.

**Why:** User chose: remove MCP from kiro-cli + document, ship v0.4.0. Hooks work in kiro-cli (automatic memory functions); both explicit-save routes (MCP #5873, CLI cmk remember cwd-flaky) are broken by kiro V3 bugs. The popup is kiro's MCP launcher. The fix: includeMcpJson:false on the kiro-cli agent stops the MCP spawn (no popup) without touching the IDE (which reads mcp.json directly + where MCP works).

**How to apply:** BUILD: (1) kiro-cli-agent.mjs: includeMcpJson:false (stops the kiro-cli MCP spawn → no popup); remove allowedTools (no MCP tools to pre-approve in cli); keep the trusted cmk remember/search commands + hooks. Reconsider the prompt: lean on HOOKS for automatic capture (working) + cmk search for recall; note explicit-save is best-effort. (2) Keep the IDE MCP untouched (.kiro/settings/mcp.json still written for the IDE; the IDE doesn't read the cli agent). (3) Docs: CHANGELOG + cut-gate-kiro-cli + README — 'kiro-cli: automatic memory via hooks works; explicit remember-this is a known kiro-cli V3 limitation (#5873 + cwd bugs)'. (4) tasks.md follow-up (Task 166-adjacent) for when kiro V3 stabilizes. LIVE-VERIFY: includeMcpJson:false → restart kiro-cli → NO cmd.exe popup + hooks still capture (now.md grows). Test-first (agent has includeMcpJson:false, no allowedTools). KEEP the --project + resolveMcpProjectRoot code (correct for Claude Code + future kiro). The PR consolidates all of it. Relates #5873, the CLI-route attempt, the popup (P-V2CQQLQQ).
