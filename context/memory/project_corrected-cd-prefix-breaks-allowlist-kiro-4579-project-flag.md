---
id: P-Z94F92ZE
type: project
title: CORRECTED-cd-prefix-breaks-allowlist-kiro-4579-project-flag-is-the-fix
created_at: 2026-06-24T18:36:42Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: ac30f9ab64267959c7b8fd865b90f8b2a38c412f76241b0a799d6795691bcb14
related: [FINAL-kiro-doesnt-execute-shell-commands-for-custom-agents-explicit-memory-dead, cmk-remember-needs-project-flag-cwd-unreliable-in-kiro-cli-s]
---

CORRECTED root cause (2026-06-24, the user's 'what do other projects do for in-chat scripts' question) — I was WRONG that custom agents can't run commands. The REAL cause is CONFIRMED Kiro bug #4579: 'cd breaks allowedCommands for shell tool' — 'the agent will often preface commands with cd to ensure correct directory, but when they do, the command doesn't match the allowlist.' THIS IS EXACTLY OUR CASE: the kiro-cli agent ran `cd /c/Temp/kiro-cli-gate5 && cmk remember ...`; our trusted allowlist is `^cmk remember .*` (start-anchored), but the actual command STARTS WITH `cd ...` → does NOT match → Kiro BLOCKS it (the agent then can't run it, may fake success). So commands DO work for custom agents — but a `cd`-prefixed command fails the start-anchored allowlist. Related confirmed bugs: #5376 (execute_bash rejects ALL commands via bad regex despite allowedCommands), #7431 (shell tool fails on Windows native kiro-cli 2.0), #9501 (1.0 shell output only in chat, no interactive terminal). SO THE FIX IS POSSIBLE: (1) tell the agent NOT to prepend cd — use `cmk remember "<fact>" --project "<abs path>"` as the WHOLE command (still matches ^cmk remember .*, no cd) — this is why the --project flag I was building IS the right move; OR (2) broaden the allowlist to tolerate a `cd ... &&` prefix (e.g. add `.*cmk remember` unanchored — but that loosens security, the M2 anchor exists for a reason); option 1 is cleaner. ALSO: the allowlist itself may be hit by #5376/#7431 (Windows regex bugs) independent of cd — VERIFY a bare `cmk remember "x"` (no cd) actually runs in a kiro-cli custom-agent session. REVISIT the whole conclusion: explicit save may be FIXABLE via --project (no cd) + the prompt telling the agent never to cd. Test: build cmk remember/search --project + prompt 'NEVER prefix with cd; always pass --project <abs windows path>'. Live-verify a fact LANDS. The hooks work regardless (kiro runs them, not the model, no allowlist gate). Popup fixed (includeMcpJson:false). KEEP building the --project flag — it's the right fix, not moot.

**Why:** The user's question (what do projects do for in-chat scripts) led to confirmed Kiro bug #4579: cd-prefixed commands break the start-anchored allowedCommands match. Our agent ran `cd ... && cmk remember`, which fails our `^cmk remember` allowlist → blocked. So commands DO work for custom agents; the cd prefix is the specific breakage — and it's FIXABLE: tell the agent to use `cmk remember --project <abs>` with NO cd. My earlier 'custom agents can't run commands / it's unfixable' conclusion was WRONG; the --project flag I was building is the correct fix.

**How to apply:** BUILD (the --project flag was right — finish it): (1) cmk remember + cmk search: add --project <dir> (normalizeProjectPath for /c/Temp→C:/Temp) → projectRoot = options.project ?? cwd. (2) kiro-cli-agent.mjs prompt: 'To save: run `cmk remember \"<fact>\" --project \"<absolute project path>\"` — NEVER prefix with cd (a cd prefix breaks kiro\\'s command allowlist, Kiro #4579). The project path is the workspace root.' Same for cmk search. (3) trusted allowlist `^cmk remember .*` already matches `cmk remember "x" --project ...` (no cd). (4) FIRST verify (live or via the agent) that a NON-cd `cmk remember` actually runs in a kiro-cli custom-agent session (rule out #5376/#7431 Windows allowlist bugs blocking even non-cd). If a bare cmk remember runs → the --project+no-cd fix works → explicit save WORKS in kiro-cli → cut-blocker truly closed. If even non-cd is blocked (#5376/#7431) → THEN it's a kiro Windows bug → document + rely on hooks. Re-enable includeMcpJson? NO — keep false (MCP tools still broken #5873 + popup); the CLI route via --project is the explicit path. Test-first + live-verify. Relates #4579 (the real cause), #5376/#7431 (verify), the --project work already started, the popup fix.
