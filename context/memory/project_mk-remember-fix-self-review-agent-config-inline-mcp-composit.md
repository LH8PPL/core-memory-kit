---
id: P-WQ4TBSYG
type: project
title: mk-remember-fix-self-review-agent-config-inline-mcp-composition-risk
created_at: 2026-06-24T14:17:14Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: 28dd38b40c440c7836d8db9cb39059ce9d5ac92a591283fccb4710c6917f1331
related: [mk-remember-kiro-cli-fix-cmk-project-dir-env-project-local-agent, CUT-BLOCKER-mcp-mk-remember-silent-dataloss-kiro-cli-claude-project-dir-only]
---

SELF-REVIEW finding on the mk_remember fix (2026-06-24, branch fix-kiro-cli-mcp-project-resolution): the fix added env.CMK_PROJECT_DIR to the PROJECT .kiro/settings/mcp.json (workspace-scoped, per-project — correct) + resolveMcpProjectRoot (CLAUDE_PROJECT_DIR → CMK_PROJECT_DIR → walk-up-to-context/ → cwd) + wired into mcp serve. PROVEN: mcp serve from a wrong cwd with CMK_PROJECT_DIR set lands in the right project (audit 4→5). UNRESOLVED COMPOSITION RISK (flag for live test): the kiro-cli AGENT-CONFIG (~/.kiro/agents/cmk.json) ALSO has its own inline mcpServers.cmk entry (DEFAULT_MCP_ENTRY) WITHOUT the env — and it's a GLOBAL agent so it CAN'T bake a per-project path. We set useLegacyMcpJson:false assuming it 're-reads the project mcp.json'. PRIMARY SOURCE (kiro.dev/docs/cli/custom-agents/configuration-reference) is AMBIGUOUS: it documents `includeMcpJson:true` as ADDITIVE (agent's mcpServers PLUS project mcp.json) but does NOT document useLegacyMcpJson OR which entry wins when both define 'cmk'. So WHICH mcp entry kiro-cli actually launches (the agent's env-less inline one, or the project's env-carrying one) is UNVERIFIED. THREE outcomes on live test: (a) kiro uses the PROJECT mcp.json (env present) → fix works, agent inline entry is redundant/harmless; (b) kiro uses the AGENT inline entry (no env) → the walk-up fallback must save it (works only if kiro's MCP cwd is at/under the project — earlier evidence says it was NOT, facts vanished) → fix may NOT work, need to remove the agent's inline mcpServers OR find another path-inject; (c) both merge. MUST LIVE-TEST: rebuild, reinstall in gate, state a pref in kiro-cli, verify the fact LANDS in C:\Temp\kiro-cli-gate\context. If it doesn't, the agent-config inline MCP entry is the culprit — consider DROPPING mcpServers from the agent config (rely on the project mcp.json via includeMcpJson:true) so only the env-carrying project entry is used. This is honest: the fix is PROVEN for the project-mcp.json path but the agent-config composition is the open variable.

**Why:** Self-review caught that the kiro-cli agent-config has its OWN env-less inline mcpServers.cmk entry (global, can't bake a path). The docs are ambiguous on whether kiro uses the agent's entry or the project mcp.json (which has the env). So the fix is proven for the project-mcp.json path but the agent-config composition is unverified — must live-test which entry kiro launches before claiming the cut-blocker is fully closed.

**How to apply:** LIVE TEST (decisive): rebuild global with this fix, cmk install --ide kiro in a fresh gate, state a preference in kiro-cli, check the fact lands in <gate>/context/MEMORY.md + audit.log. IF IT LANDS → fix works (kiro uses the env-carrying project mcp.json), done. IF NOT → kiro is using the agent-config's env-less inline mcpServers entry → FIX: either (a) add env to the agent-config entry too (but global = can't bake path → only works via a project-local agent, ties to the D-198 project-local-agent idea), or (b) DROP mcpServers from the agent config entirely + set includeMcpJson:true so kiro uses ONLY the project .kiro/settings/mcp.json (which has the env) — cleanest. Decide (b) is likely right: one MCP source (the project mcp.json), env-carrying, no global-agent path problem. Verify includeMcpJson:true makes the project mcp.json servers available to the agent. Relates the resolveMcpProjectRoot fix, D-198.
