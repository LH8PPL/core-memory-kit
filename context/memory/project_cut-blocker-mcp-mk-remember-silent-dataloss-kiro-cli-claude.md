---
id: P-GRTXENC5
type: project
title: CUT-BLOCKER-mcp-mk-remember-silent-dataloss-kiro-cli-claude-project-dir-only
created_at: 2026-06-24T11:27:42Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: d9fbfa610b59a37235dbf173c6f6cebd44665c5cd6d4667d26ddbd597834250c
related: [d198-shipped-pr225-kiro-cli-capture-inject-proven-live, FINAL-mcp-popup-is-kiro-cli-wraps-all-mcp-in-cmd-exe-not-kit-bug]
---

★★★ REAL SILENT-DATA-LOSS BUG found in the clean kiro-cli gate (2026-06-24): mk_remember via the MCP server in kiro-cli reports success ("Got it, noted") but the fact NEVER LANDS in the project. Live proof: Session 1 stated 4 preferences, the agent called mk_remember 4x (silently, autoApprove worked), agent confirmed each — but the gate's context\memory\ + MEMORY.md + audit.log show NONE of them (audit had only 1 leftover entry, zero from the 4 calls). Direct `cmk remember` from the gate DOES work (wrote P-JS4AaLMJ). So the bug is the MCP-SERVER path only. ROOT CAUSE (subcommands.mjs:1907, the `mcp serve` handler): `const projectRoot = resolvePath(process.env.CLAUDE_PROJECT_DIR ?? process.cwd())`. CLAUDE_PROJECT_DIR is a CLAUDE-CODE-ONLY env var — Kiro/kiro-cli does NOT set it. So in kiro-cli the MCP server falls back to process.cwd() = KIRO-CLI'S working directory, NOT the project. kiro-cli launches the MCP server via `cmd.exe /C cmk mcp serve` from its own cwd, so mk_remember writes to the wrong place (or a non-project dir → silently dropped/misfiled). THIS IS THE 6TH INSTANCE of the D-185/186/187/188/190 cross-agent class: a Claude-Code-only mechanism (CLAUDE_PROJECT_DIR) not carried over to Kiro — and the WORST kind (silent data loss: the tool says accepted, nothing persists). THE FIX: the kiro-cli MCP entry must pass the project root explicitly. Either (a) set an env in the agent-config mcpServers entry (e.g. `"env": {"CMK_PROJECT_DIR": "<projectRoot>"}` or kiro's cwd field) so the server knows the project, OR (b) mcp serve must read a Kiro-equivalent project-dir env, OR (c) since the kiro-cli agent is GLOBAL (~/.kiro/agents/cmk.json covers all projects), the project can't be baked in at install — so it MUST come from kiro-cli's per-session cwd or a kiro env var. INVESTIGATE: does kiro-cli set ANY project/workspace env for MCP servers (KIRO_PROJECT_DIR? a cwd)? The agentSpawn hook payload HAD cwd (we saw "cwd":"C:\\Temp\\kiro-gate2" in the probe) — so kiro KNOWS the cwd; the question is whether the MCP server process gets it. If kiro launches MCP from the project cwd, process.cwd() SHOULD work — but the cmd.exe /C wrapper may reset cwd. Check the actual cwd of the gate's cmk mcp serve node process. This is a v0.4.0 CUT-BLOCKER — memory silently doesn't save in kiro-cli.

**Why:** The clean gate surfaced a SILENT DATA-LOSS bug: mk_remember in kiro-cli says 'saved' but nothing persists to the project, because the MCP server resolves projectRoot from CLAUDE_PROJECT_DIR (Claude-Code-only) and falls back to process.cwd() (kiro-cli's cwd, not the project). The 6th instance of the Claude-tolerated/Kiro-breaks cross-agent class, and the worst kind. A v0.4.0 cut-blocker — the core capture path silently fails in kiro-cli.

**How to apply:** FIRST diagnose: check the actual cwd of the gate's `cmk mcp serve` node process (Get-CimInstance won't give cwd directly; use the agentSpawn probe's cwd we already saw = the project, OR check if kiro launches MCP from project cwd). If kiro launches MCP from the PROJECT cwd → process.cwd() fallback SHOULD work → the bug is the cmd.exe /C wrapper resetting cwd, OR CLAUDE_PROJECT_DIR is set to something wrong. If kiro does NOT cd to the project → we must inject the project dir. FIX OPTIONS: (a) the kiro-cli agent-config mcpServers entry can't bake an absolute project path (the agent is GLOBAL/~/.kiro/agents) — so rely on kiro launching MCP from the project cwd + make subcommands.mjs:1907 ALSO honor a Kiro project env (KIRO_PROJECT_DIR? check kiro docs) or trust process.cwd(); (b) verify what cwd kiro gives — if correct, the real bug is elsewhere (cmd.exe wrapper, or the write went somewhere). Test-first (mk_remember writes to the cwd-resolved project when CLAUDE_PROJECT_DIR is unset), live-verify in kiro-cli the fact LANDS. This BLOCKS the v0.4.0 tag — capture must work in kiro-cli. Relates D-185..190 (cross-agent Claude-only-mechanism class), D-198 (the agent-location sibling).
