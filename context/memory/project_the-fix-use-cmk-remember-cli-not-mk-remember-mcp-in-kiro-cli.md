---
id: P-GJ6NK9QE
type: project
title: THE-FIX-use-cmk-remember-cli-not-mk-remember-mcp-in-kiro-cli
created_at: 2026-06-24T16:01:21Z
write_source: user-explicit
trust: high
source_file: user-explicit
source_line: 1
source_sha1: b03daaef313b3deb62bf1d9fe98a2bcd2979d4c9b6834b43ccee33b01eb216ff
related: [CONFIRMED-kiro-bugs-5873-5662-custom-agents-dont-get-mcp-too, kiro-cli-mcp-list-reveals-agent-server-link-broken-includemcpjson-shows-legacy]
---

★★★ THE FIX (the user's insight, 2026-06-24): use the CLI COMMAND `cmk remember` instead of the MCP TOOL `mk_remember` in kiro-cli — it BYPASSES the Kiro MCP bug (#5873) entirely. PROVEN: `cmk remember "..."` in gate4 → "saved to P/MEMORY.md [P-7DXHJMW9]", audit 1→2, LANDS correctly. The Kiro bug #5873 only affects MCP TOOLS (kiro doesn't wire MCP tools to custom agents' LLM); `cmk remember` is a plain SHELL COMMAND — kiro runs shell commands fine (the hooks prove it: cmd.exe /c cmk hook works). So the explicit-capture path works via the CLI, just not via MCP. BONUS: the scaffolded memory-write SKILL.md ALREADY has both surfaces — "Preferred: the cmk MCP tools (when connected)" + "Fallback: the cmk CLI (when the MCP server isn't connected)". THE PROBLEM: in kiro-cli the MCP server LOOKS connected (process running), so the agent picks mk_remember (MCP) → kiro silently drops it (#5873) → no error → agent never falls back to the CLI. THE FIX: for KIRO, the skill/instruction must PREFER `cmk remember` (CLI) over `mk_remember` (MCP) — because in kiro-cli the MCP tool is silently broken but the CLI works. Options: (a) a Kiro-specific memory-write skill variant that says "use cmk remember CLI, not the MCP tool" (kiro-skills.mjs already translates the skill per-agent); (b) the AGENTS.md / agent prompt instruct "in this environment, save with the `cmk remember` shell command"; (c) since the cmk hook commands are already trusted (toolsSettings.shell.allowedCommands), add `cmk remember` to the trusted shell commands so it runs prompt-free. This makes kiro-cli FULLY work (automatic hooks + explicit cmk remember CLI) — closing the cut-blocker WITHOUT waiting for Kiro to fix #5873. Note: the --project arg + @claude-memory-kit fixes are still correct for when kiro DOES fix MCP, but the CLI route is the NOW fix. Note the trusted-commands must include `cmk remember` so it doesn't prompt.

**Why:** The user's insight cracked the cut-blocker: `cmk remember` (CLI shell command) bypasses the Kiro MCP bug (#5873) that breaks the mk_remember MCP tool. PROVEN to land in gate4. The skill already has a CLI fallback but doesn't trigger it because the MCP server looks connected (no error). So instructing Kiro to prefer the CLI `cmk remember` makes explicit capture work in kiro-cli NOW, without waiting for Kiro to fix #5873.

**How to apply:** IMPLEMENT: make Kiro prefer `cmk remember` (CLI) over mk_remember (MCP). Cleanest: (1) the agent prompt (kiro-cli-agent.mjs inline prompt) + AGENTS.md instruct: "To save a durable fact in this environment, run the `cmk remember \"...\"` shell command — do NOT rely on an MCP memory tool." (2) ensure `cmk remember` (+ cmk remember with args) is in toolsSettings.shell.allowedCommands so it runs prompt-free (currently only cmk hook + cmk-guard-memory are trusted — add `^cmd\.exe /c cmk remember` / `^cmk remember`). (3) optionally a Kiro-variant memory-write skill via kiro-skills.mjs that flips Preferred/Fallback for Kiro. Test-first: assert the kiro agent prompt mentions cmk remember + the trusted-commands include it. Live-verify: state a pref in kiro-cli → agent runs cmk remember → fact LANDS. This closes the cut-blocker. KEEP the --project + @claude-memory-kit MCP fixes (correct for when Kiro fixes #5873). Relates #5873, the memory-write skill's existing CLI fallback, kiro-trusted-commands.mjs.
