---
id: P-LPJ7BNSB
type: project
shape: State
title: 'RESUME: v0.5.0 tag blocked ONLY on the Cursor gate — everything else done'
created_at: 2026-07-08T20:09:04Z
write_source: user-explicit
trust: high
recurrence_count: 1
source_file: user-explicit
source_line: 1
source_sha1: b24857653d580da4eb6aa3ce8083006796956feb789ff5821a1e355dd5d3d5c0
related: [kiro-gate-v0-5-0-complete-all-3-sessions-pass-kit-works-end, d-292-resolved-all-three-agent-gates-block-the-v0-5-0-tag-co]
---

RESUME POINT (context compact, 2026-07-08): v0.5.0 tag is blocked on the CURSOR gate only — Claude gate PASSED, Kiro gate COMPLETE (all 3 sessions). Next: back up + clean ~/.claude-memory-kit, then user drives Cursor S1→S2→S3 in the already-installed folder C:\Temp\cursor-gate-v050b. All L3-timeout + scope-note fixes are merged; main is green; global is repacked to the fixed 0.5.0.

**Why:** This session took v0.5.0 from code-complete to nearly-tagged. DONE + MERGED to main: (1) D-293 memory-leak fix (PR #263); (2) Task 148 privacy screen full build + review (PR #264); (3) D-300 mk_search unknown-scope false-'embedder unavailable' note fix (PR #265); (4) D-301 L3-promote 20s→120s site-aware timeout fix (PR #266, main tip 01cbd23 + guide-sync b2d2742). Global repacked to fixed 0.5.0 (INVISIBLE_CODEPOINT_HEX + PII_JUDGE_TIMEOUT_MS=120s present). FILED for v0.5.1 (NOT tag-blocking): Task 203 (distill starvation), Task 204 (incremental-resumable principle ADR), Task 205 (cmk install self-lock on its own MCP-server DLL). D-292 RESOLVED (P-RSLAJ6C4): the tag is blocked on ALL 3 agent gates (Claude+Kiro+Cursor) because the README claims Kiro/Cursor compatibility — shipping unverified = a lie. GUIDE SYNC done (b2d2742): all 3 cut-gate guides (cut-gate.md + -kiro + -cursor) now carry E2 (privacy screen) + E3 (learn-loop) checks. GATE STATUS: Claude=PASS (Sessions 1-3 + cold-open, from earlier this session). Kiro=COMPLETE (P-TBGK35LC): S1 build+capture (5 correct rich memory files C:\Temp\kiro-gate-v050), S2 recall (reopened same folder), S3 cold-open wedge (C:\Temp\kiro-coldopen-v050, applied uv/venv/ruff/layered/test-first unprompted). Cursor=PENDING. The Kiro S1 transcript got contaminated by me running CLI checks in the Kiro-open folder (P-4VAY63ST) — lesson: assistant stays OUT of a gate folder while the agent-under-test has it open.

**How to apply:** RESUME the Cursor gate: (1) FINISH the user-tier clean — back up ~/.claude-memory-kit to C:\cut-gate-backups\<N>_v0.5.0_cursor-preclean\ (find next free N; the Move-Item aside errored only on a Remove-Item path-guard false positive — just Move-Item ~/.claude-memory-kit to ~/.claude-memory-kit.cursor-aside without pre-removing, or pick a fresh aside name) so Cursor starts from ZERO; (2) Cursor folder C:\Temp\cursor-gate-v050b is ALREADY installed (--ide cursor --with-semantic, git identity Alex Personname/«EMAIL», all 3 .cursor surfaces verified: hooks.json 6 events, mcp.json claude-memory-kit, rules/*.mdc; semantic hybrid); (3) USER DRIVES Cursor (restart Cursor after install — the D-262 binding rule — before any live check), assistant stays OUT of the folder: S1 = 'Create a Python web chat / FastAPI backend' stating preferences → check memory files land (the bar: if memory files write correctly, the kit works); S2 = reopen/new Cursor session same folder, ask standing rules + structure → recall works no re-brief; S3 = fresh folder cold-open 'start a new Python backend' → wedge applies persona unprompted; (4) score E1 wedge + E2 privacy (uv init → committed transcript «USER»/«NAME» masked, redactions.log) + E3 loop (recall.log) from the FILES after each session. The user's bar (from this session): 'we are checking the kit works, if we have memory files then it works' — do NOT over-fixate on B9 auto-extract-vs-explicit or E3-judge (which only fires on a failure/correction, absent in a clean session). When Cursor passes → USER tags v0.5.0 (git tag v0.5.0 + push → publish.yml publishes npm+GitHub Release; the tag is the user's outward step). ALSO owed but NOT tag-blocking: commit the accumulated dogfood memory (many uncommitted context/ files this session — pre-commit screen: validate-maintainer-name-confined + no gitignored tier). Relates P-TBGK35LC (Kiro done), P-RSLAJ6C4 (D-292), the 3 cut-gate guides.
