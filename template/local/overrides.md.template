<!-- Cap: 1000 chars · Last distilled: {{TODAY}} · Last health check: {{TODAY}} -->

<!--
overrides.md records machine-specific overrides of preferences declared
elsewhere. When the user has different defaults on this machine vs. their
normal setup (editor, shell, tool preference), record them here. Highest
precedence in the 3-tier model. Gitignored.
Use `cmk config --show-origin <key>` to trace which tier a setting came from.
Three fixed sections per design §2.1.
-->

# Machine-specific overrides (local tier)

## Tool Overrides

<!--
Examples:
- preferred-editor: neovim (instead of vscode from SOUL.md — this machine has no graphical UI)
- shell: pwsh (instead of bash — Windows laptop)
- python: /opt/python-3.13/bin/python (instead of system python)
-->

## Behavior Overrides

<!--
Examples:
- never run docker on this machine (low RAM)
- assume offline (this machine is air-gapped)
-->

## Path Overrides

<!--
Examples:
- working-dir: D:\projects\ (instead of C:\dev — D: has more space)
- cache-dir: /var/tmp/cmk (instead of $HOME/.cache — XDG override)
-->
