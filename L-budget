
 RUN  v4.1.8 C:/Projects/core-memory-kit

 ❯ tests/cli-inject-context.test.js (44 tests | 1 failed | 41 skipped) 96ms
       ↓ returns hookOutput with the documented Anthropic shape
       ↓ hookOutput.additionalContext equals the assembled snapshot
       ↓ snapshot contains content from all 3 tiers when all 3 exist
       ✓ snapshot orders tiers local → project → user (per design §7.1: highest-priority first) 51ms
       ↓ snapshot is ≤ DEFAULT_CAP_BYTES (13 KB) on the small fixture
       ↓ reports bytes count matching the snapshot byte length
       ↓ completes within the 500ms NFR-1 budget on a small fixture
       ↓ walks up from a subdirectory under projectRoot to find context/
       ↓ honors MEMORY_KIT_USER_DIR env when userDir arg is undefined
       ↓ emits empty snapshot when no tiers exist; hookOutput shape still valid
       ↓ skips a tier when its directory is absent (does not throw)
       ↓ duplicate ID across project + user tier: project wins; user is logged as shadowed
       ✓ triple-tier collision: local wins; project + user both logged as shadowed 18ms
       ↓ no duplicates → no shadow events, no shadowed_by.log writes
       ↓ private fact body sentinel NEVER appears in additionalContext
       ↓ non-private fact body CAN appear when something explicitly reads it (regression guard)
       ↓ strips the multi-line <!-- format-explanation --> header from the snapshot
       ↓ drops placeholder seed bullets (all-zero sha1 / (example) marker) and their content
       ↓ strips per-bullet provenance comments (sha1/source/trust) — only the fact + id remain
       ↓ KEEPS real captured facts (real sha1) verbatim with their citation id
       ↓ excludes a tier whose content is entirely seed/placeholder after cleaning
       ↓ shrinks the snapshot to mostly real content (noise no longer dominates)
       ↓ oversized fixture: snapshot ≤ capBytes; truncation event logged; lowest-tier dropped first
       ↓ within-cap fixture: no truncation event
       ↓ user tier > U-budget → drops user-tier sections from end; PROJECT + LOCAL untouched
       ↓ default install (seeds only) injects NO placeholder seed bullets or scaffolding (#R)
       × local tier > L-budget → local-tier sections drop; user + project untouched 25ms
       ↓ tier_truncated_to_budget events land in truncation.log as NDJSON
       ↓ per-tier truncation runs BEFORE total-cap fallback (configuration safety)
       ↓ drops the LOW-trust section and KEEPS the high-trust one — even when high sits in the LAST section
       ↓ protects a MIXED section as a unit — a high-trust bullet shields the low-trust bullet beside it (section-granularity, intentional)
       ↓ breaks trust ties by recency — drops the OLDER same-trust section first
     ↓ exits 0 and stdout parses as the documented hookOutput JSON
     ↓ with a present .mjs path → `node <path>` directly, windowsHide, NO shell (the popup fix)
     ↓ with no/absent path → graceful shell:true bin fallback (corrupt install)
     ↓ resolveCompressLazyPath() finds the real bin/cmk-compress-lazy.mjs
     ↓ the resolved default → a node-direct (NO shell) descriptor — the Kiro-path popup is gone
     ↓ respects $CMK_COMPRESS_LAZY_PATH override
     ↓ non-empty snapshot opens with the authoritative-memory preamble
     ↓ preamble carries the ranked ground-truth hierarchy + the key authority line
     ↓ empty snapshot stays empty — no preamble without memory behind it
     ↓ preamble fits the §7.1 cap slack (Σ tier budgets 12,275 + preamble ≤ 13,000)
     ↓ total snapshot (preamble + tiers) honors a custom capBytes
     ↓ over-mutation guard: tier content + dedup/truncation behavior unchanged by the preamble

 Test Files  1 failed (1)
      Tests  1 failed | 2 passed | 41 skipped (44)
   Start at  13:06:47
   Duration  1.28s (transform 591ms, setup 0ms, import 762ms, tests 96ms, environment 0ms)

JSON report written to C:/Projects/core-memory-kit/.test-logs/last-run.json
