#!/usr/bin/env bash
# cmk-compress-session — SessionEnd hook stub (Task 17 scaffold).
#
# Real implementation lands in a later Layer 4 / Layer 6 task: Haiku-
# compress sessions/now.md → sessions/today-{YYYY-MM-DD}.md (claude-
# remember's NDC pattern) and truncate now.md so the next session
# starts fresh.
#
# Hook protocol: payload on stdin; JSON on stdout; diagnostics on
# stderr; {"continue": true} lets SessionEnd complete normally.

cat >/dev/null
echo "cmk: SessionEnd hook (cmk-compress-session) — not yet implemented" >&2
echo '{"continue": true}'
exit 0
