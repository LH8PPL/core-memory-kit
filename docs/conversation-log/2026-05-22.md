---
date: 2026-05-22
session_count: 1 (continuous, ongoing)
participants:
  - Lior Hollander
  - Claude Opus 4.7
related_adrs: [0007, 0008]
related_research: [claude-ai-deep-research-option-b]
related_specs: [v0.1.0/requirements.md]
tags:
  - session-log
  - option-b-applied
  - documentation-discipline
---

# Session log — 2026-05-22

Today's session: Option-B Deep Research returned from Claude.ai; we analyzed the findings, identified bank/airgap injection as a Claude.ai memory artifact, and established the full documentation discipline (this conversation-log entry is one product of that decision).

## Thread 1 — Option-B research returned

**User** placed the Claude.ai Deep Research output in the kit's `docs/` directory: `claude-ai-deep-research-results-claude-memory-kit Targeted Research Report.md`.

**Claude**: read the full report (~400 lines, dense, very actionable). Extracted 10 proposed changes to `requirements.md`:

| FR | Change |
|---|---|
| FR-9 | 6 hooks → 5 + 1 Setup. Drop PreToolUse as primary write hook. Narrow PostToolUse matcher to `Write\|Edit\|MultiEdit`. Async for memory writes. |
| FR-9 paths | Kit-unique segment in command paths (`.claude/memkit/bin/...`) to dodge Anthropic bug #29724. |
| FR-14 | 6-char random base32 → 8-char content-addressed SHA-256 with tier prefix. |
| FR-14 consolidation | Merged bullets get NEW hash; preserve originals in `merged_from:` frontmatter. |
| FR-19 | Scheduled cron → two-stage (cheap regex filter + Haiku rolling window every ~5 turns + Stop + SessionEnd). |
| FR-26 | 6 MCP tools → 5: `mk_search`, `mk_get`, `mk_timeline`, `mk_cite`, **`mk_remember`** (NEW). |
| FR-NEW | Add `cmk config --show-origin` and `cmk doctor`. |
| FR-NEW | First-match-wins at observation level; deep-merge at settings level (Git semantics). |
| FR-17 | Specific SQLite schema with `source_sha1`, WAL mode. |
| NFR-1 | Concrete hook timeouts (SessionStart 30s, etc.). |

## Thread 2 — "Bank context — yes or no?"

**Claude noticed**: the research report mentioned "regulated bank / air-gapped" deployment context throughout the TL;DR, Stage 3 recommendations, and caveats. The user hadn't mentioned banking in the conversation. Three possibilities raised: (a) research model invented it, (b) user actually does work at a bank but hadn't said, (c) it leaked from a prior session.

**User**: *"Maybe later, but not v0.1"* — personal use for now; design with future bank-deployment in mind (pluggable compressor) but ship only Haiku adapter in v0.1.

**Outcome**: ADR-0008 (bank/airgap deferred to v0.2+ but compressor designed pluggably).

## Thread 3 — Should we re-run Claude.ai research?

**User**: *"so you dont want to re run the claude research?"*

**Claude**: honest assessment — **no**. The bank/airgap framing taints maybe 10% of the report. The load-bearing recommendations (5+1 hooks, content-addressed IDs, two-stage compression, FTS5 schema, 5-tool MCP, first-match-wins merging) hold regardless of deployment context. Re-running costs 15-20 minutes for marginal cleanup.

Committed to: when applying changes, drop bank-specific framing; reframe "pluggable compressor for bank" as "clean compressor interface for future flexibility"; skip Bedrock + local-LLM adapters in v0.1.

## Thread 4 — Claude.ai memory injection

**User**: *"cluade ai research does that all the time to me becuase he know i work at an aiggapped/bank enviornment, and sometimes he makes assumtions that i didnt ask him to do, and i dont know what to do about it."*

**Claude**: real Claude.ai behavior. Documented workaround:

1. **Scope-override sentence in every prompt** — explicit "ignore prior context" with a "stop and re-read if you find yourself assuming X" trigger.
2. **Counter-anchor with explicit positive context** — "deployment is a solo dev's laptop, no enterprise constraints."
3. **Memory hygiene** — Settings → Profile → Manage memories → delete the specific entry. Or use temporary chat mode.

**Outcome**: process doc → [process/scope-override-claude-memory.md](../process/scope-override-claude-memory.md).

## Thread 5 — Skip Option B re-run; ChatGPT still running

**Claude**: suggested running Option B research targeted-design as a parallel task using own tools. **User pushed back**: *"no, that is not a good way to do option b, i will ask claude deepreasearch to do option b as we agreed"*. Correct correction; the original plan was both research outputs in parallel from two research-mode AIs. The shortcut would have been worse.

**Claude**: agreed; offered a tightened Option B prompt with the same "discover beyond the named systems" improvements applied earlier to Option A.

**User**: did Option B earlier already. ChatGPT (Option A) still running.

## Thread 6 — Documentation discipline established

**User**: *"i want everything to be documented with every decision, every doc, url, link, citation, process, etc. in md files so i can put in my liorwiki raw so i can process it for later and this is the best for my wiki project and if i know the why, who, when and what, i will learn more and my research will be more complete."*

**Claude**: proposed full docs structure (adr/ + process/ + research/ + sources/ + conversation-log/ + SOURCES.md), wrote ADR-0002 as a complete template. Asked for format approval.

**User approved**: full ADR format + bulk backfill in one batch.

**Outcome (in progress)**: 23 docs files being written this session.

| Category | Count |
|---|---|
| Index files (docs/, adr/, process/, research/, conversation-log/, sources/) | 6 |
| ADRs (0001-0008) | 8 |
| Process docs | 4 |
| Research notes | 3 |
| Source deep-dives | 2 |
| Conversation logs | 2 (this file is one) |
| **Total** | **25** files committed |

## Lessons from today

| Lesson | Location |
|---|---|
| Claude.ai memory feature injects unwanted prior context | [process/scope-override-claude-memory.md](../process/scope-override-claude-memory.md) |
| Bank/airgap = future consideration, design pluggably for v0.1 | [adr/0008-bank-airgap-deferred-to-future-version.md](../adr/0008-bank-airgap-deferred-to-future-version.md) |
| Don't shortcut research that's been formally agreed — even if "faster" | This conversation log, Thread 5 |
| Every decision becomes an ADR going forward; every research input gets a notes file; every session gets a log entry | Whole docs/ structure |

## Open questions at time of this writing

- Option A research from ChatGPT still running. When it returns, fold findings into requirements.md alongside Option B's.
- Then revise requirements.md based on combined research.
- Then user re-approves.
- Then write `design.md`.

## Commits / tags / pushes today

| Repo | Action | Status |
|---|---|---|
| `claude-memory-kit` | Documentation backfill commit | Pending — at end of this session |

## What's still open at end of session

- `docs/SOURCES.md` expansion to master URL index
- Existing Option-B research file needs to move into `research/` with proper frontmatter
- The big "docs: backfill governance documentation" commit
- Awaiting ChatGPT Option A
