# Release plan — task → version map

> **Single source of truth for WHICH version each task ships in.** When deciding "what version does task X go in?", this file answers it. `tasks.md` holds task DETAIL + checkbox state; this file holds the version LANE. Update it in the same batch whenever a task's target version changes.

## How we decide version (the rule — D-24)

Each **MINOR** version (0.X.0) ships exactly **ONE differentiator** — a "wow" a user can feel — then: build → live-test (`npm run live-verify` + a manual run) → publish → next. **PATCH** versions (0.2.X) are polish / fixes / follow-ups to the *current* wow, with **no** new differentiator. **Breadth** features (other agents, team features) are valuable but off the wow-path → a later minor.

The wows, in order (D-25 video-parity, D-26 the three wows):

- **v0.2.0 — THE WEDGE**: cross-project persona cold-open. Open Claude on a brand-new project and it already knows how you work.
- **v0.3.0 — RECALL + CONSISTENCY**: "what did we decide & why, weeks ago?" + Claude notices and reconciles when it contradicts itself (D-26 #2 + #3).
- **v0.4.0+ — BREADTH**: other coding agents, team/shared features.

---

## v0.2.0 — the wedge (cutting now)

The differentiator is the wedge; the rest are the quality fixes that make it *feel* right (D-25).

- [x] **86** — wedge multi-rule promote (concurrent SessionEnd + transcript classifier) — _shipped #107_
- [x] **87** — compression input = the conversation, not file-write logs — _shipped #108_
- [x] **84b** — supersede stale state across the 3 compression layers — _shipped #109_
- [x] **88** — compressor `--tools ''` sandbox flag — _shipped #110_
- [x] **name de-identification sweep** — _DONE 2026-06-04 (D-51): the maintainer's name → "the user"/"the maintainer", name-based run labels → "live-test" across 54 .md (537 repl.); the durable rule is in CLAUDE.md "Name privacy". (Code/scripts/tests were missed — extended in Task 122, 2026-06-09.)_
- [x] **91 — MEMORY.md graduation (write-lock safety valve)** — _shipped 2026-06-04, PR #113 (D-54/D-57). Transactional cap-pressure valve, project MEMORY.md only; generalized to all tiers by Task 94._
- [x] **94 — load-cap (not write-cap) + universal graduation** — _closed 2026-06-12: 94.5 verified via cli-load-cap tests + cut-gate9 E1/B5/B7 (2026-06-11)._ — _CORE shipped 2026-06-04, PR #114 (D-61/D-63): 94.1 load-cap (writes always succeed; no write-lock on any tier) + 94.2 all-tier graduation (persona graduates to `fragments/` — the D-60 persona-lock fix). The never-lose invariant is delivered. 94.3 (proactive SessionEnd graduation sweep) shipped 2026-06-05 (D-65); 94.4 (= Task 93 importance-aware inject) shipped 2026-06-05 (D-66). Remaining: 94.5 (live cold-open re-test — the user's outward action)._
- _**Friend-handoff reframe (D-61):** the v0.2.0 tag is a milestone (no write-lock anywhere, zero loss); the "good enough to give a friend" bar is the **v0.3 recall lane** finish (Task 75/84/80c), not the tag. The user (2026-06-04): "this is not a good product right now… I will continue to do all the tasks until we have everything this kit promises."_
- [ ] **manual live test** — the user's clean-slate run on a fresh project (pre-tag; the cut gate per D-24). _Guide ready: [`docs/process/cut-gate.md`](process/cut-gate.md). The automated half is green (`npm run live-test`, D-50); the manual run covers R1/R2 + the recall feel. **Unblocked now that Task 91 shipped — re-run fresh on a NEW artifact (rebuild the tarball); the write-lock is fixed so multi-session capture + cap-stability can pass.**_
- [x] **90 — `cmk install` allow-lists `Skill(memory-write)` (D-52)** — _shipped 2026-06-04._ The cut-gate live run caught that Task 69's skill-delivery reintroduced a "Use skill?" prompt that Task 79's bash allow-list didn't cover (composition gap); install now allow-lists the skill too, so explicit capture is friction-free. (+ drive-by fix of a pre-existing time-bomb test.)
- [x] **status audit DONE** (D-47) — flipped verified-done: **24, 34, 56, 76, 78**; closed obsolete **44**; confirmed OPEN: **67, 69, 41.4, 45.2, 47, 51** (+ 84 stays open: 84c deferred). Methodology: evidence per task, not memory.
- [x] **69 — skills-as-delivery / de-bloat CLAUDE.md (PULLED INTO v0.2.0; D-48 → implemented D-49)** — 5 sub-tasks shipped: 69.0 rewrote the stale+UNSAFE memory-write skill (was hand-editing MEMORY.md, bypassing Poison_Guard = F1 leak class — **security**; now `Bash(cmk remember *) Bash(cmk forget *) Read` least-privilege, MUST/NEVER gate) · 69.1 `cmk install` scaffolds `.claude/skills/` (route-equivalence) · 69.2 one source (`template/` canonical → `plugin/` mirror via `sync-plugin-skills.mjs`) + `validate-skill-sources.mjs` drift+safety guard · 69.3 slimmed the CLAUDE.md block to facts + a skill pointer · 69.4 tests (17 cases incl. "gate bites" negatives). _Shipped 2026-06-04, PR #111; 1394 suite green + live-verify PASS 2/2+2/2._

## v0.2.x — wedge polish (patch, no new differentiator)

**→ v0.2.3 — the cut-gate5 sweep findings (next patch; all in [`tasks.md`](../specs/tasks.md) §"v0.2.3").** Surfaced by the 2026-06-07 cut-gate5 live run + full CLI sweep. v0.2.2's ★ headline passed; these are the honesty/automation fixes the sweep + the user's "no code-word passes; a regular user expects everything automatic" push surfaced. Headline = **Task 108** (unify CLI+MCP over one core + resilient `--from-file`/stdin input + parity guard; fixes D-81 backtick corruption; research-first on basic-memory + mempalace). Then: **109** register-crons broken on Windows (D-83) · **110** `cmk forget` must auto-reindex, no manual command (F-7) · **111** `cmk persona generate` real-run timeout (F-2) · **112** weekly-curate real verification (F-4) · **113** queue review/conflicts real verification (F-9) · **114** import-anthropic real verification (F-13) · **115** transcript temp-file hygiene · **116** coverage hardening so "never tested since v0.1" can't recur (D-84) · **117** free-speech intent→action coverage — make the kit invisible (**D-85:** the regular user never runs commands — *even the maintainer* drives via Claude, never types `cmk`; every intent needs an automatic or Claude-mediated path). · **118** resolve R2/D-80 (the `cd`-compound permission prompt) via the MCP-first surface — install allowlists `mcp__cmk__*`, rides 108 (D-80 revisited with new evidence). _Decision trail: D-80/D-81/D-82/D-83/D-84/D-85._

**⛔ v0.2.3 CUT-BLOCKERS surfaced by the live cut (D-102, 2026-06-09) — must land BEFORE the `v0.2.3` tag:**

- [x] **122** — 🔴 name-privacy: extended the D-51 de-id sweep to CODE (the maintainer's real name was in shipped `src/` + scripts) + added the `validate-maintainer-name-confined.mjs` guard so it can't recur. _shipped 2026-06-09 (v0.2.3)._
- [x] **121 (user-facing parts)** — fixed the MCP server's hardcoded `version: '0.1.0'`, the "v0.1.0/v0.1.x" user-facing capture messages, CLI.md's stale tool list, and flipped ADR-0014 → Accepted. _shipped 2026-06-09 (v0.2.3)._ **Comment-normalization internal: NOT a task** — 121.6 settled it (the `v0.1.x` deferral-markers stay as a documented convention meaning "a later patch"); nothing trailed to v0.2.4.

**→ v0.2.4 — housekeeping ✅ SHIPPED 2026-06-09.** Cleanup surfaced during the v0.2.3 cut + pre-existing CI hygiene; no new differentiator (still wedge polish). _Published to npm (`@lh8ppl/core-memory-kit@0.2.4`, provenance) + GitHub Release `v0.2.4`._

- [x] **120** — removed the stale `memsearch + Milvus` scaffolding (`doctor` 9→7 checks HC-1..HC-7 + the shipped `milvus-deploy`/`memsearch-index` template files **and the plugin mirror** + genericized the user-facing "memsearch" strings → "Layer-5b backend (not yet shipped)"). **KEPT** the `semanticBackend` seam — the §9.3.1 backend-choice deferral STANDS. Surfaced 2026-06-08 when `cmk doctor` showed "memsearch installed" during the v0.2.3 cut (D-101). _shipped 2026-06-09, PR #145 (v0.2.4). Live-verified: `cmk doctor` → 7 checks, 0 memsearch, across ubuntu/windows/macOS CI._
- [x] **119** — bumped `actions/checkout@v4` → `@v6` (Node 24) across all 7 workflows; it was the lone Node-20 laggard (the rest already Node-24). Ahead of GitHub's Node-20 runner removal (2026-09-16). _shipped 2026-06-09, PR #146 (v0.2.4)._

- _**80 / 80c moved to v0.3** (D-57) — env/config capture+recall rides with the recall wow; no longer a v0.2.0 conditional blocker. The prompt-side 80b shipped; the deterministic 80c lands in v0.3 with active recall._
- [x] **92** — LOW-trust drops must leave a trace (lifecycle-map G6; D-55) — _shipped 2026-06-05, D-67._ Each discarded LOW candidate logs a `low_trust_discarded` excerpt+reason trace to extract.log (log-only, not the review queue). Security composition fix: the diagnostic extract.log is now gitignored (carries raw un-screened excerpts).
- [x] **93** — inject drop importance-aware, not tail-order (lifecycle-map G7; = Task 94.4) — _shipped 2026-06-05, D-66._ `truncateTierToBudget` now evicts the lowest-value section first (trust → recency → tail tiebreak); strict generalization of the old tail-drop (legacy tests unchanged). Follow-up: the total-cap fallback still drops whole tiers persona-first (v-next, design §19.3).
- **84c** — de-bias scaffold seed examples (cosmetic; seed-ID regen ripple)
- [~] **72** — user-tier portability across machines (the wedge's cross-machine half) — _CORE shipped 2026-06-05, D-69: `cmk persona export`/`import` (transactional, OS-agnostic bundle) + the D-27 honesty fix. `cmk persona sync` (git auto-pull/push) deferred as a documented fork (72.2)._
- **73** — upgrade/repair: re-render stale placeholders in existing tiers
- **68** — weekly-curate LLM-semantic scratchpad pruning
- **70** — inject/output injection-defense (memory-poisoning defense-in-depth)
- **47** — `cmk doctor --repair` (prompt-then-install per failed HC)
- [x] **52** — dogfood the kit on its own repo (the meta-fix for "we kept losing context") — _shipped 2026-06-10, pulled into the v0.3 lane (D-105/D-108)_
- **48** — promote ask-before-install rule to a proper NFR (doc/requirements housekeeping)
- [x] **77** — rename version-named-but-general docs → version-agnostic (the misnomer class): `specs/v0.1.0/` → `specs/` **and** `docs/journey/v0.1.0-build-log.md` → `docs/journey/build-log.md` — _shipped 2026-06-08, one atomic refactor; ~200 refs updated, `validate-references` + suite green; point-in-time history docs left version-stamped._
- **45.1–45.4** — persona manual controls (surface / `cmk persona accept|reject` / auto-apply / hand-curated conflict — verify which already shipped via the wedge work)
- **live-verify `--permission-mode`** — let project-A actually write files in the harness (Task 89 enhancement)

## v0.3.0 — recall + consistency (the next wow)

**Lane plan approved 2026-06-09 (D-105), 5 PRs in order:** **75.0** (authoritative-memory instruction — pulled forward of 65: cheapest recall lever, no backend dependency; _shipped 2026-06-10_) → **99** (benchmark; baselines = one-shot FTS5 AND agentic/iterative keyword — the embedder must beat the latter, per Anthropic's "start with agentic search" guidance) → **52** (dogfood the kit on its own repo — the real corpus for the bake-off) → **65** (spike sqlite-vec [zvec = named fallback] → embedder ladder from bge-small upward, bigger-wins-ties, numbers decide → build behind the §9.3.1 seam) → **README professional upgrade** ✅ shipped 2026-06-10 (published the benchmark table + fixed stale HC counts). **The 5-PR lane plan is COMPLETE.** _Final lane state (2026-06-11): 75.1/75.2 + 104 (all) + 46 + 124/125/126/128/131 ALL SHIPPED; the cut-gate9 live run then surfaced + fixed 8 bugs same-day (Tasks 132/133/136/138/139 + the C3 guide probe, D-122..D-126) BEFORE the tag. v0.3.0 tagged from main with every fix in._

- [x] **65** — Layer-5b semantic recall (the video's "recall is the most important function") — _shipped 2026-06-10 (ADR-0015/D-109): sqlite-vec + bge-base, **R@5 0.941 / paraphrase 1.000** = wow #2 recall-with-reasoning = **video parity reached**._ _MemPalace blueprint (D-70): embedded vector (sqlite-vec) + hybrid pipeline (keyword-boost → temporal-proximity → optional LLM rerank); pairs with Task 99 (benchmark)._
- [x] **99** — recall benchmark (LongMemEval-style R@k) so Task 65 is decided on numbers, not vibes (D-70, MemPalace). _shipped 2026-06-10 (D-107). **Baselines: keyword R@5 0.176 → agentic-deterministic 0.471 → agentic+Haiku 0.529 (paraphrase ceiling 0.300 = the Task-65 headroom).**_
- [x] **75** — recall TRIGGER: make the agent actively USE memory when it needs old context (D-35 active recall). _The load-bearing v0.3 piece (D-57): graduation (Task 91/94) makes facts search-only, so this is what makes overflow facts findable. **Reframed instruction-first (D-64, memory-os Layer 7): the primary lever is telling the agent injected/searchable memory is AUTHORITATIVE — lead with it, don't re-derive — not just the search backend.**_ _**75.0 shipped 2026-06-10** (snapshot preamble + CLAUDE.md authority rule); **75.1 (memory-search skill) + 75.2 (per-prompt hint) shipped 2026-06-10 — the trigger is COMPLETE.**_
- **97** — dynamic trust: trust scores accumulate over time (feedback loop) vs static-at-capture (D-64, memory-os; the user wants it). Composes with F-D + Task 66 + §19.3. _Prior art when slotted: memclaw outcome-based learning (memories scored by task success/failure — the 2026-06-12 research note)._ **→ needs-deeper-design, backlog-no-version-yet (D-218): 3 incompatible trust mechanisms across sources — pick one; safe slice = derived `last_accessed` in the rebuildable index, not committed frontmatter.**
- **80 / 80c** — deterministic env/config capture (observe-edit), so config values are recallable not re-derived (moved from v0.2.0 per D-57; pairs with 75/65).
- **51** — index session-rollup + transcript files for search
- **57 / 58 / 59** — capture Claude's stated positions as decision-facts + inject a "recent decisions" digest + contradiction reconciliation (Phase 3)
- **F-D** — fact-layer auto-supersede (semantic contradiction between captured facts, e.g. uv-vs-venv)
- **95** — dream-style re-curation (D-62): re-curate from RAW transcripts → a REVIEWABLE new output (not in-place), merging dups + resolving contradictions latest-wins + surfacing insights; unifies F-D + Task 55/66 + curate. From the 2026-06-04 Anthropic Dreams primary source. _(+ trajectory-memory input: the 2026-06-12 ruflo research note.)_ **→ needs-deeper-design, backlog-no-version-yet, AFTER Task 66 (D-218/D-166): the re-curation engine (others fold into IT); forks = review-gate-vs-auto-revert + raw-transcript privacy. Our reviewable-no-loss-diff is UNMATCHED in the survey.**
- **96** — memory versioning + redact (D-62): point-in-time recovery of any prior value + a compliance scrub path; completes the never-lose invariant beyond tombstones. _(may slip to v0.4.)_ **→ REFRAMED + RE-LANED to v0.5 (D-218, 2026-06-28): recovery is ~80% free via git; the real deliverable is COMPLIANCE SCRUB (`redact` from all history, git resists it). ABSORBS Task 130 (`cmk purge --hard`). A governance feature, not a core-loop wow.**
- **184** — cross-project search (`cmk search --all-projects`): search across ALL your projects' captured facts, not just the current one (D-247, 2026-07-01 — promoted from a v0.1.0 deferred one-liner after the v0.4.3 cold-open made its case). **→ DEFERRED DESIGN task, NO version pre-assigned (per D-157: the digit tracks shipments, not pre-numbering — laned on cut, not now). NOT v0.4.3 — a new CAPABILITY, not a fix.** _(Corrected 2026-07-01: an earlier note stamped this "v0.5"; that pre-numbered a tail task against the D-157 rule — removed. It ships whenever the design lands, likely v0.4.x/v0.5, but the number is assigned on cut.)_ **DESIGN-first, and the shape is genuinely open:** needs a project registry (or a central user-tier mirror index), cross-project ranking, and a PRIVACY model (a result surfaces project A's content in project B's session — opt-in per query?). **The load-bearing design question:** is 184 even a SEPARATE feature, or a facet of the deferred `cmk persona sync <git-url>` (design §16, 72.2)? A synced user tier might deliver cross-project recall "for free" — so building a standalone `--all-projects` search could be the wrong shape. DECIDE that before building. **Distinct from Task 182** (182 = the persona isn't indexed IN-project, a bug fixed in v0.4.3; 184 = searching DIFFERENT projects, a feature) — and 182 makes 184 LESS urgent: once the persona is searchable, the COMMON cross-project need ("my standing rules") is covered by persona+injection; 184 only adds the RARER "how did I solve X in my other repo." _A governance/breadth feature, not a core-loop wow. **Trigger (D-248): decided when the `cmk persona sync` (72.2) design is taken up — that decision determines whether 184 is a standalone feature or a facet of sync (the load-bearing shape question above); OR earlier if a real user asks for cross-project search ≥2×. Re-verdict at the v0.5 cut if untouched.** D-247._
- **66** — temporal validity (facts stay true / age out correctly) _Prior art: memclaw 8-status lifecycle + crystallization (2026-06-12 note)._ **→ RE-LANED to v0.4.4 (D-218, 2026-06-28); absorbs Task 59. See the v0.4.x section.**
- **71** — external-drift guard (detect + refuse hand-edits to memory)
- **74** — re-inject memory after compaction (PreCompact hook)
- **129** — `cmk config get/set/--show-origin` (the stub became real when `--with-semantic` shipped a real setting; D-121)
- **130** — `cmk purge --hard` ships WITH 96 (the compliance-scrub verb over the versioned store; D-121) **→ ABSORBED INTO Task 96 (D-218, 2026-06-28); rides 96's v0.5 slot.**
- **131** — remove the `cmk view` stub (the viewer IDEA moves to v0.4; D-121)
- **134** — Poison_Guard catalog extension (fixed-prefix adds only; the user's add-don't-diminish directive)
- **135** — pack-completeness validator (`npm pack --dry-run --json` vs the template tree)
- **137** — close the gate-vs-automation gap (5 structural checks from the 2026-06-11 gate-found bug classes)
- **140/141** — canonicalize loop-strip (Sonar's one real finding, D-128) · npm-v12 readiness incl. the node:sqlite migration (D-129)
- **142-145** — within-paradigm sweep (D-130): import-claude-md onboarding · semantic near-dup at write · doctor memory-health · session-start status line
- **146** — kit x workflows composition: swarm recall + concurrent-MCP stress (D-131)
- **147** — `cmk digest` + the decision-journal view (squad-review provenance, D-132)
- **98** — _re-laned 2026-06-12 (was "v0.2.x test-hardening", a destination that no longer exists — caught by the user's orphan audit)_: finish the cli-scaffold smoke isolation (per-test temp CMK_PROJECT_DIR + MEMORY_KIT_USER_DIR). The hazard has fired TWICE (the maintainer's real persona round-tripped, D-69; the dogfood compress engagement, 2026-06-10) — the remaining half is the original cli-scaffold.test.js file.
- [x] **46** — `cmk install --with-semantic` (embedder bootstrap + hybrid-by-default-when-enabled; pairs with 65) — _shipped 2026-06-10, PR #152 (D-111)_

## v0.3.x — the within-paradigm POLISH lane (patch releases; D-130/D-146)

**Why these are PATCH, not a minor (the lane rule above, line 7):** v0.3.0 shipped the differentiator (recall + consistency). Everything below makes the kit *better without changing what it is* — local-first, file-based, no new "wow." So per one-differentiator-per-minor, this lane is **patch-level even though the additions are user-facing features**. The MINOR digit is reserved for the next differentiator (v0.4.0 = Kiro breadth, below). _The user's call 2026-06-13 (D-146), after catching that labeling this batch v0.4.0 would squat on the reserved Kiro minor + silently push Kiro to v0.5._

- **v0.3.1 ✅ SHIPPED 2026-06-15** (npm `@lh8ppl/core-memory-kit@0.3.1` + GitHub Release `v0.3.1`) — the within-paradigm sweep (shipped to main 2026-06-13, one autopilot grant) + the cut-gate live-test fixes: 142 import-claude-md · 141a npm-12 install-time mitigation · 137 gate-vs-automation validators · 145 status line · 144 doctor memory-health · 143 semantic near-dup-at-write · 134 Poison_Guard catalog · 135 pack-completeness · 140 canonicalize loop · 129 `cmk config` · the `.gitattributes` LF-pinning follow-up. **Cut-gate fixes (D-148→D-155):** repair-index crash (#178) · `<private>` write-path leak + SHA-256 migration (#179, D-149) · `<private>` title-truncation leak (D-151) · INDEX-rebuild observability (D-152) · recall structure/architecture trigger (#183, D-153) · persona auto-drain down-payment (#184, D-154) · SonarCloud HF-cache + ReDoS hotspot (D-155).
- **v0.3.x (post-3.1, design-gated)** — the AI-judged-automation tasks surfaced by the v0.3.1 cut-gate (all design-doc-first; same meta-question — replace human-gated queues / manual commands with AI-judged automation):
  - **148** — auto-judged privacy: Claude judges sensitivity at extraction time; default = drop from the committed tier, `<private>` survives as the explicit override (D-150). _Default action settled (Option A); the judgment-site design is the open part._
  - **150** — AI-judged memory-commit flow: close the "memory survives only if the user remembers to `git add context/`" gap via AI-proposes/one-tap-approve, reconciling SETTLED D-126 (no auto-commit) (D-150-adjacent). _**PROMOTED to a committed lane = a RIDER in v0.4.4 (with Task 66), 2026-06-30, D-235 — the user's "make it automatic, not me reminding you," after asking "commit the memories" twice in the Task-151 session.** NOT its own minor + NOT bundled into the in-flight v0.4.3: laned versions carry non-headline tasks alongside their differentiator (the user's point — v0.4.3 already rides 70.4+74 under the 151 headline), so 150 rides as a non-headline task. It rides 66's (not-yet-started) v0.4.4 lane rather than v0.4.3 because 150 is DESIGN-FIRST (the D-126 reconciliation ADR gates code) and shouldn't delay the in-flight persona release; thematic fit too (66 = facts-stay-true-as-they-age, 150 = facts-actually-persist — both durability). Build: (1) the D-126 reconciliation ADR (recommend propose-and-approve, basic-memory-validated, honors the user-owns-the-commit gate); (2) the conversational-surface prompt ("captured N facts — commit them? [y]"). The CLAUDE.md task-boundary dogfood-commit habit (Durable-state-first checkpoint #4) is the MANUAL interim that closes the gap NOW, until 150's structural prompt subsumes it._
- **v0.3.x (dev-tooling, ships nothing to users):**
  - **152** — `validate-index-completeness`: make the hand-maintained doc indexes (adr/research/sources/process READMEs) structural — assert each lists every sibling `.md`, both directions. Converts the recurring index-lag drift class (4 found stale in the 2026-06-15 docs review) into a per-`npm test` check. Same lane/shape as 135/137. _S; no dependencies._
- **v0.3.2 (FINAL SCOPE, D-162/D-164)** — **153: FTS5 query sanitization** ✅ + **152: `validate-index-completeness`** ✅. **147 (`cmk digest` + `DECISIONS.md`) CODE merged but HELD for v0.3.3 (D-164)** — un-framed from the v0.3.2 headline because it's write-only for the AI (a journal a human must open themselves doesn't deliver the "ask the AI, it recalls" thesis); ships complete in v0.3.3. **141b (`node:sqlite` migration) REJECTED on perf** (2026-06-16, D-162): node:sqlite ~10% slower on FTS5 keyword search — better-sqlite3 stays. v0.3.2 is ready to cut. _(Original 141b plan below preserved for the decision trail.)_
- **v0.3.3 ✅ SHIPPED 2026-06-18** (npm `@lh8ppl/core-memory-kit@0.3.3` with provenance + GitHub Release `v0.3.3`) — **the decision journal, fully wired: write (147) + AI-recall (156) + auto-sync (159).** **156: DECISIONS.md AI-recall** (history/evolution/"what did we reject" → read `context/DECISIONS.md`; single-decision → fact search). **155: `cmk get --include-tombstoned`** (opt-in tombstone recovery; D-163, human-only). **157: Bug-1 reindex UNIQUE crash fix** (D-165). **159: journal auto-sync** (no `cmk digest`; D-169). The principle (D-164): a feature isn't shipped until write AND automatic AI-recall work. **Cut-gate-16 (2026-06-18) ran end-to-end** — install/scaffold/capture/recall/privacy/journal gates + the cold-open E1 (the differentiator: a brand-new project got the full persona automatically) all passed; the feature sweep (F-1…F-18) green except F-4 (= the deferred Task 161). **Pre-tag fix (PR #201):** `--scope decisions` no longer emitted a scary `unknown-scope`/exit-2 under the hybrid default (the every-`--with-semantic`-user default experience) — verified through the REAL global install. Two findings deferred to v0.3.4: **Task 161** (compression timeout, D-171) + **Task 162** (no user-facing update path, D-172).

- **v0.3.4 ✅ SHIPPED 2026-06-19** (npm `@lh8ppl/core-memory-kit@0.3.4` provenance-signed + GitHub Release `v0.3.4`; publish.yml success). **Task 161 ✅ #206 — compression timeout → a bounded transient-only RETRY (D-174→D-175).** Measurement INVERTED the original size-spiral premise: the timeout is environmental, not size-driven (kit's own log: largest success 470 KB > largest timeout 334 KB; a 9 KB input timed out). Fix = `compressWithRetry` keyed on a transient-vs-deterministic classifier (9-system retry survey); ceiling-free paths retry, the SessionEnd-hook delegates to the lazy path (D-42/D-79); observability (HaikuFailedError exit_code/stderr) + retry-rate logging (161.12 #208) shipped. **Task 162 ✅ #207 — the user-facing UPDATE path (D-172→D-176):** docs for BOTH routes (README + QUICKSTART §9; the plugin flow VERIFIED against Claude Code docs) + a `cmk doctor` **HC-9** drift-check. **Cut-gate17 ran end-to-end on the real artifact — all gates incl. RT1/VD1 (the new v0.3.4 gates) + the E1 cold-open wedge passed clean.** One finding (D-177, persona transient soft-spot) → Task 151 (v0.4), not a blocker. No deferrals._

- **v0.3.5 ✅ SHIPPED 2026-06-20** (npm `@lh8ppl/core-memory-kit@0.3.5` provenance-signed + GitHub Release `v0.3.5`; publish.yml success). **Task 163 ✅ #209 — ceiling-free compress timeout + backoff fix (D-179).** The ceiling-free compress callers (daily-distill, weekly-curate, lazy session-roll) used the hook-sized **50s** timeout despite having no 60s ceiling → needless `haiku_timeout` when `claude --print` was slow → `recent.md` went 4 days stale. **Two-lever fix:** (1) timeout → 120s on ceiling-free paths (`CEILING_FREE_TIMEOUT_MS`; D-92/F-2 rule); (2) backoff → 5s between retries (was 600ms; `CEILING_FREE_BACKOFF_MS`) so a retry lands AFTER the slow-Haiku window. Grounded in a 19-system field check (escalating-timeout REJECTED — nobody does it; backoff-too-short bug found). **Dogfood-verified on the maintainer's machine** (full update path end-to-end: HC-9 FAIL→PASS, HC-5 SKIP→PASS, HC-2 FAIL→PASS) + a post-publish live distill that exercised BOTH levers (attempt 1 timed out at 120s → 5s backoff → attempt 2 succeeded; 240s total — the exact slow-window the fix targets, which 0.3.4 would have failed). Full suite + stress 5/5 + two-pass review. No deferrals._

- **v0.3.2 — 141b (the original plan, REJECTED 2026-06-16 — kept for the trail): the `node:sqlite` migration** (the structural npm-12 cure — zero native deps, immune to the install-script class) + the Node-engines-floor bump (≥22.5, the user unblocked it 2026-06-12) + whatever else closes the v0.3.x lane (96/130 if ready). **GATED on two spikes that BOTH must pass before migrating (D-147):**
  1. **Perf bake-off** (`scripts/bench-storage.mjs`): time `node:sqlite` vs `better-sqlite3` on the kit's real hot paths (FTS5 keyword query · sqlite-vec cosine query · per-read incremental reindex = the GATED read/search paths; + reindexFull bulk insert · single `cmk remember` write = reported, not gated). Each op N≥20× warm, same corpus, interleaved A/B. **Bar = NO MEASURABLE REGRESSION on the read/search paths** (the user's hardened call, D-147 — not a budget): PASS iff `node:sqlite_p95 ≤ better_p95 × 1.03` per gated path (3% = this harness's noise floor, not perf headroom). The principle: the npm-12 prompt is a one-time setup cost; search latency is paid every query forever — *"slower is worse than asking the user y/n on script install… we dont sacrifice the kit's purpose"* (the user). The D-109 bake-off discipline, applied to storage. **If it fails, 141b does NOT ship — better-sqlite3 stays, 141a's install-time ask remains the npm-12 answer.**
  2. **Cross-platform `sqlite-vec` loadExtension spike** (D-129): confirm the vec extension loads under `node:sqlite` on Windows + macOS + Linux.
  3. **`node:sqlite` FTS5-module spike** (added 2026-06-15, Task 153 research): confirm `node:sqlite` ships the FTS5 module compiled in on all three platforms. `better-sqlite3`'s prebuilt binaries include FTS5 by default; `node:sqlite` may NOT — real-world signal: openclaw #62328 ("node:sqlite missing FTS5 module — memory search keyword fallback broken"). The kit's WHOLE keyword-search layer is FTS5; if it's absent under `node:sqlite`, 141b does NOT ship regardless of the perf/vec results. Cheapest spike of the three — run it FIRST as the fast-fail gate.

## v0.4.x — breadth: cross-agent, one agent per release (D-157)

**The v0.4 differentiator = cross-agent (the breadth wow). One agent per release, Kiro first** (D-127 — the user's daily IDE; dogfood-at-work). Per the one-differentiator-per-minor rule: v0.4.0 builds the adapter SEAM + ships the first agent (Kiro); each subsequent agent is **polish on that differentiator → a PATCH**. The Taskmaster `createProfile()` factory (2026-06-15 note) de-risks this — once the seam exists, adding an agent is a thin per-agent profile, genuinely patch-sized.

- **v0.4.0 — Kiro** (Task 50): build the per-agent adapter seam (`createProfile`-style factory + lifecycle-hook wiring) + ship the Kiro adapter. The seam is the real v0.4.0 work; Kiro is its first consumer. _Verify the Kiro paths against kiro.dev (the §5.1 convergent-third-party precedent)._
- **v0.4.1 — Task 167: the now.md-roll-at-scale fix** (the 105 → 106 → 167 lineage; D-205/D-206). **RE-LANED 2026-06-25 (the user: "next version = this fix"; was the Cursor slot).** The lazy now.md→today roll (Task 105) **gates itself out of existence**: a `cron-active` short-circuit (sentinel-exists, NOT cron-actually-ran) + a busy cooldown skipped the roll every SessionStart across the v0.4.0 dogfood → `now.md` grew to 410 KB un-rolled → inject froze at a 5-day-old day-file → a SHIPPED epic read as still-pending. PROVEN from `lazy-compress.log` (skipped every time, never timed out — the user's "105 wasn't a real fix, just postponed the problem" confirmed). Three sub-bugs: 167.A cron-LIVENESS gate (primary) + 167.B size-triggered roll/cooldown carve-out + 167.C observability + `cmk doctor` HC. _Not a v0.4.0 blocker (the immediate data fix is done — manual `cmk-compress-session` drained it). Relates D-205/D-206, Task 105/106/35._
- **v0.4.2 — Task 173, a SECURITY patch (3 CodeQL alerts)** (2026-06-28). Post-v0.4.1, GitHub code-scanning showed 3 open alerts on `main`: prototype pollution in the `cmk config set` key walker (Medium) + incomplete-escaping/regex-injection in the release-notes extractor (2× High). All fixed with real hardening (self-guarding `setDeep` + full `escapeRegExp`), TDD, 100% line coverage on the touched file, two-pass review. No user-facing behavior change → a patch, no new differentiator. **This took the v0.4.2 slot, so Task 151 slides to v0.4.3** — exactly what the "patch digit tracks shipments, not pre-numbered" rule (D-157, line 123) anticipates; a security fix that's ready jumps ahead of a larger in-flight feature. _Follow-up (2026-06-28): v0.4.2's #29 (prototype-pollution) didn't actually clear — the first guard was pre-loop; CodeQL's query-help requires it INSIDE the walk loop. The corrected in-loop guard merged to `main` (PR #243) and closes #29 on the CodeQL re-scan — it takes NO version (an alert closes on a main push, independent of npm; 0.4.2's runtime security was always identical), and the code rides into the next release (v0.4.3/151). Lesson: read the CodeQL query-help BEFORE claiming an alert is fixed._
- **v0.5.0 — the LEARN-LOOP, Phase 1 (Tasks 190+191+192+193; ADR-0017) — ✅ CUT 2026-07-10 (D-307).** _Tag history: CODE-COMPLETE + release-committed 2026-07-07 (D-286..D-291); TAG HELD (D-292) pending the 3-agent cut-gate. **The gate run (2026-07-08..10) closed it:** the Claude + Kiro gates PASSED live (Kiro §2/§5/§6 including E1 wedge / E2 privacy / E3 learn-loop), and it surfaced + fixed FOUR live bugs invisible to green unit tests — D-303 (Kiro `USER_PROMPT`-empty capture), the Kiro B1 transcript-parity, D-305 (Cursor `/c:/` path), and D-306 (Cursor UTF-8 BOM breaking `JSON.parse` — the real Cursor-Windows capture killer). The Cursor capture fixes are merged + **live-verified through the repacked global on the exact bytes Cursor sends** (bi-turn `now.md`), but the full interactive Cursor gate (R-inject/E1/E2/E3) is **deferred to Task 208** (trigger: Cursor token refresh ~2026-07-24) — the maintainer's free-tier tokens ran out mid-gate. D-307: tag now because the D-292 lie-guard is satisfied (Cursor capture is a TRUE verified claim, not an unverified one; only the exhaustive interactive gate is pending, recorded honestly). **Minor-cut backlog sweep (D-248):** v0.5.1 lane = Task 206 (pre-roll now.md name window, D-304), Task 207 (Claude-bin BOM hardening), Task 208 (Cursor interactive gate, D-307); the ordered agent tail (Codex → Antigravity → …) keeps its triggers._ The kit stops just remembering and starts LEARNING from outcomes: the recall-log (which memory ids surfaced each turn — the attribution primitive), expectation pre-registration + judgment_*.md records (earned method-preferences with an append-only evidence log; misses lock, hits nudge, cycles surface), the Stop-hook judge (four deterministic outcome detectors — tool-result ±, user-correction −, re-ask −, silent-success weak-+ — riding the existing hooks, watermarked so signals measure evidence not turn cadence), and the feedback-screen (rate-limit + burst-hold + audit on every trust delta — the loop's Poison_Guard, built BEFORE the judge could emit a single signal). Phase 2 (Task 194, the confidence-gated search blend) is v0.5.x after Phase 1 proves live. _Sequencing note: v0.5 opened ahead of the Codex adapter by the user's explicit D-286 re-order (a model-availability window); Codex stays committed in v0.4.6._
- **v0.4.4 — Task 66, the temporal-validity engine (wow #3: currency / contradiction-catch)** (D-218, 2026-06-28). Facts stay TRUE as they age — a contradicting `State` fact closes the old validity window + opens the new (graphiti `resolve_edge_contradictions` arithmetic: `invalid_at=new.valid_at`, never delete → point-in-time `as_of` free). A real new differentiator → its own minor, after v0.4.3=Task 151. Ship 66.1+66.3 (shape field + expiry sweep, no-LLM, low-risk) as the v0.4.4 core; 66.2/66.4 (windows + contradiction-catch) ship in the same lane once the contradiction-DETECTION design is grilled. **ABSORBS Task 59** (decision-contradiction reconciliation = the decision-flavored slice of this engine). **+ RIDER: Task 150** (AI-judged memory-commit — a non-headline rider, like 70.4/74 ride v0.4.3; D-235, 2026-06-30): Claude proposes-in-chat "captured N facts — commit them? [y]", user one-tap approves (honors SETTLED D-126 no-auto-commit; basic-memory-validated). Design-first (the D-126 reconciliation ADR gates the small build), which is why it rides 66's not-yet-started lane rather than the in-flight v0.4.3. Thematic fit: 66 = "facts stay true as they age," 150 = "facts actually persist (get committed)" — both durability. The CLAUDE.md task-boundary dogfood-commit habit (checkpoint #4) is the interim that closes the gap NOW. _Re-laned 2026-06-28 from the fuzzy "v0.4 currency" bucket to concrete v0.4.4 via the backlog deep-audit (D-218). Task 95 (re-curation) CONSUMES this engine and ships LATER (D-166: 66 before 95) — not the same version._ **+ 2nd RIDER (D-253a, 2026-07-02): Task 141 phase (a)** — the npm-v12 mitigation (a `cmk doctor` HC for "native binding missing — npm 12 blocked the install script" + the README install note). TIME-SENSITIVE: npm 12 ships ~July 2026, i.e. now; a fresh `npm install -g` under it silently skips `better-sqlite3`'s binding → search crashes at first use. Small (an HC + docs). Phase (b) — the `node:sqlite` structural migration — stays deferred (trigger: npm-12 GA + the first failure report, or the v0.5.0 cut). **→ RIDER RESOLVED AT BUILD TIME (2026-07-02, D-260): phase (a) had ALREADY shipped in v0.3.x** — PR #169 (2026-06-12, D-134) delivered HC-8 native-bindings + the install-time ask + the README note + CLI.md docs; the D-253a sweep re-laned a shipped phase without checking the sub-checkbox (`141a [x]` all along — the stale-entry class one level deeper: this time the PARENT read as open because phase (b)'s trigger keeps it open). Nothing to build; v0.4.4 rides with 66 + 150 only.
- **v0.4.3 — Task 151 (persona-promotion redesign) + 70.4 + 74 + 182 + 183** (D-178, 2026-06-20; **182/183 added 2026-07-01, D-247**). _**RE-LANED 2026-06-28: now v0.4.3** (the security patch Task 173 took v0.4.2). Prior: RE-LANED 2026-06-25 to the version AFTER 167 (was v0.4.2/.3 behind Cursor; the now.md-roll fix jumped ahead of it)._ Pulled EARLY in v0.4 (right after the two committed agents), NOT deferred to v0.5, because the persona is the INJECTION layer — and the origin video's thesis (the kit's founding source, `youtube-to-slide/out/master-claude-memory-…`) is explicit that **recall is paramount and Tier-0 ("check MEMORY.md, already in context, zero cost") is the most-used recall path** — which depends on the injection layer putting the RIGHT curated facts into the frozen snapshot. The D-177 finding (a durable trait can graduate OUT of the injected scratchpad; explicit-promote concentrates in HABITS § Working Style → overflow → eviction) is a hole in that most-used path. **Decision (the user, 2026-06-20): do the FULL redesign once, NOT a v0.3.5 down-payment** — a down-payment pays the full process tax (branch/tests/two-pass/stress/cut-gate/release) for a partial fix, then pays it AGAIN for the real thing; the ROI is wrong. The full 151 folds in BOTH D-177 mechanisms (the confidence-gate + the graduation/routing eviction) + the recurrence-scored LLM-judged promotion. _2026-06-28 (D-215): also folds the `awrshift/claude-memory-kit` propose-then-confirm UX as an OPTIONAL in-conversation mention (heads-up, not a gate — keeps silent auto-drain as the default); their recurrence signal CONFIRMS the already-decided FREQUENCY+RECURRENCE direction, it is not new scope._ _Slotted in the open v0.4.2+ tail (D-157 left it assigned-on-cut, so this fills a slot, not re-opens a lock). Relates D-177 (the finding), D-154 (direction + the v0.3.1 down-payment already shipped — so this is genuinely the full redesign, not the first installment), the origin video thesis._
  - **+ FIX-BEFORE-TAG RIDERS 2026-07-01 (D-247, the user's call) — Tasks 182 + 183, two search bugs the cut-gate cold-open found.** The v0.4.3 gate ran clean through Task 151 (PR1–PR5 all pass live) AND Session 2 same-project recall — but the Session-3 cold-open exposed that **`cmk search` cannot see the promoted persona** (`HABITS`/`USER`/`LESSONS.md` are not in the search index — `listObservationSources` walks `<tier>/MEMORY.md` + `fragments/`, not the persona scratchpads) and that **scaffold `(example)` bullets pollute a fresh project's search** (they out-rank real facts when there are none). **The user's decision: FIX before the tag, not ship-and-patch** — "search must find the persona" is a KIT-LEVEL promise, and shipping 151 (which redesigns that exact persona surface) with the persona unsearchable would be a knowingly-false headline (the lazy-framing class this project forbids). **Both are small + verified-scoped:** 182 — `resolveTierRoot('U')` already points at the persona dir, the parser already tracks `## section` headings, the bullet+provenance format is identical to `MEMORY.md`, so the fix is "add the 3 persona filenames to the tier-U source list + a promote→search→find test"; 183 — skip the sentinel-dated (`2020-01-01` / zero-sha1) seed examples at index time + a fresh-install-search test. **NOT v0.4.3 regressions** (pre-existing; the indexer never walked the persona) and the cold-open still PASSED because the persona reaches Claude via INJECTION (the SessionStart snapshot), not search — so these fix the SECONDARY recall path. **184 (cross-project `--all-projects` search) + 181 (recurrence-vs-adherence) were also found but are NOT v0.4.3** — see their lanes below (184 = a new capability + privacy model, v0.5; 181 = open research, curation lane). _Relates Task 76 (promote writer), 46/65 (search), 184, 181, D-247._
- **Cursor (Task 50) — POSTPONED 2026-06-25 (the user's call), was v0.4.1 (locked).** Originally the second committed agent right after Kiro; the v0.4.0 dogfood surfaced the now.md-roll bug (Task 167) which the user prioritized ahead of it ("postpone Cursor to another version"). Cursor now sits in the ordered tail below — still a committed agent, just no longer the next slot. **→ RE-COMMITTED 2026-07-02 (the user's Option A, D-257): the demand signal fired (the maintainer's friends use Cursor AND Codex) — after v0.4.4, the next two slots are Cursor then Codex (expected v0.4.5/v0.4.6, digits stamped on cut per D-157); v0.5 opens AFTER they ship. The breadth promise is honored before the depth minor — decided, not drifted (Task 196).** _Decision-trail: the original "v0.4.1 — Cursor (Task 50, locked)" lane is preserved here, set aside not erased; the lock was a sequencing intent, not a contract, and real dogfood signal re-ordered it._
  - **⛔ v0.4.5 BLOCKED 2026-07-04 (D-270, the user's call) — the Cursor WIRING is merged (PR #254, unreleased) but the TAG is HELD.** The cross-agent adapters shipped hooks/MCP/install but NOT the promise: a Cursor-only / Kiro-only user (no Claude Code) has NO working automatic engine — the kit's LLM layer shells out to the `claude` binary they don't have, so auto-extract / compression / persona-wedge / temporal-sweep all silently no-op. **v0.4.5 does not tag until Task 200 makes the backend route through the AGENT'S OWN CLI** (`cursor-agent -p` for Cursor, Amazon Q for Kiro) so a Claude-free machine runs the full engine. Shipping "Cursor support" that silently needs Claude Code is a false promise. Task 196's Cursor checkboxes stay done (the wiring IS done); Task 200 is the new gate on the cut.
- **Then, an ORDERED tail — each assigned its patch number WHEN IT SHIPS** (not pre-numbered; the patch digit tracks shipments, and the order may shift on real signal — a Codex API change, a Cursor breaking release, the user's usage): **Cursor → Codex → Google Antigravity → gemini-cli → opencode → cline/roo/windsurf/… as demand warrants** (interleaved with 151 + the recall/curation tasks per real priority). (Prior-art profiles for several of these already exist in Taskmaster — design input, not a port.)
- **50** — the umbrella task (cross-agent install via `cmk install --ide <agent>`). The kit's core (store / compression / search / CLI) stays identical across agents; only the adapter differs (Task 50.2). ADR-0012 (cross-agent future).
- **v0.4 recall lane:** _(→ LANED 2026-07-12: the trio below rides **v0.6.0** — its D-253a "next recall-surface minor" trigger fires there; see the D-309 queue item 5. D-326.)_
  - **149** — recall-trigger architecture ADR: clone the real repos (not the note summaries) and decide keep judgment-pulled (+ D-153 fixes) vs always-search vs hybrid. Research done → [persona/recall research notes](../docs/research/2026-06-14-recall-triggering-models-cross-system.md); ADR pending (D-153 follow-up). _Research + ADR, no code until decided._
  - **176** — graph-recall research + ADR: should the kit add a relational/graph query mode beside keyword + semantic? (D-216, surfaced surveying `iwe`.) Asks WHAT recall modes (complements 149's WHEN-to-recall). **UPDATED D-219: we already WRITE edges (`links`/`[[cross-links]]` + `superseded_by` FK) but never TRAVERSE them — so the task is ACTIVATE-the-edges, not derive-from-scratch (much cheaper start).** _Research + ADR only; an ADOPT verdict spawns a separate build task. Pairs with 149 in the recall lane._
  - **178** — hybrid-search tuning research + ADR: evaluate Reciprocal Rank Fusion (+ other fusion/rerank methods) against the kit's hybrid recall, ON the Task 99 benchmark (D-217/D-219, surfaced surveying `pro-workflow`; the user: "make it a task so it won't get lost"). _Research + measure; numbers decide. An ADOPT verdict is a small swap (could ride a recall-tuning patch). Pairs with 149/176 in the recall lane._
- **v0.4 curation lane:** _(Task 151 — the persona-promotion redesign — is now slotted explicitly at v0.4.2/.3 above, per D-178; it WAS parked in this unnumbered bucket.)_
  - **177** — correction-trained behavior-doc research + ADR: can accumulated corrections TRAIN a durable instruction doc (validated against held-out corrections), not just store facts? (D-217, surfaced surveying `pro-workflow`'s DSPy-style skill optimizer.) Sibling to Task 55 (behavioral patterns) + 151 (recurrence promotion). _Research + ADR only; an ADOPT verdict spawns a separate build task. Pairs with 55/151 in the curation lane._
  - **179** — memory-improvement loop (THE UMBRELLA research/ADR; D-219, the user's idea): should the kit have ONE recurring background loop that makes memory BETTER — re-curate facts (↔ Task 95) + surface insight (↔ 55) + improve the persona/behavior from corrections (↔ 177/151)? Decides unified-loop-vs-separate-features + maps 95/55/177/151 onto it. **CORRECTED D-219: dropped the Hermes "skill-library curation" flavor — the kit has only 2 fixed skills, nothing to curate (the user's catch).** **Run FIRST in the curation lane — it frames the others.** _Research + ADR only; grounded in letta sleeptime + Anthropic Dreams + pro-workflow._
  - **181 — ✅ RESOLVED at the Task-185 sweep (D-253, 2026-07-02); the trigger fired via the learn-loop research arc.** Per-option verdicts: (a) behavioral counting REJECTED (self-judge-circularity evidence), (b) stability-promotion ADOPTED-as-shape (= the judgment confidence gate, Task 191), (c) correction-as-signal SHIPS (Task 192); the friction insight itself is binding design law (recurrence ≠ reinforcement — ADR-0017 Decision #2). 179 (the umbrella it waited on) is likewise resolved-into-ADR-0017. _Original framing preserved in the tasks.md entry per the decision-trail rule. D-246 → D-253._
  - **180** — memory→skill SYNTHESIS (EXPLORATORY research; D-219, the user's idea, self-flagged "maybe going too far"): generate a bespoke skill FROM the user's accumulated memory/persona + a possible improve-loop. DISTINCT from 151 (injects facts) — this EMITS a skill artifact. First job = a GO/NO-GO: is this real, or does the injected persona already cover it? _Small scoping note; sibling-but-separate to 179. NOT a build commitment._
  - **RRF-search note (D-217, NOT a task yet):** `pro-workflow` uses Reciprocal Rank Fusion (k=60) to merge BM25 + vector — possibly cleaner than our current hybrid (Task 65, shipped v0.3.0). NOT breadcrumbed onto the shipped 65/99 (dead weight on `[x]` tasks). If/when a v0.4 hybrid-search-tuning task opens (e.g. out of Task 176's graph-recall ADR or a recall-quality pass), evaluate RRF against the live hybrid on the Task 99 benchmark; adopt only on numbers. Until then it's a logged data-point, not scope.
- **Backlog with HONEST gates (D-218 deep-audit, 2026-06-28 — documented, not version-pinned, per the user's "every task documented even without a version"):**
  - **70.4** — invisible/zero-width/bidi Unicode patterns in Poison_Guard → **security-patch candidate** (cheap, conflict-free; matches the v0.4.2 precedent). The rest of 70 (inject-rescan + fence) = backlog minor lane; 70.3 (output-scrub) = needs-design (gate on a CC output hook).
  - **74** — re-inject after compaction → **fold into the inject-hardening lane** (already works by luck; shrinks to a verify-test + ADR fix). Patch-shaped.
  - **71** — drift-guard → backlog, REFRAMED refuse→reconcile+flag; sequence after Task 70 (shared supply-chain framing).
  - **146** — swarm concurrent-write safety → backlog, RIDES Task 50 (the cross-agent work stresses the race).
  - **57/58** — Phase-3 decision-consistency trio (59 folded into 66) → backlog, gated on an explicit "Phase 3 go".
  - **68** — LLM-prune stale MEMORY.md → backlog, MEASURE-FIRST (ship only if a live baseline proves staleness bites).
  - **148/150** — AI-judged-gating pair (privacy / git-commit) → backlog, DESIGN-FIRST (148 unmatched in survey; 150 reconciles SETTLED D-126). Do other tasks first, then design.
- **v0.4 within-paradigm borrows (from `awrshift/claude-memory-kit`, D-215):**
  - **174** — git-history backfill: reconstruct a working-day's session log when commits exist but no `sessions/` entry was written (closes a real timeline-hole; automatic sweep on the `daily-distill` cron per D-169 + a `cmk backfill` manual override). _M; reads Task 167's compaction-state to avoid racing the roll; independent otherwise. Patch-lane (within-paradigm, no new differentiator) — takes a v0.4.x slot when it ships._
  - **175** — `cmk tour`: narrate the user's OWN `context/` tiers (onboarding/explainer; reads real facts, degrades on empty). The `cmk tour` core is S; the per-agent `/tour` slash surface composes with the cross-agent seam (Task 50). _M; **depends on Task 50** (the seam, for the per-agent slash command). Patch-lane — a v0.4.x slot after the seam._
- **55** — behavioral pattern detection + promotion ("learn how I work," not just facts). _Design dossier (3 sources, 2026-06-12 notes): ruflo trajectory memory (capture HOW a task was done well) · PAI's RELATIONSHIP category (memory of the COLLABORATION itself — what frustrated the user, what landed) + explicit LEARN-phase task retrospectives · memclaw outcome-based scoring. Design 55 as pattern detection + collaboration memory + task retrospectives, not pattern detection alone._
- **viewer (design-first)** — a memory viewer for the non-developer audience (the parked `cmk view` idea — a product question, not a port number; D-121). _Demand evidence: PAI's Pulse dashboard (localhost surface over file-based AI state, 15.8k stars — the 2026-06-12 research note)._
- _**Team-layer companion (Task 127): POSTPONED past v0.4** (the user, 2026-06-12) — Kiro support comes first; the companion-project design (D-119) stands, just later (v0.5+)._ **→ LANED 2026-07-12: the v0.7.0 HEADLINE (D-326)** — the deferral trigger fired (v0.6 planning boundary + the origin creator's TeamOS-beta demand signal); see the D-309 queue item 6.

---

## v0.5 — depth: THE LEARN-LOOP (the differentiator: "the kit learns") — ADR-0017, D-252/D-253

**The v0.5 wow = the loop closes: memory that gets BETTER with use** — the adopted target design
([SYSTEM-MAP §6](../docs/SYSTEM-MAP.md), "our Figure 2"; ADR-0017 Accepted 2026-07-02). Laned at the
Task-185 sweep (D-253). Build = the design's phases, strict order (each is PR-sized):

- **Phase 1 — the oracle-free wedge (v0.5.0 core):** **Task 190** (RECALL-LOG — the attribution
  primitive, build FIRST) → **Task 193** (FEEDBACK-SCREEN — the prerequisite; no utility mutation
  without it) → **Task 191** (expectation pre-registration + `judgment_*.md` files; absorbs 188 +
  181(c)) → **Task 192** (the Stop-hook judge — the full automatic signal portfolio).
- **Phase 2 — close the edge (expected **v0.5.3** per the D-309 queue below; after Phase 1 proves live):** **Task 194** (confidence-gated
  SEARCH blend + survival gate + anti-pattern conversion; the narrow §20.3 amendment lands here).
  **+ Phase-2 batch riders (2026-07-10 research sweep, D-308 — same search surface, PR-sized neighbors):**
  **Task 209** (state-labeled recall — `[current]`/`[superseded]` on serialized facts; A-TMA's
  labels-alone-flip-the-answer finding; deterministic projection, hot path stays enum-ordered) ·
  **Task 211** (rule-based query state-view gate — current/historical intent biases which state
  surfaces; the un-deferred 4-view cut of §16.18's 7-mode classifier) · **Task 212** (memory-health
  behavioral dashboard — writes-per-search/empty-search/redundant-write from the shipped recall-log +
  audit log; trigger-gated: fires when 194's tuning starts, it IS 194's before/after instrumentation).
- **The v0.5 release QUEUE (D-309, 2026-07-10 — the user's "real versions, not v0.5.x" call).** The
  ORDER + CONTENT below are the commitment; the DIGIT still stamps on cut (D-157 STANDS — a ready
  security-patch-class fix may jump the queue and re-stamp what follows, exactly the v0.4.2
  precedent; that hazard is WHY the digits are "expected," not locked):
  1. **v0.5.1 — SHIPPED 2026-07-12** — 203+204 (headline: resumable-distill starvation fix + ADR-0020) + riders 205 / 206 / 207 / 213 / 214 / 215 / 216 / 219 / 220, plus the cut-gate fast-follow **222** (the MCP-preflight now informs-not-prompts on a plain `cmk install`, D-323 — surfaced live in the gate). The hardening lane: security (216 side-door screening), reliability (203/204/219), Windows polish (215 windowless / 207 BOM), install-integrity (220 duplicate-fold), dev-tooling (214). _(Was "203+204 + riders 205/206/207/213" when planned; the lane grew via the D-248 backlog sweep + the security review's 216 + live-gate 222.)_
  2. **v0.5.2 — ✅ SHIPPED 2026-07-13** (npm `@lh8ppl/core-memory-kit@0.5.2` provenance-signed + GitHub Release `v0.5.2`). **The cross-agent breadth patch (SCOPE SET 2026-07-12, the user's call; D-325):**
     the **Cursor adapter** (Task 196, shipped v0.4.5) **+ the Codex adapter** (Task 196 tail, PR #284/D-327) **+ Task 165**
     (parked Kiro-surface fixes → document-not-build, PR #285/D-328) **+ Task 218** (MCP index freshness → became a
     CLI↔MCP PARITY fix, PR #286/D-329). One theme (breadth on the
     Task-50 `defineAgentProfile` seam) → batching in a patch honors the one-differentiator rule
     (same differentiator, not competing). _Task 218 grew via the user's "make them the same": a per-query index refresh on the MCP reads (CLI parity) + 4 audited param drifts closed. Pre-tag doc walk (the user's "are all docs updated?") caught + fixed Codex drift across QUICKSTART / the 4 per-agent sibling footers / glossary._ **Task 208 (Cursor INTERACTIVE cut-gate) is FLAGGED
     PENDING-TOKENS** — the maintainer's Cursor tokens are exhausted (as of 2026-07-12), so the
     Cursor *adapter* ships this lane but the live interactive gate waits for token refresh
     (~2026-07-24) and rides whatever slot is current then. Deliberately EXCLUDED as off-theme:
     Task 223 (L3 label-name privacy — a judge concern, not adapter-surface) + Task 224 (SonarCloud
     crash — server-side, needs the maintainer's web-UI, not a code-lane item). **RE-LABELED from
     "v0.4.6" (D-309):** v0.5.0 shipped ahead of Codex (the D-286 re-order), and tagging a 0.4.6
     AFTER 0.5.0 would misorder npm `latest` — the old label is preserved in the historical notes
     as the pre-D-286 expectation. Breadth-before-more-depth also buys Phase 1 live mileage before
     Phase 2 tunes on its signals.
  3. **v0.5.3 — Phase 2, the learn-loop payoff — ✅ RELEASE-COMMITTED 2026-07-14** (CHANGELOG
     `[0.5.3]` finalized, `packages/cli/package.json` → 0.5.3, CI green; tag `v0.5.3` = the
     maintainer's outward step → publish.yml does npm + the GitHub Release). Task 194 (the
     confidence-gated search blend + survival gate + anti-pattern conversion — the loop's last
     edge closed) + the batch riders 209 (state-labeled recall) / 211 (query state-view gate) /
     212 (`cmk stats memory-health`). All four PRs (#287–#290) merged; the D-334 minor-boundary
     backlog sweep ran (Task 195 name-decision trigger fired → laned v0.5.4).
  4. **v0.5.4 — THE RENAME (Task 195) — ✅ SHIPPED 2026-07-15 (D-335).** Published `@lh8ppl/core-memory-kit@0.5.4`
     (provenance-signed) + GitHub Release `v0.5.4`; the GitHub repo renamed to `LH8PPL/core-memory-kit`.
     The cross-agent name decision laned here at the v0.5.3-cut sweep (D-334) was DECIDED + shipped:
     `claude-memory-kit` → **`core-memory-kit`** (keeps `cmk`), ADR-0021 supersedes ADR-0012, 135-file
     corpus rename + config-dir direct swap, merged (PR #292). A clean single-purpose release.
     **Full cut-gate (§0–§9) ran green under the new name** — every ★ deterministic gate + the live §2/§5/§6
     (build/recall/cold-open E1 wedge, E2 privacy, E3 learn-loop) passed; 4 findings surfaced (D-336 auto-extract
     project-name confabulation · D-337 invisible-unicode guard defeated by maskPii ordering, security · D-338
     invalid-`--shape` exit-0 · D-339 cold-open net-dampens recalled persona), **all pre-existing, none
     rename-caused, none blockers** — D-337 laned to v0.5.5. **Publish took two attempts** — the first tag hit
     npm E422 (provenance repo-URL mismatch: `package.json` said `core-memory-kit` but the GitHub repo was
     still `claude-memory-kit`); fixed by renaming the repo FIRST then re-running (D-340 — the reusable
     ordering rule). **The governance batch (Task 96 + Task 210, + Task 95 if its needs-design gate clears)
     SLID to v0.5.5.** _Outward steps DONE: npm publish + GitHub repo rename. REMAINING (maintainer): `npm
     deprecate @lh8ppl/claude-memory-kit`; SonarCloud project-key update._
  5. **v0.5.5 — the governance batch** (slid from v0.5.4) — Task 96 (compliance scrub) + Task 210
     (deletion-propagation) + Task 95 if its needs-design gate has cleared. **Plus the v0.5.4-dogfood
     fixes:** Task 230 (`cmk install` must REFRESH kit-owned scaffold on re-run/update — the skip-all-
     existing rule over-applies to skills; D-343, PROVEN live during the rename) + D-337 (invisible-
     unicode Guard defeated by maskPii, security). Task 230 is a correctness bug (an updated install
     must reflect the current version) — laned here, not deferred.
     **✅ SHIPPED CONTENT (2026-07-17): Task 231 (#293, D-337+D-338) + Task 230 (#294, D-343) + Task 96
     (#295, ADR-0022 compliance scrub) + Task 210 (#296, D-308 deletion propagation).**
     **✅ PUBLISHED 2026-07-18: `@lh8ppl/core-memory-kit@0.5.5` (npm latest, provenance) + GitHub
     Release, publish.yml fully green — including SonarCloud (the D-341 crash was SOLVED en route:
     D-350 overturned the server-side conclusion; the trigger was our own test fixtures, fixed PR #299).**
     **Backlog sweep verdict (D-349):** Task 95 (dream re-curation) — its SEQUENCING gate CLEARED
     (66 shipped, learn-loop Phase 1–2 shipped, ADR-0017 closed the umbrella) but its TWO design forks
     (review-gate-vs-auto-apply + raw-transcript privacy) are UNRESOLVED → **kept DEFERRED with a named
     grill-trigger** ("both forks decided in a grill session"), NOT auto-built on autopilot (a genuine
     design fork, not a mechanical build). **v0.5.5 CUTS on the four shipped governance tasks** — exactly
     what the lane's "Task 95 *if* its gate cleared" wording anticipated.
  6. **v0.6.0 — DAY-ONE MEMORY (the next minor differentiator; D-326, 2026-07-12 — the user's
     "actual versions, not just future versions" call after the market sweep).** Headline:
     **Task 225** (`cmk import-sessions` — bootstrap the memory from existing agent session
     history; install offers it; every imported write screened through 216 + the L3 judge;
     resumable per ADR-0020). Riders on the same recall/corpus surface: **Task 226**
     (context-expansion + the explicit recall ladder) · **Task 227** (recall citation
     completeness — date+heading on every result, §16.39 cites closed) · **Task 229**
     (memory-taxonomy vocabulary in the docs) · **the recall-research trio 149 + 176 + 178** —
     their D-253a trigger ("run WITH 149+176 at the next recall-surface minor") FIRES here;
     an ADOPT verdict from 178 lands as a small swap in this lane or the next patch.
     _Provenance: the [2026-07-12 market sweep](research/2026-07-12-day-one-memory-market-sweep.md)
     — the origin creator's v3 proved the demand ("day one your memory already knows
     everything") and Hermes's 211k-star run proved memory is THE switching reason (~30% of
     1,300 analyzed comments)._
  7. **v0.7.0 — THE TEAM LAYER (D-326).** Headline: **Task 127** (team-scoped memory — its own
     trigger fired: the v0.6 planning boundary arrived AND the market demand signal landed, the
     origin creator's TeamOS beta; design-first per D-119 — seam shape / ACL / companion-vs-in-kit
     decided before code). Rider: **Task 228** (proactive-memory connectors GO/NO-GO ADR — the
     OpenWiki Brains class; the in-theme cut = other-agents' session stores as 225 import
     sources; external SaaS connectors decided deliberately, likely NO).
  _Task 208 (the Cursor interactive gate) is a gate RUN, not a release — it fires on token refresh
  (~2026-07-24) and rides whatever slot is current when it runs. Per D-157 the minor DIGITS above
  are "expected" — the order + content are the commitment; a ready security-patch may re-stamp._
- **v0.5.1 — COMMITTED (not trigger-gated): Tasks 203 + 204 — the automatic-distill-starvation fix + the incremental-resumable principle (D-298/D-299, the user's 2026-07-08 call).** Diagnosed live during the v0.5.0 repack: `daily-distill` silently stops on a busy repo (the 23:00 cron is killed before the 3.4-min distill finishes) AND both safety nets miss it — HC-10 is false-green (heartbeat records at task start), and the lazy fallback is shadowed by `stale-now` (cascade-starvation). **This BREAKS the kit's core "memory just works, automatically" promise, so it is committed to the next minor after v0.5.0, NOT deferred behind a trigger** (the user: *"what named trigger? this task has to be done in the next version or now, it breaks the kit"*). **203** = the fix (HC-10 checks `recent.md` freshness not just the heartbeat; the lazy roll stops letting `stale-now` shadow `stale-daily`; `register-crons` sets `WakeToRun=True`). **204** = the general principle it exposes (long jobs must be incremental + resumable-from-artifacts, never all-or-nothing) → a NEW ADR + `daily-distill` refactored as the reference beneficiary + the checklist into CLAUDE.md. They ship together. Not a v0.5.0 tag blocker (pre-existing; distill works on demand; `recent.md` fresh now).
  - **+ Task 213 (D-308, 2026-07-10 research sweep): provenance pointers through the distill chain** — every condensed section in `today-*.md`/`recent.md`/`archive.md` keeps a source-session date tag, so summaries stay traceable to the raw transcript floor (the Always-On survey's provenance-preservation invariant; softens lifecycle-map G8). RIDES 203/204 because they refactor the exact distill code this touches — the resumable rewrite is the natural place to add the invariant.
  - **+ Task 205 (D-302): `cmk install` breaks itself on its own running MCP server's DLL lock (Windows).** Found when the v0.5.0 repack needed a manual kill of the `cmk mcp serve` procs (they hold `vec0.dll`) — the user: *"that is a bug, no?"* Yes: a reinstall/upgrade while an MCP server is running can leave a half-broken global; documented-never-fixed (only "kill the server manually"). Fix = graceful handling (detect+stop own server / retry-on-EBUSY / actionable half-install message). NOT a tag blocker (usually cosmetic, workaround known, hits reinstall not first-install). NOT the rejected node:sqlite cure (Task 141b, perf).
- **Riding non-headline (per the laned-versions-carry-riders pattern):** **96** (compliance scrub —
  the governance slot, expected **v0.5.4** per the D-309 queue) · **95** (dream re-curation — CONSUMES
  the loop's utility data; sequenced after 66 (v0.4.4) + Phase 1; its needs-design gate stands —
  expected v0.5.4 IF the gate clears, else it slides) · **210** (deletion-propagation
  guarantee — a tombstoned/redacted fact verifiably gone from index/summaries/snapshot; rides 96's
  governance slot, expected **v0.5.4**; the 2026-07-10 sweep's cheapest differentiator — rollback/deletion is the field's
  27-of-435 gap and the kit already owns the hard parts; D-308).
- **Phase 3 is NOT v0.5** — host-dependent ambitions (peer-disagreement, used-vs-ignored, richer
  judges, same-task replay) ride the agent-host arc (Task 50 tail / 127) and are triggered by it.

_Sequencing (SHARPENED 2026-07-02, the user's Option A — D-257): **v0.4.4 (66+150+141a) →
v0.4.5 Cursor → v0.4.6 Codex → THEN v0.5.0 opens** (the two committed breadth agents first; real
demand — the maintainer's friends use both; the name decision, Task 195, re-fires when they're live).
66's validity windows are input the loop's staleness handling composes with. Per one-differentiator-per-minor: v0.3 recall · v0.4 breadth (Kiro) · **v0.5 depth (the
loop)** — the roadmap's original depth-lane promise, now with a designed system behind it._
- **🌱 FUTURE-DIRECTION SEED — U-Mem's escalation cascade (2026-07-01, [U-Mem note](../docs/research/2026-07-01-umem-autonomous-memory.md), D-251).** When 127 (or any agent-facing / non-interactive kit consumer) is scoped, revisit U-Mem's cost-aware acquisition cascade (arXiv:2602.22406, NUS): an autonomous agent that hits a knowledge gap ESCALATES — self-reflection → stronger teacher model → tool-verified research → human last-resort — and SAVES the verified result to memory so the next agent doesn't re-solve it (they retain 99.2% of expert quality invoking the human on only 23% of cases). This is N/A for the single-user-at-a-keyboard IDE kit (we record what the session produced; we don't autonomously acquire external knowledge) but ON-THEME the moment the consumer is an AUTONOMOUS agent rather than a human driving an IDE. **Named trigger (D-248):** revisit when the kit gains a non-interactive / autonomous-agent consumer (the 127 companion, or a headless agent mode). The single-user CUT of this idea — "escalate to the USER as last resort, then capture the resolution" — is already filed as the near-term Task 188 (it needs no agent-facing future). _Relates Task 188 (the single-user cut, near-term), the U-Mem note, D-251, D-119 (the companion design this seeds)._

---

## Notes

- **This file is authoritative for the version LANE.** A task's full detail + checkbox state lives in [`specs/tasks.md`](../specs/tasks.md); its narrative in the journey log; its version assignment here.
- **F-D and 84c were deferred from v0.2.0** (2026-06-03): F-D is the v0.3 semantic-contradiction engine (didn't break recall; 84b covers the summary layer); 84c is cosmetic placeholder text. Both are zero-ripple leaves — nothing depends on them.
- When a new finding/task appears, assign it a lane HERE in the same batch, so version decisions stop being ad-hoc.
- **Backlog-hygiene audit 2026-06-07** (the user: *"are all tasks slotted to a planned version, or are we lugging around problems?"*): all v0.2.3 findings (Tasks 108–117, D-81→D-85) are slotted; nearly every other open task already had a lane. Two orphans were tied off: **41.4** (`test-quickstart.sh` — stale vs Task 62 node-only; retire-or-reshape at v0.2.x) and **76.2** (lessons-promote `--tier U` decision — folded into Task 108/v0.2.3). **94.5** (manual cold-open re-test) is not an orphan — it IS the in-progress 2026-06-07 cut-gate. No silently-unslotted open tasks remain.
