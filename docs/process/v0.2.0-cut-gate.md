# v0.2.0 — full test + cut gate (test every feature, then ship)

**The single guide to run before tagging v0.2.0.** It exercises *every* kit
feature end-to-end on the **real installed artifact**: install + scaffold
integrity, the Task-69 skill, the staged build with organic capture, explicit
capture probes, recall + recall-*quality*, the cross-project cold-open (the
wedge), the full `cmk` CLI surface, the plugin route, privacy, and portability —
then the tag-push.

> This supersedes the two older guides. [`v0.2.0-self-test-guide.md`](v0.2.0-self-test-guide.md)
> (FastAPI feature sweep) and [`v0.2.0-self-test-guide-pdf.md`](v0.2.0-self-test-guide-pdf.md)
> (de-biased recall-quality run on a Docling stack) are kept only as references —
> their checks are folded in below.

**Already green (don't re-do by hand):** `npm run live-test` drives the
deterministic half headlessly on the real tarball (install, safe skill, capture,
persona-fill, explicit probes, cold-open). Trail: [`../journey/live-test-runs/`](../journey/live-test-runs/).
What you add by hand: **R1** (console flash), **R2** (prompt UX), and the
**recall feel** — the things automation can't see.

The cut gate is the **★ checks**. The rest is the full feature sweep — run it so
nothing ships untested.

~60–75 min. **Prereq:** Python 3.12+ on PATH.

---

## 0. Clean slate + build from main (the real artifact)

```powershell
cd C:\Projects\claude-memory-kit
git checkout main; git pull                       # must include Task 69 (ca71f4d+)

cd C:\Projects\claude-memory-kit\packages\cli
npm pack                                           # → lh8ppl-claude-memory-kit-0.2.0.tgz
npm uninstall -g @lh8ppl/claude-memory-kit
npm install -g .\lh8ppl-claude-memory-kit-0.2.0.tgz
cmk --version                                      # ✅ 0.2.0

# wipe the user tier for an honest capture-from-zero (back it up first if you care)
Remove-Item -Recurse -Force $env:USERPROFILE\.claude-memory-kit
```

- [ ] **G0** `cmk --version` → `0.2.0`

---

## 1. Scaffold + READ every file (scaffold integrity + the Task-69 surface)

```powershell
mkdir C:\Temp\cut-gate; cd C:\Temp\cut-gate
git init; cmk install; cmk doctor
code .
```

- [ ] **★ G1** `cmk install` → "ready, hooks wired"; `cmk doctor` → **0 fail** (`6 pass · 0 fail · 3 skip` without memsearch). Type `/hooks` → the 5 `cmk-*` hooks loaded.
- [ ] **★ G2 — the skill scaffolded + SAFE (Task 69).** Open `.claude\skills\memory-write\SKILL.md`: `allowed-tools: Bash(cmk remember *) Bash(cmk forget *) Read` (**no `Edit`/`Write`**); the **"NEVER hand-edit `context/memory/`"** gate; no `packages/cli/src` paths.
- [ ] **★ G3 — CLAUDE.md is slim (Task 69).** The "Memory write rules" block is a few invariants + a pointer to the skill, NOT the old multi-step procedure.
- [ ] **G4 — scaffold reads clean (Task 82).** `type context\MEMORY.md`, `SOUL.md`, `context.local\machine-paths.md` — no kit-internal cruft (no `Task 12`, `design §16.16`, dev-speak), no literal `{{TODAY}}`, no `C:\Users\<you>` username. `.claude\settings.json` has the hooks + `Bash(cmk:*)` allow-list.

---

## 2. Session 1 — build it, stating preferences (R1/R2 + capture)

Build a small real thing across **one** session. Each stage pairs a **Build**
prompt with a **Say it out loud** preference (a real opinion, stated naturally —
**never** "remember this"). End each turn normally (the Stop hook fires auto-extract). Do
not start a new session between stages.

**Stage 0 — baseline.**
- *Build:* "Create a minimal Python web chat UI: a FastAPI server with a WebSocket endpoint and a single static `index.html`. Plain HTML/JS, no framework. Put the server in `app.py`." → "yes, run it" if offered.

**Stage 1 — refactor to layers.**
- *Build:* "Refactor this into a layered FastAPI project — `app/{api,services,repositories,schemas,core}/` and `app/main.py`. WebSocket route into `api/`, connection/broadcast logic into a service, Pydantic schemas. Keep it on port 8000."
- *Say:* "How I build backends: FastAPI is the delivery layer, not the brain. Routes stay thin and orchestrate; logic lives in services; data access in boring repositories; Pydantic schemas are the boundary contracts. I'd rather pay the structure cost now than fight it in six months."

**Stage 2 — swap to Claude + typing/TDD rule.**
- *Build:* "Change it to a single-user chat with Claude via the Claude Agent SDK (`claude-agent-sdk`) — a `ClaudeAgentService` wrapping `ClaudeSDKClient`, each WebSocket connection its own session."
- *Say:* "Type hints on every signature — Python 3.12+. Comments explain why, not what. And tests first: boundary test, watch it fail, then implement."

**Stage 3 — stream + async rule + the universal rule (B4).**
- *Build:* "Stream Claude's output to the browser as it arrives — push JSON frames over the WebSocket; the client appends to the live bubble."
- *Say:* "Async all the way down — nothing blocking in the event loop." **Then state one cross-project rule:** "From now on, in **every** project I work on, always use `uv` for packages, never `pip`, and always run `ruff` before committing."

**Watch while you build:**
- [ ] **★ R1 — no console flash (Task 81).** At every SessionStart, NO black `node` window flashes to the foreground. (Visual.)
- [ ] **★ R2 — no `cmk` permission prompt (Task 79).** When the agent runs an explicit `cmk remember`, Claude Code does NOT ask "Allow this bash command?".
- [ ] **★ G5 — the explicit-skill security check (Task 69).** Mid-build, say: *"remember this: my local cache is at C:\Users\<you>\cache\app."* Verify the agent **ran `cmk remember`** (not a hand-edit of a memory file), and the committed `context\MEMORY.md` / fact file shows `~\cache\app`, **never your username**. (A hand-edit or a leaked username = blocker.)

---

## 3. Capture checks (B1–B4) — read the files

```powershell
cmk search "layered"; cmk search "type hints"; cmk search "port 8000"
type context\MEMORY.md
dir context\memory; type context\memory\feedback_*.md
```

- [ ] **B1 — auto-capture fires.** Your decisions/prefs (layered, Claude SDK, type hints, async, port 8000) show up **without** "remember this".
- [ ] **★ B2 — F1 rich capture.** Durable preference facts are **rich fact files** (frontmatter + `**Why:**` + `**How to apply:**`), not bare one-liners.
- [ ] **★ B3 — the wedge fills.** `type %USERPROFILE%\.claude-memory-kit\HABITS.md` (+ USER/LESSONS) → your cross-project style is there (was empty pre-v0.2).
- [ ] **★ B4 — stated rule → `trust: high`, auto.** The uv/ruff rule landed in a user-tier scratchpad on its own (no command), provenance `trust: high` + `write: user-explicit`:
  ```powershell
  findstr /S /C:"trust: high" %USERPROFILE%\.claude-memory-kit\*.md
  ```

---

## 4. Explicit capture probes (C1–C4) — run in the build terminal

- [ ] **C1 — terse:** `cmk remember "We deploy with Kamal to Hetzner, never Vercel."` → appears in `context\MEMORY.md`.
- [ ] **C2 — rich (F1):** `cmk remember "Reflection beats one-shot generation" --type feedback --title "reflection-loop" --why "iterative critique catches errors a single pass misses" --how "generator then critic then route; cap iterations"` → a rich `context\memory\feedback_reflection-loop.md` with Why/How.
- [ ] **C3 — Poison_Guard:** `cmk remember "key sk-ant-api03-AAArealishlooking..."` → **rejected** (exit 2), nothing written anywhere.
- [ ] **C4 — sanitization:** `cmk remember "venv at C:\Users\<you>\proj\.venv"` → the file shows `~\…`, **never** your username.

---

## 5. Session 2 — recall + recall-QUALITY ⬅️ start a NEW session

Without re-explaining anything, ask: *"What are my standing cross-project rules,
and how is this project structured?"* then *"Add a `/health` endpoint."*

- [ ] **★ D1 — recall.** It names your rules (uv/ruff/type-hints/layered) + the structure (port 8000, layered, Claude SDK) **without a re-brief**.
- [ ] **D2 — style follow-through.** `/health` lands as a **thin route** in `api/`, **type-hinted**, without being re-told your style.
- [ ] **★ D3 — recall FEEL (the cut judgment).** Watch *how* it answers your **rules**: lead with memory, or **glob/read the code** to re-derive? The harness found this ~**50/50** — the known **Task-75 active-recall gap** (v0.3). The cold-open (§6) is the reliable wedge; in-project recall is the part that varies. **Decide:** ship with this, or want Task 75 first? (Rec: ship.)
- [ ] **R2-quality — no hallucinated summary (Task 84).** `type context\sessions\recent.md` and `today-*.md` → the rolling summary describes only what the session contained (FastAPI, layered, Claude SDK, port 8000). **FAIL signals:** an invented framework (Flask/Django), wrong SDK, or a port the project never had.
- [ ] **R3-quality — config recall, not a code-read (Task 80).** `cmk search "port 8000"` returns a hit (env/config captured), and the "what port" answer doesn't require globbing `**/*.py`.
- [ ] **D4 — stale-thread baseline (Task 68, watch).** `context\MEMORY.md` "Active Threads" — are finished threads still listed as active? Does Claude treat resolved work as pending? (Baseline for the v0.2.x pruning fix.)

---

## 6. Session 3 — the cold-open (the wedge, wow #1) — a BRAND-NEW project

```powershell
mkdir C:\Temp\cut-gate-coldopen; cd C:\Temp\cut-gate-coldopen
git init; cmk install; code .
```
Ask: *"Start a new Python backend for me — set up the structure."*

- [ ] **★ E1 — cold-open.** It scaffolds the **layered** shape + `uv`/`ruff` tooling **without being told** — because the user-tier persona (filled in Session 1) injected. *"How does it know that?"* = the wedge. **This is the gate that matters most.**

---

## 7. FULL FEATURE SWEEP — every `cmk` subcommand (~15 min, in `C:\Temp\cut-gate`)

**Recall & index**
- [ ] **F-1** `cmk search "<kw>"` finds facts · `cmk reindex` rebuilds, search still works · `cmk view` inspects memory/audit.

**Cross-project persona**
- [ ] **F-2** `cmk persona generate` → runs synthesis; candidates promote or land in `~\.claude-memory-kit\queues\persona-review.md`.
- [ ] **★ F-3 — explicit promote (Task 76):** `cmk lessons promote <id>` (get an id from `cmk search`) → moves a project fact to the user tier at **`trust: high`** via the safe path; verify the bullet in `~\.claude-memory-kit\LESSONS.md`; try `--to HABITS.md`.

**Lifecycle & self-curation**
- [ ] **F-4** `cmk daily-distill` → consolidates daily summaries · `cmk weekly-curate` → rolls `today-*.md` >7d into `archive.md`.
- [ ] **F-5** `cmk roll` / `cmk compress` → session rotation/compression (no error on your OS).
- [ ] **F-6** `cmk register-crons` → registers host-scheduler jobs (then `cmk doctor` HC-6 passes); confirm no error.

**Memory management**
- [ ] **F-7** `cmk forget <id>` → tombstones (moved to `archive\tombstones`, still resolvable — NOT hard-deleted).
- [ ] **F-8** `cmk trust <id> <high|medium|low>` → changes a fact's trust.
- [ ] **F-9** `cmk queue review` / `cmk queue conflicts` → walk pending medium-trust extracts / conflicts.
- [ ] **F-10** `cmk purge --hard <id>` → the explicit destructive path (on a throwaway fact).

**Health & repair**
- [ ] **F-11** `cmk doctor` → HC-1..HC-9 accurate · `cmk repair --hooks` re-wires if settings drift · `cmk config get/set --show-origin` resolves across tiers.

**Native coexistence & import**
- [ ] **F-12** `cmk disable-native-memory` → writes committable `autoMemoryEnabled:false` (doctor HC-8 reflects it) · `cmk enable-native-memory` reverses.
- [ ] **F-13** `cmk import-anthropic-memory` → imports existing native Auto Memory files into the kit schema.

**MCP + transcripts**
- [ ] **F-14** `cmk mcp serve` → stdio MCP server starts (Ctrl-C to stop); `mk_remember`/`mk_get`/`mk_search` respond (and report `queued` correctly, not a false `accepted`).
- [ ] **F-15** `cmk transcripts extract <context\transcripts\*.md>` → pulls durable facts out of a captured transcript.

---

## 8. Lighter scenarios (~10 min)

- [ ] **L1 — plugin route (route B).** In a *second* empty folder, inside Claude Code: `/plugin marketplace add LH8PPL/claude-memory-kit` → `/plugin install claude-memory-kit` → `/claude-memory-kit:bootstrap` → `/reload-plugins`. Scaffolds `context/`, hooks active. (Local variant if the marketplace lags: `claude --plugin-dir <repo>\plugin`.)
- [ ] **L2 — "remember this".** Say *"Remember this: the API base URL is `https://api.example.com`."* → `cmk search "api.example.com"` finds it.
- [ ] **★ L3 — privacy tag.** Say *"`<private>` my license key is ABC-123 `</private>` — now add a README."* → `context\MEMORY.md` does **NOT** contain `ABC-123`.

---

## 9. Portability ("another computer")

In `C:\Temp\cut-gate`: `git add -A; git commit -m "wip"`. Clone elsewhere
(`git clone C:\Temp\cut-gate C:\Temp\cut-gate-clone`), open *that* in Claude Code.
- [ ] **★ H1** the clone already has the project memory (`context/` is committed — tenet T2).

---

## Verdict + the cut

**Cut if** every **★** passes — `G1–G3, G5, R1, R2, B2, B3, B4, D1, E1, F-3, L3, H1` —
and you've decided D3's recall variance is acceptable for v0.2.0 (rec: yes, active
recall is v0.3). A clean full sweep (F-1..F-15 + L1–L2) = nothing ships untested.

**To publish (your outward action):**
```powershell
git tag v0.2.0
git push origin v0.2.0
```
`publish.yml` runs the suite, publishes `@lh8ppl/claude-memory-kit@0.2.0` to npm
with provenance, and creates the GitHub Release from the `[0.2.0]` CHANGELOG section.

**Verify after:** `npm view @lh8ppl/claude-memory-kit version` → `0.2.0`; the npm
page shows a **provenance** badge; the GitHub Release matches `## [0.2.0]`.

Per-finding notes go in a dated doc under [`../journey/`](../journey/), not here —
this stays a clean script.
