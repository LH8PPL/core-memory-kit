# Research notes — index

Outputs from research sessions. Each file is a dated, self-contained markdown report — either a manual survey we conducted, a Deep Research run from Claude.ai or ChatGPT, or notes on a paper/article.

## Conventions

- Filename: `{YYYY-MM-DD}-{topic-slug}.md`
- Frontmatter: required (`date`, `topic`, `source`, `tags`).
- Body: structured with clear headings; raw research output retained verbatim where useful.
- Citations: full URLs inline, not bare references.

## Index

| Date | Topic | Source | Status |
| --- | --- | --- | --- |
| 2026-05-21 | [claude-mem architecture survey](2026-05-21-claude-mem-architecture.md) | Manual survey via gh CLI + WebFetch | Complete |
| 2026-05-21 | [claude-remember architecture survey](2026-05-21-claude-remember-architecture.md) | Manual survey via WebFetch | Complete |
| 2026-05-21 | [Anthropic Memory tool documentation notes](2026-05-21-anthropic-memory-tool.md) | Manual fetch of platform.claude.com docs | Complete |
| 2026-05-21 | [Claude.ai Deep Research — Option B (targeted)](2026-05-21-claude-ai-deep-research-option-b.md) | Claude.ai Deep Research mode | Complete |
| 2026-05-22 | [ChatGPT Deep Research — Option A (broad landscape)](2026-05-22-chatgpt-deep-research-option-a.md) | ChatGPT Deep Research mode | Complete |
| 2026-05-22 | [Anthropic Claude Code auto-memory](2026-05-22-anthropic-claude-code-auto-memory.md) | Manual survey | Complete |
| 2026-05-22 | [Claude Code leak — architecture](2026-05-22-claude-code-leak-architecture.md) | Manual survey | Complete |
| 2026-05-22 | [Primary-source examination](2026-05-22-primary-source-examination.md) | Verification pass | Complete |
| 2026-05-22 | [ChatGPT bibliography](2026-05-22-chatgpt-bibliography.md) · [Claude.ai bibliography](2026-05-22-claude-ai-bibliography.md) | Deep Research bibliographies | Complete |
| 2026-05-23 | [Cold-start bootstrap A/B test](2026-05-23-bootstrap-test.md) | In-session doc-transfer experiment | Complete |
| 2026-05-24 | [Beyond the log — time-aware memory](2026-05-24-beyond-the-log-time-aware-memory.md) | Paper/article notes | Complete |
| 2026-05-24 | [GBrain architecture](2026-05-24-gbrain-architecture.md) | Manual survey | Complete |
| 2026-05-24 | [OpenClaw templates](2026-05-24-openclaw-templates.md) | Manual survey | Complete |
| 2026-05-24 | [TencentDB agent memory](2026-05-24-tencentdb-agent-memory.md) | Paper notes | Complete |
| 2026-05-25 | [claude-remember code dive](2026-05-25-claude-remember-code-dive.md) | Source code read | Complete |
| 2026-05-26 | [Claude Code memory guide — verification](2026-05-26-claude-code-memory-guide-verification.md) | Verification pass | Complete |
| 2026-05-29 | [claude-mem install model](2026-05-29-claude-mem-install-model.md) | Manual survey | Complete |
| 2026-05-30 | [gstack skill layer](2026-05-30-gstack-skill-layer.md) | Manual survey | Complete |
| 2026-06-01 | [memory lifecycle + competitive position + Layer 5 deep-research brief](2026-06-01-memory-lifecycle-and-competitive-position.md) | Synthesis + brief | Complete |
| 2026-06-01 | [how researched products implement skills (survey → Task 69.0)](2026-06-01-how-products-implement-skills.md) | Survey (gstack/claude-mem/antigravity) | Complete |
| 2026-06-01 | [deep dive: product memory implementations (source-level) + things we don't do](2026-06-01-deep-dive-product-memory-implementations.md) | Source dive (cloned repos) | Complete |
| 2026-06-04 | [Anthropic Managed-Agents Memory Stores + Dreams — validates D-61, what to steal (Tasks 95/96)](2026-06-04-anthropic-managed-agents-memory-and-dreams.md) | Primary-source fetch (platform.claude.com) | Complete |
| 2026-06-04 | [memory-os (ClaudioDrews) review — authoritative-memory instruction (Task 75) + dynamic trust (Task 97)](2026-06-04-memory-os-review.md) | WebFetch (github) | Complete |
| 2026-06-05 | [MemPalace review — verbatim+vector recall; steal the hybrid pipeline (Task 65) + temporal model (Task 66) + a benchmark (Task 99)](2026-06-05-mempalace-review.md) | WebFetch (github README) | Complete |
| 2026-06-06 | [Competitive research brief — steal / research-the-how / re-visit, for recall+temporal+trust (D-71)](2026-06-06-competitive-recall-research-brief.md) | Synthesis (planning doc) | Brief / in-progress |
| 2026-06-06 | [Source dive: memsearch + MemPalace — the HOW (hybrid recipe + RRF k=60 + keyword/temporal weights + temporal-graph schema + benchmark) (D-72)](2026-06-06-recall-deep-dive-memsearch-mempalace.md) | Cloned + read the code | Complete |
| 2026-06-06 | [Source dive: Graphiti + mem0 + memory-os — bi-temporal, ADD/UPDATE/DELETE memory-manager, the authoritative-memory Ground-Truth wording + trust decay (D-73)](2026-06-06-recall-deep-dive-graphiti-mem0-memoryos.md) | Cloned + read the code | Complete |
| 2026-06-06 | [Native Auto Memory coexistence investigation — variance not regression; the whole field captures via the Stop hook (immune); the fix is enrich auto-extract (D-74, Task 103)](2026-06-06-native-auto-memory-coexistence-investigation.md) | Live cut-gate A/B + 10 competitor READMEs + Anthropic primary source | Complete |
| 2026-06-03 | [Hermes dedicated memory/persona review pass + its trigger cadence — primary-source re-verification answering the Task 86 wedge bug (multi-rule turns don't promote)](2026-06-03-hermes-dedicated-review-primary-source.md) | Primary-source re-verification | Complete |
| 2026-06-07 | [Dual-surface CLI+MCP architecture — how products share ONE core + handle input + keep parity (source research for Task 108 / ADR-0014)](2026-06-07-dual-surface-cli-mcp-architecture.md) | Source-level research | Complete |
| 2026-06-08 | [Claude Code MCP install-config — verified findings for Task 108b.6 / R2 / D-80](2026-06-08-claude-code-mcp-install-config.md) | Primary-source verification | Complete |
| 2026-06-12 | [Stellman's ERR pattern — detect + recover from context collapse](2026-06-12-stellman-err-pattern-context-collapse.md) | Article/pattern notes | Complete |
| 2026-06-10 | [Simon Scrapes v2 — "Best Claude Memory System (Beats Hermes)"; same creator who set the video-parity bar; reviewed same day as the v0.3.0 recall build](2026-06-10-simon-scrapes-v2-best-memory-system.md) | YouTube transcript + slide captures | Complete |
| 2026-06-12 | [ruflo (ruvnet) — counter-positioning reference + one transferable idea (post-v0.3.0 review sweep; Task 55 trajectory-memory input)](2026-06-12-ruflo-counter-positioning.md) | README-level review (github) | Complete |
| 2026-06-12 | [caura-memclaw — governed fleet memory; the strongest Task-127 prior art yet (PostgreSQL+pgvector, multi-tenant, trust tiers)](2026-06-12-memclaw-fleet-memory.md) | README-level review (github) | Complete |
| 2026-06-12 | [Personal AI Infrastructure (Miessler) — the paradigm's highest-profile ally + two Task-55 inputs (RELATIONSHIP memory + LEARN-phase retrospectives; Pulse dashboard → v0.4 viewer)](2026-06-12-pai-miessler-paradigm-ally.md) | README-level review (github) | Complete |
| 2026-06-14 | [Recall TRIGGERING across systems — always-search vs judgment-pulled vs inject-everything; we shipped only the hard half of memsearch's model (missing the hint→skill link); skill triggering is semantic-intent not keyword (D-153, Task 149)](2026-06-14-recall-triggering-models-cross-system.md) | Cloned + read 9 repos + Anthropic Agent-Skills docs + skill-creator | Complete |
| 2026-06-15 | [project-memory (SpillwaveSolutions) — the Claude skill the user USED BEFORE building this kit (the origin artifact); 4 hand-maintained flat files (bugs/decisions/key_facts/issues). Every kit feature reads as a deliberate fix for a limitation here. Yields: the error→fix Issue/Root-Cause/Solution/Prevention schema (Task 55, 2nd source), Poison_Guard validates their manual "never store secrets" warning, AGENTS.md cross-tool interop (Task 50)](2026-06-15-project-memory-skill-predecessor.md) | Cloned + read the skill | Complete |
| 2026-06-15 | [AWS Bedrock AgentCore Memory + 4 AWS-build-event slides — the MANAGED-CLOUD version of the kit's exact thesis (auto-extract "strategies" = our auto-extract; fact/summary/preference records; semantic namespace retrieval; typed markdown folders). Strong validation + a positioning line ("AgentCore, but local + git-native"). One signal: error→fix as a first-class memory category → Task 55 design input](2026-06-15-aws-agentcore-memory-and-build-slides.md) | AWS primary docs + the user's build-event photos | Complete |
| 2026-06-15 | [OKF (Open Knowledge Format, Google Cloud) — STRONGEST convergence of the batch: markdown+frontmatter, git-shippable, concept-per-file, index.md + newest-first log.md, untyped prose-links — independently arrived at the kit's ADR-0002 design. Actionable as an INTEROP/export target for the team (Task 127) + cross-agent (Task 50) lanes; NOT a format to adopt-instead-of-ours](2026-06-15-okf-open-knowledge-format-interop.md) | Read the OKF v0.1 SPEC directly | Complete |
| 2026-06-15 | [agent-infra/sandbox — ORTHOGONAL: ephemeral agent EXECUTION container (browser/shell/file/MCP/VSCode), no memory/recall; nothing to borrow. One positioning insight ("the sandbox is ephemeral; the kit is the state") + a multi-harness data point](2026-06-15-agent-infra-sandbox-orthogonal.md) | Metadata + MCP-surface check (not cloned — execution sandbox, not memory) | Complete |
| 2026-06-15 | [Node.js worker_threads (freeCodeCamp) — applicability NEGATIVE result: wrong tool for the kit's short-lived-CLI + stdio-MCP + already-async/out-of-process model; the real concurrency surface (Task 146) is multi-PROCESS SQLite, not in-process threads](2026-06-15-nodejs-worker-threads-applicability.md) | Article assessed vs our execution model | Complete |
| 2026-06-15 | [Qdrant (vector DB) — assessed vs ADR-0015; RE-REJECTED as the server-vector-DB class (collides with D-23 no-server + ADR-0002 markdown-is-truth + lean-install); HNSW/quantization non-actionable at our bounded corpus scale; no new evidence to re-open](2026-06-15-qdrant-vector-db-assessment.md) | Metadata + settled-ADR assessment (not cloned — mismatch decisive) | Complete |
| 2026-06-15 | [OpenHands — context CONDENSER taxonomy as prior art for our compression layer (design §8); 4 un-adopted ideas: keep_first / minimum_progress / HARD-vs-SOFT trigger / shrink-and-retry; NOT a cross-session memory system](2026-06-15-openhands-condenser-context-management.md) | Cloned (sparse) the platform + software-agent-sdk | Complete |
| 2026-06-15 | [claude-task-master (Taskmaster) — cross-IDE PROFILE/ADAPTER pattern as Task-50 prior art (createProfile factory + rule-transformer + per-agent lifecycle hooks); incl. real Kiro paths to verify; NOT a memory system](2026-06-15-claude-task-master-cross-ide-profiles.md) | Cloned + read the JS source | Complete |
| 2026-06-15 | [TencentDB Agent Memory — CODE-LEVEL re-dive (write + search); search fully converged (hybrid RRF k=60), their batch LLM store/update/merge/skip over a unified candidate pool is the concrete blueprint for F-D / Task 95 / Task 151](2026-06-15-tencentdb-agent-memory-code-dive.md) | Cloned + read the TS source (supersedes the 05-24 README survey) | Complete |
| 2026-06-15 | [FTS5 query preparation across systems — SQLite spec §3 (primary) + basic-memory's `_prepare_search_term`; grounded Task 153's per-token quoting + flagged the node:sqlite-FTS5 gate for Task 141b](2026-06-15-fts5-query-preparation-cross-system.md) | FTS5 spec + basic-memory source | Complete |
| 2026-06-14 | [Persona PROMOTION across systems — every real system AI-judges promotion (auto), none use a human-gated review queue; our signal is backwards (form not frequency/recurrence); drop the queue, AI-judge by recurrence + post-hoc revert (D-154, Task 151)](2026-06-14-persona-promotion-models-cross-system.md) | Cloned + read 9 repos (mem0/MemoryOS/Letta/langmem/graphiti/…) + ChatGPT memory + articles | Complete |
| 2026-06-16 | [Index-uniqueness root cause for Bug 1 (`reindex --full` UNIQUE crash) — file-keyed DELETE vs id-keyed PK; 3 independent markdown-first analogs converge (TencentDB id-keyed upsert / basic-memory partial-unique-index + resolve_permalink disambiguation / memweave content-hash dedup) + SQLite UPSERT canon; fix = id-keyed upsert with archive-beats-scratchpad precedence, NOT composite PK (v0.3.3)](2026-06-16-index-uniqueness-id-vs-file-scoped-delete.md) | Cloned + read 4 repos' store/index code + memweave article + web pass | Complete |
| 2026-06-18 | [How squad keeps `decisions.md` CURRENT (auto-maintenance) — inbox drop-box + an LLM "Scribe" agent at a session-end ceremony; the kit should take squad's session-end TIMING but reject its LLM mechanism (the kit's typed-fact journal is deterministically derivable → cheap no-LLM `syncDecisionsJournal` on a hook). Source for Task 159 / D-169 auto-sync](2026-06-18-squad-decision-journal-auto-maintenance.md) | Cloned + read squad's skill files (kept at /c/tmp/squad-dive) | Complete |
| 2026-06-18 | [Session-buffer compaction under LATENCY + GROWTH — 19-system fresh-clone survey for Task 161 (the compress-session timeout spiral); 17/19 BOUND the LLM input before the call, the 2 that don't are the kit's own precedents (claude-remember unbounded@120s + pre-fix claude-mem); fix = bound the input (cap/partial-evict/mid-session-cap/shrink-retry), NOT raise the timeout. D-173 / design §8.2.5. NOTE: D-174 later INVERTED this — the kit's failure is environmental, not size-driven](2026-06-18-session-buffer-compaction-under-latency-growth.md) | Fresh clones (19 repos) + kit dogfood compress.log data | Complete |
| 2026-06-19 | [LLM-call RETRY patterns across the field — for Task 161's retry fix (after D-174 inverted the size-bound to a retry); read 9 systems' real retry code. Convergent: bounded 2–4 attempts, exponential backoff, retry ONLY transient (rate-limit/5xx/timeout/empty) keyed on error TYPE, NEVER deterministic (4xx/context-length/policy/ENOENT), reraise after exhaustion. Decisive negative: claude-remember (kit precedent) does NOT retry — the kit inherited the gap. D-175](2026-06-19-llm-call-retry-patterns-cross-system.md) | Fresh-clone code reads (9 systems' retry paths) | Complete |
| 2026-06-20 | [Cross-agent ADAPTER SEAM for Task 50 (v0.4.0, Kiro first) — full-corpus survey (66 notes) confirms NO researched product solved core-identical-plus-thin-per-agent-wiring cleanly: claude-mem has the breadth but bespoke code, Taskmaster the cleanest profile model, rest are single-agent/connector/instruction-only/negative-example (Tencent). Seam = a shared tested `mutateAgentConfig` primitive + per-agent metadata as DATA not classes. KIRO CORRECTION (primary-verified): target CLI agent-hooks (`agentSpawn`/`stop` in `.kiro/agents/<name>.json`), NOT the IDE Agent-Hooks surface; the `.kiro/hooks/*.kiro.hook` claim was the wrong system. Top risk: Kiro transcript format UNVERIFIED (kit hardcodes Claude-Code touchpoints). D-157 gate output. **NOTE: this note's Kiro conclusions were REVISED by the 2026-06-20 Kiro deep-research note below** — see it for the corrected IDE-vs-CLI model + the default-agent finding.](2026-06-20-cross-agent-adapter-seam-task50.md) | Corpus survey (66 notes) + cloned adapter source (claude-mem/taskmaster/opencode/roo/continue) + kiro.dev primary pass | Complete |
| 2026-06-20 | [Kiro AUTOMATIC MEMORY deep-research (Task 50 v0.4.0) — how 3 REAL Kiro memory systems achieve auto-memory. Ground truth: CLI hooks live in an agent-config JSON + auto-fire ONLY when that agent is `chat.defaultAgent` (AgentCore sets it = automatic; PILOT/langfuse don't = manual `--agent`). Custom `steering/` is DROPPED under a custom agent → use AGENTS.md (always-included). Kiro needs its OWN installer branch (agent-config + default-agent file write), not a thin profile. IDE hooks are file-installable (`.kiro/hooks/*.kiro.hook`, `when`/`then`/`runCommand` — verified from a real GUI-created hook) + auto-fire `agentStop` but no installer-writable inject. Fixes the 3 defects in the shipped Kiro profile. D-181 follow-up. **Superseded on the install-path question by the 2026-06-21 note below (14-project survey + authoritative Rust contract).**](2026-06-20-kiro-automatic-memory-deep-research.md) | 3 real impls (AWS bash-hooks/AgentCore/langfuse) + cloned aws-samples + kiro.dev IDE-vs-CLI primary pass + a real user-created .kiro.hook | Complete |
| 2026-06-21 | [Kiro install-path SETTLED (Task 50 v0.4.0, D-182) — 14-REAL-PROJECT survey + the AUTHORITATIVE Amazon-Q Rust hook contract (Kiro CLI = Amazon Q Developer CLI; the published `agent-v1.json` is a STALE subset). Decisions: (1) CLI agent-config `hooks{}` is the dominant pattern (9/14, 3/4 memory repos) + the capture SPINE; (2) IDE `.kiro.hook` DISQUALIFIED for capture (0 repos use `runCommand` — all `askAgent`/LLM-prompt, + IDE-only) → optional convenience only; (3) set `chat.defaultAgent`/`q_cli_default` for automatic activation (5/14 did, GUARD non-clobber); (4) inject=`agentSpawn` capture=`stop`, deterministic `runCommand`→one `cmk hook <event>` dispatcher. Rust contract: 5 triggers, `timeout_ms`(default 30000), camelCase. Full installer spec + 8-point live-test checklist](2026-06-21-kiro-install-path-settled.md) | 14 real .kiro project clones (Auriti/kiro-memory, mempalace, oh-my-kiro, vibekit, semio, DesignerPunk, naas, coder/registry, …) + aws/amazon-q-developer-cli Rust source | Complete |
| 2026-06-25 | [Cron-liveness + lazy-compaction prior art (Task 167 / D-206) — internal + external deep-research on "a scheduled compaction didn't run, self-heal with no manual command." CONVERGENT invariant (logrotate / anacron / Postgres-autovacuum / SQLite-WAL / claude-mem / Letta / mem0): gate on a PERSISTED last-success timestamp, NEVER on "scheduler registered"; make lazy-on-access PRIMARY, cron an optimization. Our bug = the named anti-pattern "monitoring detects presence not absence." Internal: WE invented it (claude-remember has no sentinel gate); the liveness pattern already exists in `lock-discipline.mjs`, never applied to crons; D-179 is the same-week sibling. Scheduler catch-up table (cron=no, anacron/systemd-Persistent/launchd/Windows-StartWhenAvailable=opt-in, all COALESCE). Drives Task 167.A/D/E + the 3 sibling-bug sub-tasks. **Marker-vs-derive RESOLVED to a HYBRID** (make §4.8 rule: derive when the work's product carries the signal — now/daily/weekly from artifact mtime, NO marker; stamp ONLY cron-liveness — one anacron-style heartbeat, age-not-existence). **Peer-code sweep VALIDATES it: OpenWolf** (new find, the only peer with the kit's scheduled-job-needs-liveness architecture) independently implements the same two-part design (`last_heartbeat` by-age + `statSync().mtimeMs` derive); claude-mem/mem0/Letta/Graphiti are event-synchronous (no cron) → confirm the lazy path must be an always-available floor](2026-06-25-cron-liveness-lazy-compaction-prior-art.md) | Internal corpus sweep + external web deep-research (man-pages, MS/Apple docs, logrotate/anacron) + full compaction-code audit + a focused marker-vs-derive pass + a wide peer-CODE clone sweep (claude-mem/mem0/Letta/Graphiti from our collection + OpenWolf new) | Complete |
| 2026-06-25 | [EverOS (EverMind-AI) — the markdown-first memory RUNTIME, SAME thesis OPPOSITE architecture (server + cloud API keys vs the kit's no-server local CLI/hooks). AHEAD of the kit: semantic search SHIPPED (LanceDB+rerank, we defer it ADR-0015/Task 65), a more principled reflection (Select→Merge→Re-extract→Deprecate with frontmatter `deprecated_by` — steal for Task 151), orthogonal retrieval (user/agent/app/project/session). KIT AHEAD on the decisive fork: EverOS REQUIRES a running server + OpenRouter/DeepInfra keys ("no in-process library mode"); the kit is zero-server, no-key-needed, installs INTO the agent via hooks (D-23). Different PRODUCT CLASS (mem0/Zep/AgentCore runtime vs claude-mem/OpenWolf agent-companion). Task 167 lesson: EverOS schedules via in-process APScheduler — the 3rd peer confirming "everyone who schedules reliably has a persistent process", which the kit lacks → validates the lazy-fallback+heartbeat. ADR-0002 validated AGAIN by convergence. Apache-2.0 (license-clean to learn from)](2026-06-25-everos-markdown-memory-runtime.md) | Cloned + read 732 files / 33K LOC Python (memory/reflection/cascade/search + QUICKSTART server model) | Complete |
| 2026-06-29 | [Curation-cluster CODE study (Task 151 + 97/95/179/180) — 7 systems read at code level (MemoryOS/mem0/letta/langmem/memclaw/graphiti/MemOS). Headline: every system at scale converged on never-delete+status-flip; recurrence-CAPPED earns promotion; trust moves on outcome events not a clock. **Verdicts:** promotion = capped recurrence (MemOS `min(count·w,2)` + MemoryOS lazy recency), NOT pure-LLM (mem0 ABANDONED its judge for hash-dedup) NOT trained-trust; eviction = DEMOTE-NOT-EVICT (letta condense-on-cap + graphiti/memclaw never-delete; MemoryOS-LFU/MemOS-top-N value-blind sweeps ARE the Task-151 bug); trust = event-driven `+0.1/−0.15` floor `0.05` from PASSIVE signals (memclaw), two-fields-not-one (recurrence gates promotion, trust gates protection). Feeds design.md §20 + ADR-0016 + the Task 151 redesign. D-228](2026-06-29-curation-cluster-code-study.md) | Cloned + code-read 7 repos (promotion/trust/re-curation/loop/skill/eviction mechanisms, functions+formulas+prompts quoted); 8-agent parallel workflow | Complete |
| 2026-07-01 | [U-Mem "Towards Autonomous Memory Agents" (arXiv 2602.22406, NUS) — 10 general ideas + our harvest (9 landing spots). Headline: their cold-start/Thompson-sampling contribution VALIDATES our SETTLED design §20.3 (we already chose NOT to rank retrieval by the evolving `trust_score`, for the exact reason U-Mem exists to fix). Two NEW out-of-the-box ideas surfaced: **recurrence = memory ROI** (r=0.888 — a measure-your-own-value differentiator) + **can't self-diagnose a failure without a correct reference** (reframes user-correction as the highest-value capture signal). Cascade = future seed on Task 127 (agent-facing). Enriches Tasks 55/66 + the G5 dedup line. D-251](2026-07-01-umem-autonomous-memory.md) | Primary-source PDF read (full text + appendix prompts) + arXiv abstract cross-verify | Complete |
| 2026-07-01 | [Failure-learning field survey (47 systems) — who learns from failure, oracle-free or not. Corrects a convenience-sample first pass. Verdict: ~14 learn from failure but only ~4 ORACLE-FREE (memclaw/Memoria/A-MemGuard/SkillOpt-fallback); the "inert utility socket" (letta/MemOS/A-Mem + the kit's own trust_score) is a field-wide anti-pattern; ExpeL gates SURVIVAL not just rank. Expanded the signal portfolio with peer-disagreement, blame-attribution, dead-end-veto, negative-exemplar-retention. Two axes: oracle-vs-no-oracle ≠ automatic-vs-human. D-251](2026-07-01-failure-learning-field-survey.md) | Two-wave multi-agent survey (enumerate→triage→deep-read→synthesize) | Complete |
| 2026-07-01 | [Earned comparative judgment — can memory KNOW "method A > method B" oracle-free? (10 skill-memory systems + 4 outside lenses). Headline: earning verified A>B is STRUCTURALLY UNSOLVED at single-user/single-arm/non-replayable/low-n scale (the kit's regime) — 4 lenses converge (single-arm, scale-floor, silent-success asymmetry, self-judge circularity); ZERO of 10 systems earn it oracle-free from one non-repeatable trajectory. ONE ready wedge = propose-provisional + pre-register-expectation + prediction-error; the scale tension IS the quantitative case for the agent-host arc. D-251](2026-07-01-comparative-judgment-earned-method-preference.md) | Cohort mine + 4-lens study (preference-learning / cognitive-science / bandits / epistemics) | Complete |
| 2026-07-02 | [Contradiction-detection bake-off on the kit's OWN 1,246-fact corpus (Task 66.2/66.4) — three read-only experiments + a live-Haiku judge. Headline: whole-text lexical pairing is DEAD (zero true contradictions at any threshold; the only 0.4+ pairs are RESTATEMENTS = the 151 recurrence signal); the real class is state-progression chains (v0.3.2: 18 facts "scope locked"→"published", within-chain Jaccard 0.00–0.21 — subject-shared, word-disjoint); a ONE-batched-call Haiku judge scores 10/10 twice at $0.004/10-pairs (SUPERSEDES/DUPLICATE/COEXIST). Settles the design: search-retrieved candidates → queue → weekly batched judge → event-time window-close; state_key DERIVED not declared (D-169 class); D-221 tension-holding CLOSED negative (zero genuine simultaneous disagreements). FTS5 caveat: version tokens shred (`v0.3.2`→`[v0,3,2]`) — OR-query construction is the build detail.](2026-07-02-contradiction-detection-bakeoff-real-corpus.md) | Scratchpad scripts on the real corpus + live `HaikuViaAnthropicApi` runs + a live `cmk search` probe | Complete |
| 2026-07-02 | [Expiry/TTL precedents (Task 66.3 reference set) — who sets `expires_at`, what fires at expiry, where enforced, across mem0 (Platform API + OSS cookbook) / LangGraph store TTL / graphiti / letta / the kit. Headline: caller-set is the ONLY precedented writer (no system lets the LLM set expiry); at expiry the field converges on HIDE-from-retrieval-never-delete (mem0 `show_expired`, graphiti view-filter — matches our D-163 tombstone posture); enforcement = read-time filter + background sweep, and LangGraph's no-sweep-configured=nothing-expires trap is why 66.3 needs the read-time half. Two distinct semantics named: declared-validity-end (66.3) vs staleness-TTL (`refresh_on_read` — our 14-day drop already covers it). Also corrected the assistant's in-chat "mem0 auto-removes" claim (hidden, NOT removed).](2026-07-02-expiry-ttl-precedents-mem0-langgraph.md) | Primary-source checks (docs.mem0.ai API reference + cookbook, LangGraph TTL how-to) + in-base deep-reads | Complete |
| 2026-07-03 | [Memora (Microsoft, ICML 2026) — harmonic memory: embed ONLY the 6–8-word primary abstraction (the rich value never indexed), write-time judged consolidation (θ=0.80 candidates → one LLM call judges+merges; App. F: 0.6 → 3.4× merges NO gain, ratio flat 16.5–22.2% as the store grows), cue anchors (1–3 × 2–4 words, factual + RELATIONAL) forming an ontology-free implicit graph, REFINE/EXPAND/STOP policy retrieval (advantage vanishes without cues). LoCoMo 0.863 / LongMemEval 87.4% — BEATS full context at ~98% fewer tokens (the kit's thesis, ICML-validated); raw>extracted episodic grounding re-validates D-44; their future work (MemLoop/Deferred/Group) = the kit's v0.5 roadmap (third convergence). Drives D-266 (the sweep-cadence re-decision → Tasks 198/199) + folds into 149/176/178/95/190.](2026-07-03-memora-harmonic-memory-review.md) | Full 30-page paper (pdftotext, all appendices) + cloned code read (builder/cue-generator/expander/store) + MSR blog | Complete |
| 2026-07-04 | [Cursor memory landscape (Task 196 companion) — Cursor's native Memories feature was REMOVED in 2.1.x (rules are the only built-in persistence → NO coexistence problem on Cursor + a real auto-capture gap the kit fills; one watch-item trigger: re-check if Cursor re-ships native memory); "dynamic context discovery" (2026-01-06 blog) independently validates the kit's files-first thesis; mimir + memex are the visible competitor class on Cursor and both are MCP-ONLY (judgment-gated recall — the 2026-06-14 recall-triggering failure mode) vs the kit's hook-wired deterministic inject/capture; memex bitemporal edges converge with Task 66, mimir recall-reinforcement noted against the D-253 line.](2026-07-04-cursor-memory-landscape.md) | Manual survey (cursor.com + forum.cursor.com + GitHub repos via WebFetch/WebSearch; the user's wiki article captures) | Complete |
| 2026-07-04 | [Agent-relative LLM backend (Task 200 / D-270) — the kit's automatic engine shells out to the `claude` binary, so a Cursor-only/Kiro-only user has NO working engine; route the "Haiku call" through the AGENT'S OWN CLI instead. **Kiro LIVE-CONFIRMED:** `kiro-cli chat --no-interactive --model claude-haiku-4.5` works as a one-shot (0.01 credits / ~1 s, the user's Google login, NO Claude/API-key), answer clean on STDOUT (TUI noise → stderr; strip ANSI + `> `). **The recursion hazard is REAL (reproduced live** — `kiro-cli` fires the kit's own `agentSpawn` hook → timeout storm**); guard = env-var `CMK_BACKEND_SPAWN` at the dispatcher entry → inner hooks no-op (env-inheritance verified). **Cursor: doc-confirmed feasible** (`cursor-agent -p --output-format json`, `CURSOR_API_KEY`) but the exact flags need a live `agent --help` probe in the build (not installed on the dev machine). Design sketch: a CompressorBackend per agent + selection by installed-agent + degradation-surfacing doctor HC + agent-parametric live-test harness.](2026-07-04-agent-relative-llm-backend.md) | LIVE probes on the installed kiro-cli (primary) + cursor.com/docs/cli (doc-based for Cursor) + compressor.mjs / hook-dispatcher code read | Complete |
| 2026-07-05 | [Cross-agent headless-LLM backend survey (Task 200 / D-271) — the DEEP survey the user mandated: 41 projects code-read across 5 multi-agent waves (~7M tokens) out of a 119-project manifest. **Headline:** two properties matter — HARD-automatic (deterministic hook, not model-discretion) + PORTABLE (any agent CLI) — and almost nobody holds both (no-LLM/MCP 23, agent-SDK 8, single-CLI 6, cloud-key 4). **Multi-CLI routing = 1 of 41 (codemem)** — the lone prior-art reference, VALIDATES Task 200's approach (auto-detect env → shell whichever agent CLI is authed → API-key fallback; native Windows; a pure selector fn), covers claude+codex (not the kit's kiro/cursor). **Cursor-on-Windows RESOLVED** (my 2-search "no Windows" was WRONG; user live-verified `agent -p --trust --model composer-2.5-fast` on their box, off subscription, NO key). Novel mechanisms the OPEN trace surfaced (the user's don't-lead-the-question catch): claude-mem's persistent async-generator Observer agent, GBrain's extraction-as-side-effect-of-a-generic-write, MemClaw's passive outcome-inference-from-exhaust, PreCompact-as-checkpoint, mem0's chat-SDK MITM (+ its ADD/UPDATE/DELETE is now DEAD CODE). Recommendation: two-tier agent-relative backend (CLI first, cloud-key opt-in) + warn-and-file-only degrade. Drives D-273/274/275/276/277 + Task 201 (split-brain backend).](2026-07-05-cross-agent-llm-backend-survey.md) | 5-wave multi-agent code-read (clone+code+docs, Sonnet-5) + user live-verification of cursor-agent on real Windows | Complete |
| 2026-07-07 | [Auto-judged privacy prior art (Task 148 / ADR-0019) — three-wave sweep after the v0.5.0 cold-open captured the maintainer's real name+email into a would-be-committed transcript (148's own re-verdict trigger fired at the cut). **Headline: NO prior art for LLM-judged sensitivity routing between committed/gitignored tiers** — the feature is genuinely novel; mem0 moved BACKWARDS (its new prompt calls personal/health details "the MOST valuable"); the whole field persists raw conversation verbatim. **Closest: caura-memclaw** — a real 2-detector PII pipeline (deterministic regex+checksum ALWAYS inline, masks in place PRE-hash; LLM contains_pii signal inline-or-deferred by write mode; findings carry category+offsets never text) but quarantines by metadata/visibility in the SAME store (tier-routing stays ours) and has NO false-positive release path (our redactions-log fills a field gap). **hermes:** ONE shared threat-pattern lib with scoped subsets across 3 lifecycle boundaries + load-time re-scan defense-in-depth + `[BLOCKED]`-in-snapshot-only-original-preserved (its `privacy.redact_pii` is just messaging-ID hashing, NOT PII screening); no LLM tier anywhere in hermes. **Anthropic primary sources:** Memory Stores' first-party `redact` op, Dreams' read-raw-write-NEW-reviewable pattern, and the official PII-purifier prompt (full-redacted-text output, obfuscation defense) = the judge's foundation. Landscape: JS libs regex-only (names admittedly broken), NER domain-brittle (GLiNER 0.81→0.41 on health text), LLM-judgment the only high-recall option for names; gitleaks = zero PII detectors but the regex discipline to steal (keyword pre-filter, entropy gates); Memoria's "quarantine" is a hard DELETE (the naming pitfall); Honcho = staging→async-derive template sans judge; A-MemGuard validates isolate-don't-delete + the two-stage judge shape. Drives ADR-0019 + design §6.10.](2026-07-07-auto-judged-privacy-prior-art.md) | Three-wave agent research (10 sonnet runs): targeted deep-reads + full-corpus triage (SOURCES.md + all ~78 notes) + code-level shortlist reads; clones under C:/tmp/cmk-research-clones/ | Complete |
| 2026-07-10 | [Five-source memory-research sweep — A-TMA (state-labeled recall + rule-based query state-view → Tasks 209/211), Always-On-Agents survey (deletion-propagation + provenance-through-consolidation + source-trust tiers → Tasks 210/213 + 70.5; ADR-0002 validated: extracted-fact stores score WORSE on governance than raw storage), AutoMem (behavioral health metrics → Task 212; meta-LLM scaffold self-audit → Task 95), stash (curator patterns → 95/199 refs; positioning), 6G-latent-memory (nothing transfers — white-box + training). MEMORA re-read confirmed the 2026-07-03 disposals; the full-research-base check prevented re-filing cue anchors.](2026-07-10-memory-research-sweep.md) | Per-source deep-read subagents (full PDFs incl. appendices + repo sources), mapped against the open backlog (D-308) | Complete |
| 2026-07-11 | [Faithful live-test automation — closing the unit-green/integration-broken gap so the manual cut-gate finds less; the Google small/medium/large taxonomy explains WHY hermetic tests miss boundary bugs (BOM/path/cron/env); technique-per-bug-class table + the prioritized playbook (fixture corpus → property-based → watch-the-watchmen → fault-inject kill-mid-run → OS matrix → broaden live-verify); drives Task 221 + the CLAUDE.md cut-gate-parity rule](2026-07-11-faithful-live-test-automation.md) | Web research (Kent C. Dodds, Google SE book, testscript, fast-check, PagerDuty, GitHub Actions) synthesized against the kit's bug history (D-313) | Complete |
| 2026-07-12 | [Day-one-memory market sweep — three sources published in one 48h window (the origin creator's v3 Hermes-rebuild video, LangChain's OpenWiki Brains, the MLM memory-taxonomy tree), gap-mapped against the kit. Headline: Hermes 0→211k ⭐ / ~30% of switchers cite memory = the strongest external validation of the thesis yet, near-total architectural convergence (down to `context/memory/` + frozen-snapshot semantics); the kit is AHEAD on screening/trust/tiering/learn-loop and BEHIND on exactly four things → Task 225 (bootstrap import of existing session history — the v0.6.0 differentiator), 226 (context-expansion + recall ladder), 227 (citation completeness), 229 (taxonomy vocabulary in docs); Task 127's team-layer trigger FIRED (TeamOS beta) → v0.7.0; OpenWiki's reactive-vs-proactive framing + their "Claude/Codex local sessions" connector roadmap → Task 228 (GO/NO-GO ADR). D-326.](2026-07-12-day-one-memory-market-sweep.md) | Full-read of both clipped articles + the complete youtube-to-slide capture (transcript, 20 slides + OCR, the free build plan), then a code-level gap check (search/semantic-backend/import-anthropic/38b) before filing tasks | Complete |

| 2026-07-12 | [Codex adapter surfaces (Task 196 tail) — the June-20 "plugin-marketplace, out of scope" classification is OBSOLETE: Codex shipped a first-class hooks system (`.codex/hooks.json`, events SessionStart/UserPromptSubmit/Pre+PostToolUse/Stop, Claude-compatible responses — `hookSpecificOutput.additionalContext`, `permissionDecision: deny`, `tool_name: Bash`) making it a hooks-json+MCP agent, patch-sized on the Task-50 seam. Hash-based hook trust = one-time `/hooks` review (the honest-docs item); MCP writes via live-verified `codex mcp add` (no TOML surgery; project config.toml is trusted-only with open Desktop issues); AGENTS.md = the existing 50.G leg; transcripts `~/.codex/sessions/YYYY/MM/DD/rollout-*.jsonl` + `transcript_path` on hook stdin; `codex exec` = the Task-200 backend leg. Dev machine runs the desktop app's bundled codex-cli 0.142.5 (absolute path, real auth) → CLI-side legs live-testable now; the interactive gate is the user's manual step. D-327.](2026-07-12-codex-adapter-surfaces.md) | Primary-source docs (developers.openai.com/codex → learn.chatgpt.com/docs) + live probes on the installed codex-cli 0.142.5 (`--version`, `mcp add --help`, `exec --help`) | Complete |
| 2026-07-19 | [Graph-memory code sweep (Task 176) — 9 systems read at code level (graphiti/mem0/cognee/MemOS/MemoryOS/iwe/sift-kg/pulse8/Memora) + the kit-fit analysis. Field honesty: mem0's clone has NO graph store, MemOS's relational path is commented out, Letta ships no graph. Kit facts: ~5% of facts carry `related:` (auto-extract writes zero edges), the edges are invisible (not indexed, not in mk_get, targets not FTS-searchable — probed); of 6 real relational query shapes, hybrid+ladder answers 4; the 2 graph-only = supersession chains + backlinks. → ADR-0023: ADOPT the activation slice (Task 232), DEFER Memora-cue derivation behind a relational-qtype benchmark trigger, REJECT graph-DB/typed-KG/hand-authored. D-361.](2026-07-19-graph-memory-code-sweep.md) | Cloned repos read at code depth + first-hand kit code/corpus probes | Complete |
| 2026-07-19 | [Letta (MemGPT lineage) deep-read — the user's "isn't this what we're trying to do?" answered: same thesis, different layer. Letta IS the runtime (server-owned DB memory, self-editing in-context blocks, zero write screening — null-byte strip only); the kit plugs into existing agents with repo-owned screened markdown. Convergences: RRF hybrid recall, sleep-time agents ≈ auto-extract + the Task-95 sibling (watermark + curator-persona division-of-labor), and Letta's newest tier projects blocks to markdown+YAML in a git repo — server-side. 3 borrowables: the memory_metadata existence-advertisement block, read_only-as-schema, the sleep-time tool-budget split. 149 input: judgment-pulled with scaffolding (doctrine + tool-strategy + existence advertisement — the kit lacks only the third), and the lineage is SHRINKING its pulled surface (archival tools deprecated) → argues hybrid. D-362.](2026-07-19-letta-deep-read.md) | Cloned letta-ai/letta @ b76da90, code read | Complete |
| 2026-07-19 | [Recall-trigger architecture study (Task 149) — 15 systems code-verified + 4 closed products docs-level. The tally: judgment-pulled 7 / always-search 5 / inject-everything 1 / position-hybrid 3(+kit). The load-bearing pattern: the recall model follows WHO OWNS THE LOOP (loop-owners pipeline; MCP servers are structurally judgment-pulled; every Claude-Code-plugin peer converges on the kit's hybrid, and the one true per-prompt semantic inject — claude-mem's — ships DEFAULT-OFF "experimental"). Memora nuance: it validates the LADDER, not the trigger (retrieval is unconditional). → ADR-0024: keep judgment-pulled as the deep spine; upgrade the static per-prompt hint to a gated FTS5 pointer injection; add Letta's existence advertisement; adopt the in-skill Memora refinements. 9 borrowables with file:line. D-362.](2026-07-19-recall-trigger-architecture-study.md) | Direct code reads of the clones + primary web sources + the closed-products docs track | Complete |
| 2026-07-20 | [Prior-art sweep: the open backlog vs the whole research corpus — the process fix after Task 174 shipped having missed a source in our own `docs/sources/`. Corpus is the NOTES (~101 projects), not the 14 locally-cloned repos I first searched (14%). **5 findings likely to change what we build:** 184 (basic-memory already solved multi-project navigation — our own 2026-05-22 note said so, and 184 was drafted 2026-07-01 without it) · 67 (9-system survey: NOT ONE gates promotion behind a manual signal, so the `--cross-project` flag lean may be the rejected minority approach) · 146 (the WAL/concurrency harness already exists; also the probe that tells whether 238's trigger fired) · 177 (pro-workflow/src/optimizer already cloned, unread) · 223 (the privacy note PREDICTED its cut-gate failure 12 days early and argues against an L1 regex pass). 3 corpus gaps: self-healing CLI repair UX, command-obfuscation detection, npm native-dep migration. Process change: sweep the whole backlog at each minor and annotate findings ONTO the task entries. D-374.](2026-07-20-prior-art-sweep-backlog-vs-corpus.md) | Indexed 95 research + 6 source notes + 24 ADRs, matched against 34 open tasks | Complete |
| 2026-07-20 | [ECC (`affaan-m/ecc`) v2.0.0 comparison — the user's "how does this stack up to our kit?" answered at code level. Verdict: DIFFERENT CATEGORY, not a memory rival. ECC is a harness OS (278 skills / 67 agents / 94 commands / ~12 harnesses / 230 contributors / paid Pro tier); memory is its least-invested subsystem. Their memory = LLM-written markdown session summaries in a HOME-DIR GLOBAL dir (`~/.claude/session-data/*.tmp`, machine-local, never committed, project-matched only at read time, 30-day prune, ≤8KB injected) + per-project instinct YAML. Scorecard: ZERO of 6 memory primitives present (no fact files, no FTS/embeddings — searched `fts5|MATCH|bm25|embedding|cosine`, no citations, no dedup, no conflict detection, no tombstones, no evolving trust). Their `state.db` is a control-plane store (its `decisions` table is session-CASCADE-scoped and written by release tooling); `ecc2`'s `graph recall` is a substring scan NOT wired to any hook. Their documented "confidence decreases on user correction" is UNIMPLEMENTED (`instinct-cli.py` reads confidence 55×, writes it 0×). Borrows (all non-memory): the stale-replay guard (P-RYN7KWJJ), a PreCompact hook, catalog/registry CI gates, supply-chain-watch, agent-proximity collision avoidance. Includes a logged self-correction (§6): a first-pass claim that their pipeline = shipped Task 95 was made from a diagram, not code — wrong; the re-curation half is unbuilt there too. D-364.](2026-07-20-ecc-harness-os-comparison.md) | Shallow clone @ v2.0.0 read at code level (D-153) + first-hand kit-side probes | Complete |
| 2026-07-18 | [Task-95 design-input synthesis — the full 11-source base re-read (D-349 review step 1). SETTLED by convergence: ADD/UPDATE/SUPERSEDE-never-DELETE; deterministic-floor→one-batched-LLM-pass→code-decides (event-time wins, hallucinated ids rejected); conservative merge (two independent over-consolidation warnings); read RAW not derivatives; the Sleep ordering invariants; provenance + deletion-propagation obligations; regression-gated output. F1 sharpened from a binary to an OP-CLASS SPLIT question (non-destructive ops auto under a screened envelope vs lossy/generative ops behind an adopt-or-discard diff); F2 evidence one-sided (extend the Task-216 screen + Task-70.5 source-trust tiers; no source supports unscreened raw). Grill-only remainder: the auto/queue line, v1 scope (fact-level vs +schema-audit), cadence.](2026-07-18-task-95-design-input-synthesis.md) | Dedicated extraction pass over all 10 recorded sources + the same-day Sleep note, then synthesis against the forks | Complete |
| 2026-07-19 | [Fusion benchmark (Task 178) — verdict KEEP-CURRENT, with numbers. The characterization settled the headline without a bench: the kit's hybrid IS textbook RRF k=60 (equal 0.5/0.5 weights preserve plain-RRF ranking; pro-workflow's `embeddings.ts:128` is the same formula). Measured alternatives on the Task-99 harness: RRF k=20/k=10, weighted-sum raw + min-max-normalized (captain-claw 0.65/0.35) all TIE at 0.941/1.000/0.860; 21-day temporal decay is ACTIVELY HARMFUL (paraphrase R@5 0.900→0.000 — age is the wrong penalty; supersession/validity-windows are the right one); Memora's title-only embedding LOSES on every metric (0.882/0.941/0.750 vs content 0.941/1.000/0.848 — rich facts don't fragment). Honest bound: 40-entry corpus; fusion is not the bottleneck at kit scale. D-360.](2026-07-19-fusion-benchmark.md) | Cloned pro-workflow + captain-claw code + a scratch experiment harness over the kit's real search()/embedder + the Task-99 corpus | Complete |
| 2026-07-18 | ["Language Models Need Sleep" (arXiv 2606.03979, Google/Cornell — the Hope/Nested-Learning line) — Task-95 research review, paper 1 (the user's annotated PDF). Wake/sleep lifecycle for continual learners; the kit already IS a file-based Continuum Memory System (tier cascade = the frequency spectrum). Transferable ordering invariants for 95: consolidate-BEFORE-the-fast-tier-rolls (the G8 spot), compute→consolidate→update = a structural argument for F1's review-gate (validate the transfer before applying; input preserved until confirmed), write-into-NEW-capacity (the D-61 no-loss invariant, parameter-space edition), more-consolidation-stages-help + fast-stable-tier-HURTS (2nd independent over-consolidation warning alongside Memora App. F), regression-gated self-modification (dreams kept only if they improve — the AutoMem gate's sibling). Parameter-level mechanics (LoRA experts, distillation, RL) deliberately do NOT transfer.](2026-07-18-sleep-paradigm-memory-consolidation.md) | Full 26-page read (pdftotext; annotations flattened, not extractable) mapped against the lifecycle map + Task 95's scope + ADR-0017 | Complete |

| 2026-07-13 | [SQLite reader-freshness — how a long-lived MCP server sees another process's writes (Task 218/D-329). 17-project field survey (claude-mem/basic-memory/official MCP memory server/mem0/graphiti/…) + SQLite primary sources (isolation/WAL/pragma data_version) + 3 empirical repros on the kit's own code. Headline: the staleness is NOT a WAL/watcher problem — indexing is lazy/reader-driven (the writer never commits to the db; a fresh connection still sees 0 until a reader reindexes). The field consensus is RE-READ/RE-DERIVE PER REQUEST (nobody keeps a long-lived reader + a watcher; watchers are only re-ingest triggers, always backstopped). Recommended: make the MCP reader run the per-query incremental reindexBoot every CLI read already does — deterministic, load-immune, the field's dominant answer. Overturned the chokidar-watcher approach (dropped events 5/5 under load).](2026-07-13-sqlite-reader-freshness.md) | Deep research: sqlite.org docs + better-sqlite3/chokidar issues + code-read of 17 memory/MCP projects + 3 repros | Complete |
| 2026-07-21 | [ChatGPT-Dreaming critique #1 — consolidation-without-controls; OpenAI primary 403 at read time (later settled — see the primary-source-verification note); the article's own Tenable citation found BROKEN (resolves to their homepage)](2026-07-21-chatgpt-dreaming-context-layer-gone.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [ChatGPT Dreaming "under the hood" — verdict: systems-vocabulary dressing on a marketing post; only the eval numbers trace to OpenAI (later primary-verified); the mechanism itself is undisclosed, per The Decoder's own admission](2026-07-21-chatgpt-new-memory-under-the-hood.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [The 3-layers article — surfaced the pass's load-bearing find: the "100 sessions / 200-line index / 4 phases" mechanism traces to an admittedly-UNOFFICIAL community repo (grandamenium/dream-skill), not Anthropic; the official blog confirms the feature exists (Managed Agents scope), not those internals](2026-07-21-three-layers-agent-memory-dreaming.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [OpenClaw's dreaming — unsourced (zero product/repo links exist in the article); serves as a naive-baseline contrast for §21, not a design source](2026-07-21-openclaw-dreaming-first-principles.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Anthropic-Dreaming press take — official-blog-confirmed existence + review-or-automate control surface; the granular numbers absent from the reachable primary](2026-07-21-anthropic-dreaming-learn-from-mistakes.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [The vendor-lock-in critique — Tier-1 verification: fetched platform.claude.com Dreams+Memory docs directly, POST /v1/dreams contract verified; the article's model whitelist already stale vs live docs (expected drift)](2026-07-21-anthropic-dreaming-markdown-rewriter-lockin.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [The "memory debt management" critique — Tier-1 primary-doc verification of its mechanism facts; the critique's target (opaque full-rewrite) is a shape §21 already rejects by design](2026-07-21-claude-dreaming-memory-debt-critique.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [The four-phase "Dreaming architecture" article — zero load-bearing links; its 6x/50% stats unverifiable; third-party sources disagree on release status](2026-07-21-scheduled-cross-session-curation-arch.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Dreaming-AI Part 1 (hobbyist build) — single self-reviewing-agent consolidation; its own self-critique is an argument for the kit's AUTO/QUEUE split](2026-07-21-dreaming-ai-part1.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Dreaming-AI Part 2 — the follow-on brain build; the manifest-file resumability mechanism claimed but unverifiable](2026-07-21-dreaming-ai-part2-built-a-brain.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Mnemo (hackathon MemoryAgent) — repo link is a literal REPO_URL placeholder; the ONE transferable new idea: a runtime pack-trust assessor gate + two-sided (unsafe-acceptance vs over-abstention) metric discipline](2026-07-21-mnemo-qwen-memoryagent.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [ZenBrain 7-layer article — paired arXiv is real (fetched, AI-summarized; benchmarks unverified); see the repo note for claims-vs-code](2026-07-21-zenbrain-7-layer-article.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [EverOS article — reflect_episodes is the closest §21 analog but auto-applies with NO review gate (diverges from the D-352 op-class split); their deprecated_by echoes our superseded_by](2026-07-21-everos-runtime-article.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Context-vs-memory engineering — the header IMAGE carried the densest content (a Poisoned-Memory-BLOCKED icon = independent Poison_Guard convergence); also the WebFetch-image-hallucination methodology find (never trust its description of an image; re-Read the cached binary)](2026-07-21-context-vs-memory-engineering.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [5 context-window strategies — uncited listicle; strategy #5 (backend escalation after N consecutive failures) is the one thin GENUINE gap vs §8.2.5; strategies #1/#3 are already-rejected shapes](2026-07-21-context-window-management-strategies.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [Memory-poisoning security paper (submission draft; N=10/cell) — Opus flags-but-does-not-delete (96.7% persistence) = empirical evidence FOR §21.2's QUEUE + §21.5's gates; stabilization sessions ENTRENCH payloads more often than remove them; all figures/tables read from the PDF; companion repo 403 (reachable in the user's browser)](2026-07-21-arxiv-2607-14611.md) | PDF read in full (all pages incl. figures/tables) + 3 reference links | Complete |
| 2026-07-21 | [Google "always-on memory agent" — code-verified takedown: no retrieval at all (SELECT last-50 by recency, silent unreachability past the cap), consolidation cannot compound BY CONSTRUCTION (consolidated rows excluded forever), hard-delete with no tombstone; the anti-§21](2026-07-21-google-always-on-consolidation.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [graphify — the pass's one REAL graph: persisted, traversed, deterministic AST-derived CODE graph (different domain than conversational facts — structurally aligned with what ADR-0023 ADOPTED, not what it rejected); dedup-guard patterns (entropy gate, variant-suffix, cross-file blocking) worth borrowing as §21 implementation references](2026-07-21-repo-graphify.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [headroom — fresh COST evidence for ADR-0023's rejection line: a silent graph-bypass integration surface](2026-07-21-repo-headroom.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [git-lrc — NOT a memory system at code level: "institutional memory" = one appended git-log status line; the SQLite table is a tally; "feedback loop" is an unverifiable proxy call to a hosted service; reviews require their authenticated API (not local)](2026-07-21-repo-git-lrc.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [claude-second-brain — community dream-adjacent build; hard-delete-on-contradiction (a rejected shape)](2026-07-21-repo-claude-second-brain.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [zenbrain claims-vs-code — NO real graph store (knowledge_entities has ZERO inserts anywhere; the PPR implementation is a pure function whose own docstring cites files that do not exist in the repo — extracted from a closed platform, bridge never shipped); consolidate() is unbatched per-episode LLM calls with NO idempotency guard (double-call duplicates facts); the FSRS spaced-repetition loop IS genuinely live](2026-07-21-repo-zenbrain.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [EverOS code-read (upgrades the paper-only SOURCES row) — hybrid BM25+vector confirmed; Reflection auto-applies (no review gate found; orchestrator internals unverifiable this pass); entry-level sha256-keyed selective re-embedding matches the kit's content-addressed embedder cache, split across two mechanisms](2026-07-21-repo-everos.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [MegaMemory — a bespoke merge engine built solely to pay down graph/branch drift: more cost evidence FOR ADR-0023's rejection](2026-07-21-repo-megamemory.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [obsidian-mind — capture-nudge signal catalog + validate-write hygiene checks; no Poison_Guard analog found (inferred, not exhaustive); .shardmind packaging skimmed only](2026-07-21-repo-obsidian-mind.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [COG second brain — a memory-hygiene budget worth remembering ("never spend more than ~1 minute per entry; unverifiable is not drifted"; propose-only for destructive actions) + the skill-distillation meta-pattern (explore-expensive-once then execute-cheap; cost claims unverifiable)](2026-07-21-repo-cog-second-brain.md) | Shallow clone read at code level (D-153) | Complete |
| 2026-07-21 | [graphiti delta — NOTHING-MATERIAL: one file changed (a CLA json); every 2026-07-19 claim re-verified at current HEAD, exact file:line matches](2026-07-21-delta-graphiti.md) | git pull + re-verification of the 2026-07-19 sweep's claims at current HEAD | Complete |
| 2026-07-21 | [cognee delta — CHANGED, and in OUR direction: a new Turso/libSQL graph backend storing the graph as TWO RELATIONAL TABLES with recursive CTEs (the ADR-0023-ADOPTED shape — not a graph server), plus an opt-in deterministic no-LLM enola code-graph task with graph-only retrieval](2026-07-21-delta-cognee.md) | git pull + re-verification of the 2026-07-19 sweep's claims at current HEAD | Complete |
| 2026-07-21 | [mem0 delta — NOTHING-MATERIAL: still zero graph store / zero relation-extraction code at current HEAD; the one new fact (issue-label retirement) reinforces the 2026-07-19 finding](2026-07-21-delta-mem0.md) | git pull + re-verification of the 2026-07-19 sweep's claims at current HEAD | Complete |
| 2026-07-21 | [Huawei openJiuwen "JiuwenMemory" — article-text-only (no repo linked anywhere; both legal-citation fetches failed); GraphMemory matches the already-rejected LLM-KG-as-store pattern (unverified in code); the piece's own legal-exposure argument is a real-world case FOR the kit's local-first/no-server posture (ADR-0002/D-64)](2026-07-21-huawei-agent-memory-framework.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [The TechTimes-class audit-trail article (id 317840 — the one two notes 403d on) AND the actual Tenable Nov-2025 report, finally fetched via its working link: Tenable explicitly CONTRADICTS the article's "separate data layer" framing (system-prompt-appended only); 30-day deletion + audit-trail limits confirmed; the injection surface now triple-corroborated (2024 ETR / Nov-2025 Tenable / this)](2026-07-21-chatgpt-dreaming-audit-trail.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [ChatGPT cross-conversation search (July 2026) — no primary linked; Glean's "1.9x" is vendor self-comparison (their own blog also reports 1.6x-vs-Claude, omitted by the article); ABA Journal confirms the 20M-log court order; comparison table vs the kit's own Task 38b/148 transcript search](2026-07-21-chatgpt-searchable-conversations.md) | Full local read incl. images; load-bearing links followed (<=5) | Complete |
| 2026-07-21 | [PRIMARY-SOURCE VERIFICATION over the user's locally-saved pages (OpenAI Dreaming + Memory FAQ; Anthropic Managed-Agents blog RAW + Dreams docs). SETTLES the pass's biggest open question: the "100-sessions/200-line/4-phase" numbers are ABSENT from both Anthropic primaries — the press laundered a community repo's invention (the only official "100" is the per-call 1-100 session cap, a different claim); ALL OpenAI eval numbers CONFIRMED verbatim (factual 41.5-67.9-82.8 / preference 31.4-55.3-71.3 / freshness 9.4-52.2-75.1, ~5x compute); Harvey ~6x is ON Anthropic's own blog (Reuters unnecessary); mechanism internals remain genuinely undisclosed](2026-07-21-primary-source-verification-dreaming.md) | The user's locally-saved primary pages read in full vs the pass's open-questions checklist | Complete |
| 2026-07-21 | [SYNTHESIS — dreaming corpus vs §21: the contract SURVIVES contact with Anthropic's shipped system (evidence tiers separated 1/2/3; §21's defenses map one-to-one onto the corpus's documented failure modes). 8 borrow candidates (capped --focus on the Stage-2 call · relative-to-absolute date normalization as a named AUTO op · a no-same-session-self-grading invariant in §21.4 · a stabilization-entrenchment pre-mortem test beside HC-12 · graphify's dedup guards · Mnemo's two-sided metric · Anthropic's condense-before-cap playbook as prose · redact-preserving-audit as a future compliance idea). Rejects re-confirmed: opaque full-rewrite, LLM-decided which-wins, hard-delete-on-contradiction, unscreened raw ingest, the Tier-3 numbers as citable facts](2026-07-21-synthesis-dreaming-vs-s21.md) | Synthesis over the full pass (relevant notes read whole, not digests) | Complete |
| 2026-07-21 | [SYNTHESIS — graph: VERDICT **CONFIRM ADR-0023**. No source clears the re-open bar (the DEFER trigger's gate is a relational-qtype benchmark score, and nobody supplies one); headroom + MegaMemory add fresh COST evidence for the rejection; graphify's real graph is a different domain aligned with the ADOPTED deterministic shape; cognee's new Turso backend likewise (graph-in-relational-tables). Sweep-time citation for the next D-267 fired-trigger walk](2026-07-21-synthesis-graph-vs-adr0023.md) | Synthesis over the full pass (relevant notes read whole, not digests) | Complete |
| 2026-07-21 | [SYNTHESIS — context engineering: mostly ALREADY-HAVE, with the kit AHEAD of every source on input-bounding (§8.2.5; none of them bound consolidation input bytes) and PreCompact (no analog anywhere in the corpus); ONE thin genuine gap (backend escalation after N consecutive compress failures — a §8.3 seam annotation, deliberately weighted down: uncited listicle evidence); REJECTs confirmed: transcript-wipe/JSON-scratchpad-only persistence, pure sliding-window truncation](2026-07-21-synthesis-context-engineering.md) | Synthesis over the full pass (relevant notes read whole, not digests) | Complete |
| 2026-07-22 | [OKF (Google's Open Knowledge Format) v0.1 — spec verified at primary source; near-isomorphic to the kit's context/ tier (markdown+frontmatter, index.md/log.md, links-as-graph, agents-as-librarians); the Medium article hype-graded (U-2AD4YBKD class); OKF's link-graph is a THIRD independent arrival at ADR-0023's deterministic shape; spawned Task 251 (OKF interop, deferred)](2026-07-22-okf-google-open-knowledge-format.md) | Direct fetch of github.com/GoogleCloudPlatform/knowledge-catalog + article read | Complete |
| 2026-07-22 | [Octopoda-OS code dive — the rare inverse of README-inflation: 10 real deterministic loop classifiers behind a "five-signal" headline (all CODE-VERIFIED, but zero tests + never-measured accuracy); reflection.py's false-positive suppressor is the top Task-250 input; classifiers are pure functions runnable over OUR logs (Task 212); retry/nondeterminism need a tool-call log the kit doesn't write; audit chain real but self-anchored; server-shaped runtime = ignore](2026-07-22-octopoda-os-code-dive.md) | Shallow clone + code read (v3.3.5), opus agent | Complete |

### Competitor spec stacks (raw research inputs — requirements / design / tasks as written by each system)

These are external specs captured verbatim for comparison; they use their own FR/Task namespaces (not the kit's) and are excluded from the kit's reference validator.

- **ChatGPT**: [requirements](chatgpt-requirements.md) · [design](chatgpt-design.md) · [tasks](chatgpt-tasks.md)
- **Cursor**: [requirements](cursor-requirements.md) · [design](cursor-design.md) · [tasks](cursor-tasks.md)
- **Kiro**: [requirements](kiro-requirements.md) · [design](kiro-design.md) · [tasks](kiro-tasks.md)
- **Google Antigravity**: [requirements](google-antigravity-requirements.md) · [design](google-antigravity-design.md) · [tasks](google-antigravity-tasks.md)

## How research feeds into decisions

1. Research is conducted (manual or Deep Research mode).
2. Output is saved here with frontmatter.
3. Findings are summarized into either:
   - A new ADR ([adr/](../adr/)) if the research drives a decision.
   - A revision to existing requirements ([specs/requirements.md](../../specs/requirements.md)) if the research refines an existing decision.
   - A process update ([process/](../process/)) if the research changes how we work.
4. The research note's tags include the ADR numbers it informed, for traceability.

See [../process/research-prompt-design.md](../process/research-prompt-design.md) for the prompt patterns we use.
