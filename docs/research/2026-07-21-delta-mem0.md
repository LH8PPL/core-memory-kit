---
date: 2026-07-21
topic: Delta re-read of mem0ai/mem0 — what changed since the 2026-07-19 graph-memory code sweep
source: C:/Projects/research-clones/mem0 (git fetch + pull origin main, ddaa655 → 8005e18, 10 commits over ~1 day)
tags: [delta, mem0, graph, ADR-0023, D-361]
---

# Delta re-read — mem0ai/mem0 (2026-07-19 → 2026-07-21)

## What changed (commit sweep)

`git log --oneline ddaa655..origin/main` — 10 commits, all merged 2026-07-20/21:

| Commit | What |
| --- | --- |
| `9383e9a` | fix(server): scope auth DB sessions to prevent connection-pool exhaustion |
| `726bcc8` | fix(vector-stores/baidu): convert L2 distance to similarity score in search() |
| `756b0b1` | fix(ts-sdk): lazy-load optional provider SDKs in mem0ai/oss |
| `8a57967` | chore(labels): retire graph-memory, ollama, and benchmark from issue templates + labeler |
| `b05cce5` | fix(opencode-plugin): recover MEM0_API_KEY from shell profiles |
| `d0c23a5` | fix(ts-oss): don't let update() metadata overwrite user_id/agent_id/run_id |
| `fec2fe6` | fix(vector-stores): scope Pinecone delete_col()/reset() to namespace |
| `c2bc28e` | fix(memory): don't let update() metadata overwrite user_id/agent_id/run_id (Python) |
| `3955114` | fix(ts-oss): coerce non-string entity ids instead of crashing on trim() |
| `8005e18` | fix(qdrant): actually clear points on reset() for local Qdrant |

All ten are bugfixes/chores to existing surfaces (vector-store backends, tenant-identity scoping on `update()`, an opencode integration plugin, GitHub issue-template labels). None touch `mem0/memory/main.py`'s core add/search/update logic beyond the identity-field fix, and none add or restore graph-recall code.

## Re-verifying our 2026-07-19 claims

Source of the original claims: [`2026-07-19-graph-memory-code-sweep.md`](2026-07-19-graph-memory-code-sweep.md), Part 1 table row "mem0 (this clone)":

> **No graph store** — zero graph refs in `memory/main.py` (verified). spaCy-NER entities in a 2nd vector collection + `linked_memory_ids`; query = vector + entity co-occurrence score **boost** (`main.py:1757`)

Re-checked against `8005e18` (current HEAD):

- `grep -c graph mem0/memory/main.py` → **0** (same as 2026-07-19; the one commit that touched `main.py` this window, `c2bc28e`, only added an `_strip_identity_keys()` helper for `update()` — nothing graph-related).
- `find mem0 -iname '*graph*'` → **no source files** (`mem0/memory/` contains only `__init__.py, base.py, main.py, notices.py, setup.py, storage.py, telemetry.py, utils.py` — no `graph_memory.py`, no graph store module).
- `grep -rln 'graph_store\|GraphMemory\|graph_memory' --include='*.py'` repo-wide (excluding tests) → **2 hits**, both vestigial: `mem0/exceptions.py` (an error-detail dict citing `"package": "kuzu", "feature": "graph_store"` — a dependency-missing message, never raised by any live code path since nothing instantiates a graph store) and `examples/misc/strands_agent_aws_elasticache_neptune.py` (a standalone example script, not SDK code).
- Confirmed this was **already** the state at `ddaa655` (our 2026-07-19 read point) — `git ls-tree -r ddaa655` shows only doc/example graph artifacts (`docs/platform/features/graph-memory.mdx`, `examples/graph-db-demo/*.ipynb`), no `mem0/memory/graph_*.py` at that commit either. The graph-store Python module was gone **before** this delta window, not removed during it.
- One tangential hit: `mem0/vector_stores/neptune_analytics.py` uses AWS Neptune Analytics as a vector-store backend (graph DB used for vector ops, `langchain_aws.NeptuneAnalyticsGraph`) — this is a storage backend choice, not the entity/relation "graph memory" feature; it predates this window and isn't new.

**New signal this window:** `8a57967` ("chore(labels): retire graph-memory, ollama, and benchmark from issue templates + labeler", 2026-07-20) removes "Graph Memory (Neo4j, Memgraph, etc.)" as a selectable category from `bug_report.yml` / `feature_request.yml` and deletes the matching `advanced-issue-labeler.yml` rule. This is governance, not code — but it's a maintainer-visible confirmation that graph-memory is being formally pruned from the OSS repo's supported-surface taxonomy, consistent with the code already showing no graph store. (`docs/platform/features/graph-memory.mdx` still exists, so the *hosted* mem0 platform likely still markets graph memory as a paid feature — the retirement reads as "not an OSS-repo concern," not "the feature is dead everywhere.")

## Verdict

**NOTHING-MATERIAL.** The graph story is unchanged: mem0 (this OSS clone) still ships zero graph store / zero relation-extraction code, confirmed at current HEAD `8005e18`. The one new fact — the issue-template label retirement — *reinforces* rather than contradicts our 2026-07-19 finding: it's the maintainers signaling the same absence we found in code, one day later, in their own governance surface. ADR-0023's premise ("mem0's famous Neo4j graph memory is absent from the shipped OSS snapshot") holds with no revision needed.

No other claim in our other mem0-touching notes (`2026-07-02-expiry-ttl-precedents-mem0-langgraph.md`'s `expiration_date`/hide-not-delete claims, `2026-06-06-recall-deep-dive-graphiti-mem0-memoryos.md`'s ADD/UPDATE/DELETE/NONE LLM-manager prompt, `2026-07-19-recall-trigger-architecture-study.md`'s proxy always-search + floor-0.1 claims) is touched by any of the 10 commits — none of the changed files (`qdrant.py`, ts-oss entity-id handling, `memory/main.py`'s update() identity-field guard, `pinecone.py`, the opencode plugin, `vector_stores/baidu.py`, `server/main.py`'s auth session scoping) overlap with expiration, the memory-manager prompt, or the proxy/recall-trigger code paths cited in those notes. Not independently re-verified in this pass (out of scope for this delta) — flagged as an honest gap below.

## Relevance to core-memory-kit

- **Task 232/ADR-0023 (graph edges):** confirms, doesn't change, the ADR's factual premise. No action needed.
- **Task 95 (dream re-curation) / design.md §21:** none of these 10 commits touch mem0's ADD/UPDATE/DELETE/NONE memory-manager prompt (the piece previously flagged as a reusable template for Task 95's batched re-curation pass) — that finding stands unchanged from the 2026-06-06 note.
- **Tasks 233/161/203 (context-compaction):** no overlap; none of the 10 commits touch compaction/compression paths.

## Borrow candidates

- **Weak/optional:** the `_strip_identity_keys()` pattern in `c2bc28e` — silently drop caller-supplied identity/tenant-scoping fields on `update()` rather than erroring, with a `logger.warning` per dropped key. This is a defensive-immutability pattern generically useful for any system with immutable-after-creation fields, but the kit has no multi-tenant identity fields (`user_id`/`agent_id`/`run_id` equivalents) today, so relevance is speculative, not concrete.

## Reject candidates

- Everything else in the 10-commit window (Qdrant/Pinecone/Baidu vector-store fixes, ts-sdk lazy-loading, opencode-plugin API-key recovery, server auth session scoping) — all backend-specific bugfixes for stores/integrations the kit doesn't use (kit uses SQLite FTS5, not Qdrant/Pinecone/Baidu; no ts-sdk or opencode-plugin surface). No transferable mechanism.
- The issue-template label chore — administrative, not a mechanism; noted for context only.

## Honest gaps

- Did not re-verify the TTL/`expiration_date` claims (2026-07-02 note), the ADD/UPDATE/DELETE/NONE prompt claims (2026-06-06 note), or the proxy always-search/floor-0.1 claims (2026-07-19 recall-trigger-architecture-study note) against current HEAD — reasoned by file-path non-overlap with the 10 commits, not by re-running greps against those specific files/line numbers this pass. Should be treated as "presumed unchanged by absence of touching commits," not "independently re-confirmed."
- `mem0/exceptions.py`'s `"feature": "graph_store"` detail string and the `kuzu` package reference were not traced further (e.g., whether `kuzu` graph-store support exists in a separate `mem0[graph]` optional extra not present in this clone's installed deps, or is fully vestigial dead code). Flagged as unverified, not asserted either way.
- No images in this delta (pure git log + code read); no external links followed (repo-internal only).
