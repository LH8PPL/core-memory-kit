# v0.1.1 scenario test — real user journeys

**Purpose**: validate the *published* `@lh8ppl/claude-memory-kit@0.1.1` by walking the journeys a real user takes — not a smoke test. This is Task 44 done as a scenario walkthrough, and it doubles as the script to hand a friend/tester.

**Why scenarios, not "does it install"**: the 2026-05-29 engine live test ([`v0.1.0-live-test.md`](v0.1.0-live-test.md)) proved the *engine* (compression → auto-extract → snapshot → search with real Haiku) but NOT (a) the hooks firing automatically inside a real Claude Code session, nor (b) the `/plugin` marketplace flow end-to-end. Those gaps are exactly what these scenarios cover.

**Who runs what:**
- 🤖 **CLI half (Claude can run from here)**: install in a sandbox, `cmk doctor` / `search` / `repair`, compression — against the published package.
- 🧑 **In-session half (needs a human in a real Claude Code session)**: hooks firing on Stop, SessionStart injection, the `/plugin` flow, the "remember this" skill. These require an interactive Claude Code session and can't be simulated.

Test on the **published** v0.1.1 (most faithful — catches packaging issues).

---

## Scenario matrix

| # | Scenario | Who | Steps | Expected | Pass? |
|---|---|---|---|---|---|
| 1 | **Route A install (npm)** | 🤖/🧑 | `npm install -g @lh8ppl/claude-memory-kit@0.1.1`; in a fresh project: `cmk install` | scaffolds `context/`; writes 5 hooks to `.claude/settings.json`; `hooks=wired` in output | ☐ |
| 2 | **doctor green** | 🤖/🧑 | `cmk doctor` | HC-1..HC-9 reported; HC-2 (hooks) PASS; no FAILs except optional (memsearch/cron) | ☐ |
| 3 | **Route B install (plugin)** | 🧑 | `/plugin marketplace add LH8PPL/claude-memory-kit` → `/plugin install claude-memory-kit` → `/claude-memory-kit:bootstrap` → `/reload-plugins` | marketplace registers; plugin installs; bootstrap scaffolds `context/`; hooks active. **(First-ever end-to-end test of this route.)** | ☐ |
| 4 | **"Let Claude run it"** | 🧑 | In Claude Code: *"install claude-memory-kit and set it up in this project"* | Claude runs npm + `cmk install` (you approve); works without you typing commands | ☐ |
| 5 | **Auto-capture (the core loop)** | 🧑 | Restart Claude Code (`/exit` → `claude`). Work normally: state a decision + a preference (e.g. "we're using Postgres", "keep replies terse"). End the turn. | auto-extract writes those as facts to `context/MEMORY.md` at trust:high — **without** saying "remember this" | ☐ |
| 6 | **Recall (the payoff)** | 🧑 | Start a **new** session in the same project. Ask something that needs the earlier context. | SessionStart injected the snapshot; Claude answers from memory with no re-briefing | ☐ |
| 7 | **Explicit capture** | 🧑 | Say "remember this: …" or "from now on, …" | memory-write skill fires; fact lands silently (no "I saved that" noise — T5) | ☐ |
| 8 | **Search** | 🤖/🧑 | `cmk search "<term you stated earlier>"` | returns the stored memory with its citation id | ☐ |
| 9 | **Privacy tags** | 🧑 | Include a `<private>…</private>` block + a `<retain>` fact in a turn | `<private>` content NOT captured; `<retain>` kept | ☐ |
| 10 | **No-cron fallback** | 🤖/🧑 | Don't run `cmk register-crons`; accumulate session content; start a session | lazy-on-read compression bounds the scratchpad at SessionStart (Layer 6 fallback) | ☐ |
| 11 | **git portability** | 🤖/🧑 | Commit `context/`; clone the repo elsewhere | memory travels with the clone (T2) | ☐ |
| 12 | **doctor → repair recovery** | 🤖/🧑 | Remove a hook from `.claude/settings.json`; `cmk doctor`; `cmk repair --hooks` | HC-2 FAILs + surfaces `cmk repair --hooks`; repair restores it; HC-2 PASS | ☐ |
| 13 | **provenance verify** | 🤖 | `npm view @lh8ppl/claude-memory-kit@0.1.1 dist.attestations` | provenance attestation present | ☐ |

---

## Findings

_(log issues here as scenarios are run — file, scenario #, what happened, severity)_

## Verdict

_(fill after the run: is it ready to hand to a friend? what must be fixed first?)_
