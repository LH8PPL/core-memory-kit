# v0.2.0 live-test findings (2026-05-31)

Fresh self-test of v0.2.0 (the local tarball `@lh8ppl/claude-memory-kit@0.2.0`, installed globally) in a new project `C:\Temp\lior-test-2`, following [`v0.1.1-self-test-guide.md`](../process/v0.1.1-self-test-guide.md). Comparison baseline: `C:\Temp\lior-test-1` (the v0.1.1 run) — NOT Claude's native Auto Memory (the kit exists to *beat* native, not match it; native is what started this).

## STATE AT CHECKPOINT (resume here)

- **Live test paused mid-Session-1** (Stage 1 done: layered-FastAPI refactor + architecture preference stated). lior-test-2 has the kit installed (global tarball); Session-2 recall not yet run.
- **`main` is at v0.2.0, UNPUBLISHED** (version bumped + CHANGELOG finalized via `npm run release -- minor`; **no `v0.2.0` tag pushed** — publish is gated on the tag-push). PR #94 (doctor severities + release-verification lockstep + CodeQL fix) is **merged**.
- **DECISION: DO NOT cut v0.2.0** until F1 + F2 below are fixed. Shipping now = a kit that captures 2 terse bullets where v0.1.1 (and native) captured rich files. Not worth the friend handoff.

## THE HEADLINE FINDING

**F1 — capture-richness REGRESSION (v0.1.2 traded richness for safety).** This is the big one and the answer to "why don't I have memories / is this a failure?"

- **v0.1.1 (`lior-test-1`):** the agent **hand-wrote rich granular fact files** — e.g. `context/memory/feedback_layered-backend-architecture.md` and `feedback_python-venv-313-only.md`: full doctrine + **Why** + **How to apply** + `[[links]]`, written per-turn (timestamps 01:18 / 01:34, *before* the 01:56 distill — so agent-authored, not distilled). Richer than native Auto Memory. **BUT** they leaked the raw username path (`C:\Users\tamir.bn-sh\...\Python313\python.exe`) into committed memory, and used a schema the index couldn't read.
- **v0.1.2 fix** (correct, but costly): routed ALL durable writes through `cmk remember` (home-path sanitization + Poison_Guard + correct schema) and **changed the scaffolded `CLAUDE.md` to "never hand-write fact files."** `cmk remember` writes a **terse one-line bullet to MEMORY.md** — no Why, no How, no fact file.
- **v0.2.0 (`lior-test-2`) inherits this:** the agent writes `cmk remember "Lior builds FastAPI backends in layers"` — one line — where last time it wrote a 30-line doctrine file. Hence "MEMORY.md feels empty."

**Fix (get BOTH richness + safety):** make `cmk remember` (or a new `cmk fact`) accept **rich, structured** input (Why/How) and write it as a proper **granular fact file** *through* the home-path sanitizer + Poison_Guard + correct schema (reuse `writeFact()` in [`write-fact.mjs`](../../packages/cli/src/write-fact.mjs)). Then update the scaffolded `CLAUDE.md` to tell the agent to capture *richly* via the safe path again. = v0.1.1 richness + v0.1.2 safety, no leaks.

## OTHER FINDINGS

**F2 — cross-project promotion BROKEN (section-name schema failure).** Auto-extract DID fire correctly: it captured the project fact (P-26D26DAC) AND emitted a `confidence: high` cross-project `PERSONA CANDIDATE`. But it targeted `HABITS.md § "Architecture Preferences"` — **not one of HABITS.md's fixed sections** (Iteration Cadence / Destructive Operations / Communication Style). The write hit a schema error → routed to `queues/persona-review.md` with `reason: not-promoted-schema` → **HABITS.md stayed empty.** (Confirmed by reading `~/.claude-memory-kit/queues/persona-review.md` directly — the candidate is there, `confidence: high`.) So the v0.2.0 headline ("cross-project memory fills in real time") does NOT deliver in the real run — same empty-HABITS.md symptom as the v0.1.x self-test Finding #2 that Tasks 45+61 were built to fix.
- **Fix:** in `promoteCandidatesToUserTier` (auto-persona.mjs), **create the section if it doesn't exist** in the target scratchpad (instead of failing schema → queue), AND tighten the extraction-prompt routing so Haiku prefers a valid section. New test: candidate whose section doesn't pre-exist → asserts created + promoted (unit tests missed this by hard-coding valid sections + `confidence=high`).

**F3 — `cmk remember` bypasses Task-61 cross-project promotion.** Explicit captures write project-tier only; the inline promotion rides *auto-extract*, not `cmk remember`. So the agent's `cmk remember` reflex (per CLAUDE.md) routes around the cross-project layer. (Composition gap — "separately-correct-jointly-broken.") The F1 rich-capture fix should also feed cross-project promotion (classify + promote on rich-fact write).

**F4 — auto-extract conservative on build turns (minor).** Stage-0 build turns → `nothing_durable` (correct-ish: no stated preferences, facts were assistant-authored code → demoted). But env facts (FastAPI, Python 3.13, port 8000) never landed in Environment Notes. The kit under-captures project/env facts from build work. Lower priority than F1/F2.

**Working correctly:** hooks fire (HC-4 transcripts), auto-extract runs without errors (~15-25s Haiku calls), `cmk remember` works on Windows (no bash failure — the explicit path is solid), the Task-45 durable persona-review queue works (candidate landed there), `cmk search` finds project-tier facts.

## v0.2.0 GATE (revised)

Before cutting v0.2.0: **fix F1 (capture richness) + F2 (section-promotion).** F3/F4 can follow. Then re-run the live test, confirm HABITS.md fills + MEMORY.md/fact-files are rich, THEN `npm run release` is already done (main at 0.2.0) → push the `v0.2.0` tag.
