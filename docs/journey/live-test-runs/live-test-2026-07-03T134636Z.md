# live-test findings — 2026-07-03

**29 pass · 0 fail · 0 skip** (gate-fail 0; 0 known-variable recall probe fail — Task 75) · claude 2.1.199 (Claude Code)

- [x] **S0-install** — cmk 0.4.4 (real artifact, isolated)
- [x] **S1-install** —   Tip: `cmk install --with-semantic` adds local semantic recall (ask in your own words; one-time ~260 MB, no API calls).
- [x] **S1-doctor-0fail** — 6 pass · 0 fail · 4 skip (17ms)
- [x] **S1-skill-safe** — safe skill scaffolded (no Edit/Write, NEVER gate)
- [x] **S1-claudemd-slim** — CLAUDE.md block slim (skill pointer, no fat procedure)
- [x] **S1-hooks-allow** — settings.json: hooks + cmk allow-list
- [x] **S1-no-placeholder** — no {{placeholder}}; install date rendered
- [x] **S1-no-username-leak** — no username in committed scaffold
- [x] **S2-0-build** — 1 .py file(s) built
- [x] **S2-0-output** — turn output references the work
- [x] **S2-1-build** — package shape built (5 .py files)
- [x] **S2-2-build** — type hints present in built source
- [x] **S2-3-build** — .venv added to a .gitignore
- [x] **B1-autocapture** — 4/4 build-turn signals captured (no "remember this")
- [x] **B2-rich-capture** — 3 fact file(s)
- [x] **B3-persona-fills** — user tier filled with cross-project style
- [x] **B4-stated-rule-high** — uv/ruff rule promoted at trust:high
- [x] **C1-terse** — terse cmk remember → MEMORY.md
- [x] **C2-rich** — rich fact file with Why/How
- [x] **C3-poison-guard** — Poison_Guard rejected the secret (exit 2)
- [x] **C3-no-write** — rejected secret wrote NOWHERE on disk
- [x] **C4-sanitize** — home path → ~ (no username)
- [x] **S69-no-hand-edit** — no hand-edit of memory files (tools: Bash,Edit,Read,Edit)
- [x] **S69-skill-no-leak** — no username leaked from the "remember this" capture
- [x] **S69-audited** — audit.log has write entries (cmk-routed)
- [x] **D1-recall** _(probe·Task75)_ — recalled 5/5 persona signals in a fresh session
- [x] **D2-from-memory** _(probe·Task75)_ — answered from memory, no code-read (tools: Skill,Bash,ToolSearch,mcp__cmk__mk_search)
- [x] **S5-install-B** — projectB scaffolded
- [x] **E1-cold-open** — applied 3/3 persona signals (text+built; package built=true, 14 .py)

## Session-2 recall (tools: Skill,Bash,ToolSearch,mcp__cmk__mk_search)

No matter — the session-start snapshot is itself the authoritative memory index, and it already carries your standing cross-project profile. Here's what I know about how you work:

## Code structure (your strongest, most-repeated preference)
- **Boring single-responsibility modules with a thin entry point.** Each module does one thing.
- **Modules never reach into each other** — no cross-module internals grabbing.
- You **pay the structure cost upfront** deliberately, to avoid untangling god-files / refactoring debt later.

This one shows up in nearly every tier of your memory (working style, preferences, active threads), so I treat it as a hard default, not a nice-to-have.

## Typing
- **Type hints on every function signature** — no untyped signatures.
- **Target Python 3.12+** (use modern features accordingly).

## Testing habits
- **Strict TDD**: write the boundary test first, **watch it fail**, *then* implement.

## Tooling (standing rules for *every* project)
- **Package management: always `uv`, never `pip`** — for install, add, sync, lock, run.
- **Always run `ruff` before committing — never skip it** (lint/format must pass).

## Deployment
- Deploy with **Kamal to Hetzner** — **never Vercel**.

## Environment notes (machine-local)
- Your venv lives at `~\proj\.venv`.
- Local data cache at `C:\Users\tamir.bn-sh\cache\app`.

---

A couple of gaps I *don't* have recorded yet: your **communication-style** preferences and any **destructive-operations** guardrails are both blank in memory. If you want, tell me and I'll capture them so they're in force next session.

## Session-3 cold-open (built 14 .py files)

Done. The service is scaffolded, dependencies are installed, **3/3 tests pass, and ruff is clean**.

## What's here

```
pyproject.toml          # uv-managed, requires-python >=3.12, ruff configured
README.md
src/app/
  main.py               # thin entry point — create_app() builds & wires routers
  config.py             # env-driven Settings
  api/health.py         # GET /health
  api/items.py          # GET/POST /items, GET /items/{id}
  models/item.py        # ItemCreate / Item schemas
  services/items.py     # items logic (in-memory store, swappable for a DB)
tests/
  conftest.py           # client fixture
  test_health.py        # boundary test
  test_items.py         # create→fetch + 404 boundary tests
```

**Conventions honored:** uv (no pip), ruff passing, type hints on every signature, Python 3.12+. The structure keeps modules single-responsibility with a thin entry point — routers depend on services, services own logic, models own schemas; nothing reaches sideways into another module. `services/items.py` is deliberately isolated so you can swap the in-memory store for a database without touching the router.

## Run it

```bash
uv run uvicorn app.main:app --reload   # → http://127.0.0.1:8000/docs
uv run pytest                          # tests
uv run ruff check .                    # before every commit
```

One note: FastAPI's `TestClient` emits a `StarletteDeprecation

### built source (excerpt)
"""Health-check router."""

from fastapi import APIRouter

router = APIRouter(tags=["health"])


@router.get("/health")
def health() -> dict[str, str]:
    """Report that the service is up."""
    return {"status": "ok"}

"""Items resource router."""

from fastapi import APIRouter, HTTPException

from app.models.item import Item, ItemCreate
from app.services import items as service

router = APIRouter(prefix="/items", tags=["items"])


@router.get("")
def list_items() -> list[Item]:
    """List all items."""
    return service.list_items()


@router.post("", status_code=201)
def create_item(data: ItemCreate) -> Item:
    """Create a new item."""
    return service.create_item(data)


@router.get("/{item_id}")
def get_item(item_id: int) -> Item:
    """Fetch a single item by id."""
    item = service.get_item(item_id)
    if item is None:
        raise HTTPException(status_code=404, detail="item not found")
    return item

"""HTTP routers."""

"""Application settings, sourced from the environment."""

from pydantic_settings import BaseSettings, SettingsConfigDict


class Settings(BaseSettings):
    """Runtime configuration for the service."""

    model_config = SettingsConfigDict(
