# live-test findings — 2026-06-11

**29 pass · 0 fail · 0 skip** (gate-fail 0; 0 known-variable recall probe fail — Task 75) · claude 2.1.170 (Claude Code)

- [x] **S0-install** — cmk 0.3.0 (real artifact, isolated)
- [x] **S1-install** —   Tip: `cmk install --with-semantic` adds local semantic recall (ask in your own words; one-time ~260 MB, no API calls).
- [x] **S1-doctor-0fail** — 4 pass · 0 fail · 3 skip (8ms)
- [x] **S1-skill-safe** — safe skill scaffolded (no Edit/Write, NEVER gate)
- [x] **S1-claudemd-slim** — CLAUDE.md block slim (skill pointer, no fat procedure)
- [x] **S1-hooks-allow** — settings.json: hooks + cmk allow-list
- [x] **S1-no-placeholder** — no {{placeholder}}; install date rendered
- [x] **S1-no-username-leak** — no username in committed scaffold
- [x] **S2-0-build** — 1 .py file(s) built
- [x] **S2-0-output** — turn output references the work
- [x] **S2-1-build** — package shape built (3 .py files)
- [x] **S2-2-build** — type hints present in built source
- [x] **S2-3-build** — .venv added to a .gitignore
- [x] **B1-autocapture** — 4/4 build-turn signals captured (no "remember this")
- [x] **B2-rich-capture** — 1 fact file(s)
- [x] **B3-persona-fills** — user tier filled with cross-project style
- [x] **B4-stated-rule-high** — uv/ruff rule promoted at trust:high
- [x] **C1-terse** — terse cmk remember → MEMORY.md
- [x] **C2-rich** — rich fact file with Why/How
- [x] **C3-poison-guard** — Poison_Guard rejected the secret (exit 2)
- [x] **C3-no-write** — rejected secret wrote NOWHERE on disk
- [x] **C4-sanitize** — home path → ~ (no username)
- [x] **S69-no-hand-edit** — no hand-edit of memory files (tools: Glob,Read,Edit)
- [x] **S69-skill-no-leak** — no username leaked from the "remember this" capture
- [x] **S69-audited** — audit.log has write entries (cmk-routed)
- [x] **D1-recall** _(probe·Task75)_ — recalled 5/5 persona signals in a fresh session
- [x] **D2-from-memory** _(probe·Task75)_ — answered from memory, no code-read (tools: (none))
- [x] **S5-install-B** — projectB scaffolded
- [x] **E1-cold-open** — applied 3/3 persona signals (text+built; package built=true, 12 .py)

## Session-2 recall (tools: (none))

From the injected memory snapshot, here's what's recorded about how you work:

**Language & Runtime**
- Python 3.12+ minimum, no exceptions

**Package Management**
- Always `uv` — never `pip`

**Code Style**
- Type hints on every function signature, always
- Single-responsibility modules with a thin entry point
- Modules never reach into each other (no cross-dependencies)
- Prefer upfront structural cost over untangling a monolith later

**Linting**
- Always run `ruff` before committing — never skip it

**Testing**
- TDD: write the boundary test first, watch it fail, then implement

**Deployment**
- Kamal to Hetzner — never Vercel

**Venv**
- Lives at `~/proj/.venv`

That's the full picture currently recorded. If anything is missing or wrong, say the word and I'll update the record.

## Session-3 cold-open (built 12 .py files)

All clean. Here's what was created:

```
projectB/
├── pyproject.toml           # uv project, ruff config, pytest config
├── .python-version          # pins 3.12
├── src/app/
│   ├── main.py              # thin entry point — creates app, mounts routers
│   ├── core/
│   │   └── config.py        # Settings via pydantic-settings (.env aware)
│   ├── api/
│   │   ├── health.py        # GET /health
│   │   └── items.py         # full CRUD for /api/v1/items (in-memory store)
│   └── models/
│       └── item.py          # Item, ItemCreate, ItemUpdate Pydantic models
└── tests/
    ├── conftest.py          # async httpx client fixture
    └── test_health.py       # first passing test
```

**To run the dev server:**
```
uv run uvicorn app.main:app --reload
```

Docs are live at `http://localhost:8000/docs`.

The in-memory `_store` in `items.py` is the obvious first thing to replace once you wire up a real database. What should this API actually do?

### built source (excerpt)
from fastapi import APIRouter
from pydantic import BaseModel

router = APIRouter(tags=["health"])


class HealthResponse(BaseModel):
    status: str


@router.get("/health", response_model=HealthResponse)
async def health_check() -> HealthResponse:
    return HealthResponse(status="ok")

from fastapi import APIRouter, HTTPException

from app.models.item import Item, ItemCreate, ItemUpdate

router = APIRouter(prefix="/items", tags=["items"])

_store: dict[str, Item] = {}


@router.get("", response_model=list[Item])
async def list_items() -> list[Item]:
    return list(_store.values())


@router.post("", response_model=Item, status_code=201)
async def create_item(body: ItemCreate) -> Item:
    item = Item(**body.model_dump())
    _store[item.id] = item
    return item


@router.get("/{item_id}", response_model=Item)
async def get_item(item_id: str) -> Item:
    item = _store.get(item_id)
    if item is None:
        raise HTTPException(status_code=404, detail="Item not found")
    return item


@router.patch("/{item_id}", response_model=Item)
async def update_item(item_id: str, body: ItemUpdate) -> Item:
    item = _store.get(item_id)
    if item is None:
        raise HTTPException(
