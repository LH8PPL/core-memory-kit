# live-test findings — 2026-06-11

**28 pass · 1 fail · 0 skip** (gate-fail 1; 0 known-variable recall probe fail — Task 75) · claude 2.1.170 (Claude Code)

- [x] **S0-install** — cmk 0.3.0 (real artifact, isolated)
- [x] **S1-install** —   Tip: `cmk install --with-semantic` adds local semantic recall (ask in your own words; one-time ~260 MB, no API calls).
- [x] **S1-doctor-0fail** — 4 pass · 0 fail · 3 skip (8ms)
- [x] **S1-skill-safe** — safe skill scaffolded (no Edit/Write, NEVER gate)
- [x] **S1-claudemd-slim** — CLAUDE.md block slim (skill pointer, no fat procedure)
- [x] **S1-hooks-allow** — settings.json: hooks + cmk allow-list
- [x] **S1-no-placeholder** — no {{placeholder}}; install date rendered
- [x] **S1-no-username-leak** — no username in committed scaffold
- [x] **S2-0-build** — 1 .py file(s) built
- [x] **S2-0-output** — turn output references the work
- [x] **S2-1-build** — package shape built (4 .py files)
- [x] **S2-2-build** — type hints present in built source
- [x] **S2-3-build** — .venv added to a .gitignore
- [x] **B1-autocapture** — 3/4 build-turn signals captured (no "remember this")
- [x] **B2-rich-capture** — 3 fact file(s)
- [x] **B3-persona-fills** — user tier filled with cross-project style
- [ ] **B4-stated-rule-high** — uv/ruff rule promoted at trust:high
- [x] **C1-terse** — terse cmk remember → MEMORY.md
- [x] **C2-rich** — rich fact file with Why/How
- [x] **C3-poison-guard** — Poison_Guard rejected the secret (exit 2)
- [x] **C3-no-write** — rejected secret wrote NOWHERE on disk
- [x] **C4-sanitize** — home path → ~ (no username)
- [x] **S69-no-hand-edit** — no hand-edit of memory files (tools: Glob,Read,Edit)
- [x] **S69-skill-no-leak** — no username leaked from the "remember this" capture
- [x] **S69-audited** — audit.log has write entries (cmk-routed)
- [x] **D1-recall** _(probe·Task75)_ — recalled 5/5 persona signals in a fresh session
- [x] **D2-from-memory** _(probe·Task75)_ — answered from memory, no code-read (tools: (none))
- [x] **S5-install-B** — projectB scaffolded
- [x] **E1-cold-open** — applied 3/3 persona signals (text+built; package built=true, 9 .py)

## Session-2 recall (tools: (none))

From the injected memory snapshot, here's what's recorded about how you work:

**Language & Runtime**
- Python 3.12+ only
- Type hints on every function signature, no exceptions

**Package Management**
- `uv` always — never `pip`

**Code Structure**
- Single-responsibility modules with thin entry points
- Modules never reach into each other — no cross-module imports sideways
- Pay the structure cost upfront rather than untangling god-files later

**Testing**
- Test-first: write a failing boundary test, watch it fail, then implement
- No mocks at the database layer (burned by mock/prod divergence before)

**Linting / Commits**
- Always run `ruff` before committing — never skip it

**Deployment**
- Kamal to Hetzner — never Vercel

**Anti-patterns to avoid**
- God-files
- Abstractions beyond what the current task requires
- Half-finished implementations

That's the full picture from the current snapshot. If you want me to search the deeper archive for anything more specific (e.g. framework preferences, naming conventions, CI setup), I can run `cmk search` on a topic.

## Session-3 cold-open (built 9 .py files)

Clean pass, no warnings. Here's what was created:

```
projectB/
├── src/api/
│   ├── app.py          # create_app() factory — wires routers
│   ├── main.py         # thin entry point: app = create_app()
│   ├── models/
│   │   └── responses.py  # Pydantic response models
│   └── routes/
│       └── health.py   # GET /health → {"status": "ok"}
├── tests/
│   └── test_health.py  # boundary test
├── pyproject.toml      # uv, Python 3.12+, ruff, pytest-asyncio
└── .python-version     # 3.12
```

**Run the dev server:** `uv run uvicorn api.main:app --reload`

Docs available at `http://localhost:8000/docs` once running. What domain/resource do you want to add next?

### built source (excerpt)
from fastapi import FastAPI

from api.routes import health


def create_app() -> FastAPI:
    app = FastAPI(title="API", version="0.1.0")
    app.include_router(health.router)
    return app

from api.app import create_app

app = create_app()

from pydantic import BaseModel


class HealthResponse(BaseModel):
    status: str


from fastapi import APIRouter

from api.models.responses import HealthResponse

router = APIRouter(prefix="/health", tags=["health"])


@router.get("", response_model=HealthResponse)
async def health_check() -> HealthResponse:
    return HealthResponse(status="ok")



from fastapi.testclient import TestClient

from api.main import app

client = TestClient(app)


def test_health_check_returns_ok() -> None:
    response = client.get("/health")
    assert response.status_code == 200
    assert response.json() == {"status": "ok"}


[project]
name = "api"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pydantic>=2.10.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx2>=0.28.0",
    "ruff>=0.9.0",
]

[build-system]
requires = ["hatchl
